---
phase: 01.1-reconcile-the-ibp-method-version
plan: 01
subsystem: docs
tags: [ibp, method-version, comparison, documentation]
requires: []
provides:
  - "docs/technical/ibp-version-comparison-v3.0-v3.2.md: factor-by-factor v3.0 (app) vs v3.2 register with API, mobile and matrix impact"
affects:
  - "01.1-02 (ADR-003 decides on this comparison; its CH-1..CH-12 cite these row IDs)"
  - "01.1-03 (v3.2 target matrix cases MAT-A-03..MAT-VER-01)"
  - "01.8 (implements the change list)"
  - "phase 2 (Factor A genus list, Table 1 and Table 2 species)"
tech-stack:
  added: []
  patterns: ["7-column difference rows with printed v3.2 page, no pipes inside cells"]
key-files:
  created:
    - docs/technical/ibp-version-comparison-v3.0-v3.2.md
  modified: []
decisions:
  - "Baseline for the v3.0 column is the app's own rules (D-02); cross-checks labelled '2021 v3 sheet', never 'v3.0 (2023)'"
  - "G-5 and H-4 given severity 'none between versions (app bug BUG-2)' to fit the four-value severity key"
  - "Summary counts state 56 rows: 7 engine rows (4 changes), 20 field rows + 2 linear-only, 5 minor, 9 wording, 11 none, 2 app-bug"
metrics:
  duration: "~20 min"
  completed: 2026-09-26
  tasks: 2
  files: 1
---

# Phase 01.1 Plan 01: IBP method version comparison Summary

Page-cited, 452-line comparison of the app's IBP Fr v3.0 rules against IBP FR v3.2 (SHA-256 `f130b3d6…d001`). It covers 56 difference rows across the classification model, A to J and the global score, the complete Factor A genus diff with the Table 1 and Table 2 species, six app bugs, and the 17 matrix cases recomputed under v3.2 plus 9 proposed cases.

## What was done

- **Task 1 (8389135):** Created `docs/technical/ibp-version-comparison-v3.0-v3.2.md` with the header (status, date, ADR-003 link, summary), `## Sources and baseline` (v3.2 URL, size, SHA-256, retrieval date, cited pages; the 2021 sheet SHA-256 and caveat; the dead 2023 links; the D-02 baseline statement; how to reproduce), `## How to read the tables`, the classification model (CLS-1..6 and the cas mapping table), Factor A (A-1..10), the genus list (33 genera plus the Quercus split, changes, Table 1 species as Latin names, Table 2 exclusions), Factor B (B-1..4), Factor C (CD-1..6) and a separate Factor D section that points to the CD rows and covers D's own points (CD-5, MAT-D-01).
- **Task 2 (8c173cc):** Appended Factors E to J (E-1..5, F-1..7, G-1..5, H-1..4, I-1..3, J-1..3), the global score (GS-1..3), summary counts, the bug table (BUG-1..5, DRIFT-1, 7 cells each), the 17-case matrix recomputation and 9 proposed cases (MAT-A-03, A-04, A-05, C-02, E-02, G-02, G-03, H-02, VER-01), the CNPF questions (Ficus, Pistacia, linear C/D table), assumptions A1 to A6, and the change-list pointer to ADR-003.

## Row counts

- Difference rows: 56 (CLS 6, A 10, B 4, CD 6, E 5, F 7, G 5, H 4, I 3, J 3, GS 3), no duplicates, all 7 cells filled, every v3.2 cell has "p. N".
- Bug/drift rows: 6, all 7 cells filled.
- Matrix: 17 existing cases plus 9 proposed.
- Summary counts: 4 engine-level score changes (7 rows); 20 field-definition rows plus 2 linear-only (F-5, G-3); 5 minor; 9 wording only; 11 no change; 2 app bug (G-5, H-4). Total 56.

## Verification

- Task 1 and Task 2 `<automated>` commands both print `OK` (run from the worktree root).
- No `\|` in the file; no French quote of 120 characters or more; `git status --porcelain` shows no `.pdf`. The PDF hash was re-checked with `sha256sum` against the scratchpad copy.

## Deviations from Plan

**1. [Rule 3 - Blocking] Verify commands run from the worktree, not `/home/user/cortege`.** The plan's verify commands `cd /home/user/cortege`. The worktree isolation refuses that, and the file lives in the worktree, so the same checks were run from the worktree root through a scratchpad script. Only the directory changed; the checks are identical.

**2. [Presentation] Severity for G-5 and H-4.** RESEARCH writes "app bug (BUG-2)" as the severity. The plan's key has four values, so these rows say "none between versions (app bug BUG-2)", and the summary counts list them as a separate 2-row group so the total still reaches 56.

**3. [Completeness] Extra "no change" rows counted.** RESEARCH's headline "No change" list leaves out A-10 and F-4. The summary counts include them (11 rows) so that every one of the 56 rows falls in exactly one group.

## Known Stubs

None. The `## Change list` section intentionally points to ADR-003 (plan 01.1-02, running in parallel) instead of duplicating CH-1..CH-12, as the plan requires. The ADR-003 link resolves once plan 02 lands.

## Threat Flags

None. No PDF or extracted text was added. Cells labelled "2021 v3 sheet" carry the cross-checks, and both source hashes are recorded (T-01.1-01, T-01.1-02 mitigated).

## Self-Check: PASSED

- FOUND: docs/technical/ibp-version-comparison-v3.0-v3.2.md
- FOUND: 8389135
- FOUND: 8c173cc
