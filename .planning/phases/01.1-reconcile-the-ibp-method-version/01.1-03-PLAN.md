---
phase: 01.1-reconcile-the-ibp-method-version
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/references/README.md
  - docs/specs/ibp-form-spec.md
  - docs/technical/ibp-validation-matrix-v1.md
autonomous: true
requirements: [REQ-QA-ibp-version]

must_haves:
  truths:
    - "docs/references/README.md and docs/specs/ibp-form-spec.md each say the app implements IBP Fr v3.0 today and targets IBP FR v3.2 (02/02/2026) per ADR-003, landing in phase 01.8; neither claims the app implements v3.2 (ROADMAP criterion 5)"
    - "The dead v3.0 links are kept but labelled as no longer published by CNPF (HTTP 404, checked 2026-09-26); the v3.2 URL, date and SHA-256 are added, and 'Cite as' gives both references"
    - "The form spec flags, without rewriting its v3.0 thresholds, the known divergences: the native-cover cap on B (BUG-1, CH-1), `ibp_method_version` not implemented (BUG-4, CH-6), and the region/stage model replaced by cas 1-4 in v3.2"
    - "The validation matrix keeps its 17 v3.0 cases byte-for-byte, marks which ones change under v3.2, and appends the proposed v3.2 cases in a separate section clearly marked 'v3.2 target, not implemented yet'"
  artifacts:
    - path: docs/references/README.md
      provides: "Implemented/target version statement, v3.2 source, dead-link note"
      contains: "ADR-003"
    - path: docs/specs/ibp-form-spec.md
      provides: "Method version status, v3.2 reference, change-log entry"
      contains: "IBP_FR_v3_2_260202.pdf"
    - path: docs/technical/ibp-validation-matrix-v1.md
      provides: "v3.2 impact annotations and v3.2 target cases"
      contains: "v3.2 target"
  key_links:
    - from: "docs/references/README.md"
      to: "docs/technical/adr-003-ibp-method-version-v1.md"
      via: "relative link ../technical/adr-003-ibp-method-version-v1.md"
      pattern: "\\.\\./technical/adr-003-ibp-method-version-v1\\.md"
    - from: "docs/technical/ibp-validation-matrix-v1.md"
      to: "docs/technical/ibp-version-comparison-v3.0-v3.2.md"
      via: "relative link from the method-version note"
      pattern: "ibp-version-comparison-v3\\.0-v3\\.2\\.md"
---

<objective>
Make the two citation files state the version the app actually implements (v3.0) and the adopted target (v3.2, ADR-003, phase 01.8), and annotate the validation matrix with its v3.2 impact and the v3.2 target cases.

Purpose: ROADMAP criterion 5 has a wording trap (RESEARCH Pitfall 1): the code still runs v3.0 after this phase, so writing "the app implements v3.2" would make the criterion false. The matrix is the reference 01.8's parity fixture will be built from (CH-10), so it must show which cases move.
Output: edits to three Markdown files. No code change; no test reads the matrix file (checked at planning time: only `.planning/intel/classifications/` mentions it), but its 17 case rows are kept verbatim anyway.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-CONTEXT.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-RESEARCH.md

<interfaces>
Current state (read at planning time):
- `docs/references/README.md`: section "## IBP methodology — CNPF / INRAE Dynafor" lists the IBP home page `https://www.cnpf.fr/ibp`, two v3.0 PDF links (`…ibp_def_fr_v3_0_230323_0.pdf`, `…ibp_rel_fr_v3_0_230323_0.pdf`, both HTTP 404 per D-02), the survey-methods link `…ibp_methodes_de_releve_v221010_0.pdf`, and "Cite as: Gonin P., Larrieu L., 2022 — *Indice de Biodiversité Potentielle (IBP Fr v3.0)*. CNPF, INRAE Dynafor." Then "The scoring rules implemented in this app are derived from these documents" with links to the spec and matrix. "## Working locally" says to download the PDFs from the links above.
- `docs/specs/ibp-form-spec.md`: §2 Scope lists "IBP Fr v3.0 biogeographic versions" ACA/M (l. 15-18); §3 Metadata "Target IBP version (required field): `ibp_method_version`", recommended default `cnpf_ibp_fr_v3_0_2023-03-23` (l. 30-31); §5 "Version and Region Typology" (l. 57); §6 Factor B (l. 101) holds the < 50 % native-cover cap; §8.1 blocking validations; §10.3 payload example with `ibp_method_version` (l. 387); §12 "Official References (Primary Source)" (l. 443-447, CNPF page `https://www.cnpf.fr/n/ibp/n:2006` + the same three PDFs); §13 Change Log (l. 449-450).
- `docs/technical/ibp-validation-matrix-v1.md` (51 lines): `## Status` "Accepted for implementation baseline (2026-03-09)"; `## Case Matrix` with 17 rows `| MAT-… |` (l. 20-36); `## Aggregate Score Rules`; `## Notes` with "Allowed scores: A..H: `0 | 1 | 2 | 5`; I, J: `0 | 2 | 5`".

v3.2 facts to add (RESEARCH "Sources used"): URL `https://www.cnpf.fr/sites/socle/files/2026-04/IBP_FR_v3_2_260202.pdf`, dated 02/02/2026, 28 pp., SHA-256 `f130b3d66522ec0f5692e87e361b628b4941ce9748d1ed5519a49734854bd001`, retrieved 2026-09-26. Reference string from its p. 28 (in our own formatting): Gonin P., Larrieu L., Marty P., Baiges T., Corezzola S., Miozzo M., Palero N., 2026 — *Indice de Biodiversité Potentielle pour les forêts de France métropolitaine (IBP FR v3.2) : définition et fiches de relevé*. CNPF, INRAE Dynafor, 02/02/26, 28 p. v3.2 also contains the survey instructions (p. 18-19).

Required status wording (RESEARCH "Criterion 5 wording"), in substance in both citation files: "Implemented: IBP Fr v3.0 (as transcribed in the form spec and validation matrix; the official v3.0 PDFs are no longer published). Target: IBP FR v3.2 (02/02/2026), adopted by ADR-003; the rules change in phase 01.8." Forbidden: any sentence saying the app implements/is conformant with v3.2.

Matrix v3.2 impact (RESEARCH "Validation matrix under v3.2"): MAT-B-01 value changes (B = 5, cap moves to A; A capped at 2 if its raw score is above 2); MAT-A-02 becomes cas 3 (A = 2) but A = 1 if lower subalpine (cas 1); MAT-A-01 and MAT-G-01 context → cas 1 (values unchanged); MAT-SUBMIT-01 error list grows if cas/method version become required; MAT-SUBMIT-02 context → cas (same totals); the other 11 unchanged. Proposed v3.2 target cases: MAT-A-03 (cas 1, count 5, native cover 40 → A = 2), MAT-A-04 (cas 1, count 5, cover 50 → A = 5), MAT-A-05 (cas 2 in a cas-3 zone or special habitat, count 2 → A = 2), MAT-C-02 (bmg 1, bmm 1, surface 2 → C = 1; v3.0 gave 0), MAT-E-02 (tgb 1, gb 1, surface 2 → E = 1; v3.0 gave 0), MAT-G-02 (cas 3: 0.5 % → G = 2, 6 % → G = 5), MAT-G-03 (direct G = 1 → blocking), MAT-H-02 (direct H = 1 or class_score 1 → blocking), MAT-VER-01 (a v3.0-tagged payload is scored with v3.0 rules; an identical replay of a submitted v3.0 survey stays accepted).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Citation files state implemented v3.0 and target v3.2 (criterion 5)</name>
  <files>docs/references/README.md, docs/specs/ibp-form-spec.md</files>
  <read_first>
    - docs/references/README.md (whole file)
    - docs/specs/ibp-form-spec.md lines 1-70, 95-125, 305-325, 360-420, 440-452
  </read_first>
  <action>First check the three old CNPF links once with `curl -sS -o /dev/null -w '%{http_code}' -L <url>` (through the configured proxy); record the codes in the SUMMARY; if the network fails, write "not re-checked" in the SUMMARY and rely on D-02 for the two v3.0 PDFs. In `docs/references/README.md`: add a "### Method version status" block at the top of the IBP section with the required wording and links to `../technical/adr-003-ibp-method-version-v1.md` and `../technical/ibp-version-comparison-v3.0-v3.2.md`; add the v3.2 link with date, page count and SHA-256; keep the v3.0 links but mark each "no longer online (HTTP 404, checked 2026-09-26): CNPF no longer publishes v3.0" (mark the survey-methods link the same way only if curl showed 404; say v3.2 now includes the survey instructions, p. 18-19); make "Cite as" list both references (v3.0 as the implemented method, v3.2 as the target) and keep authors "Gonin P., Larrieu L." plus the v3.2 author list; change "The scoring rules implemented in this app are derived from these documents" so it says they are derived from v3.0; in "Working locally" say the v3.0 PDFs can no longer be downloaded. In `docs/specs/ibp-form-spec.md`: add under §2 Scope a "Method version status" note with the required wording and the two links (`../technical/...`); in §3 add that `ibp_method_version` is specified but not implemented yet (no column, DTO or mobile field; BUG-4), to be added by phase 01.8 or 2 (CH-6); in §5 add one note that v3.2 replaces region/stage with cas 1-4 (CLS-1, see the comparison); in §6 Factor B next to the native-cover cap add a "Known divergence" note: both CNPF v3 editions put this cap on Factor A, the app applies it to B, fixed in 01.8 (BUG-1, CH-1); in §12 mark the v3.0 links as no longer online, add the v3.2 link (date, SHA-256) labelled "target method (ADR-003)" and the `https://www.cnpf.fr/ibp` home page; add a §13 change-log line "2026-09-26: phase 01.1: method version status (implemented v3.0, target v3.2 per ADR-003), dead v3.0 links marked, v3.2 reference added, known divergences flagged". Do not rewrite any v3.0 threshold or rule text to v3.2 (the spec must keep matching the code until 01.8).</action>
  <verify>
    <automated>cd /home/user/cortege && for f in docs/references/README.md docs/specs/ibp-form-spec.md; do grep -iE "v3\.0" "$f" | grep -qiE "implement" || { echo "$f: implemented v3.0 not stated"; exit 1; }; grep -q "ADR-003" "$f" || { echo "$f: no ADR-003"; exit 1; }; grep -q "adr-003-ibp-method-version-v1.md" "$f" || { echo "$f: no ADR link"; exit 1; }; grep -q "IBP_FR_v3_2_260202.pdf" "$f" || { echo "$f: no v3.2 URL"; exit 1; }; grep -qi "01.8" "$f" || { echo "$f: no 01.8"; exit 1; }; grep -qiE "no longer (online|published)" "$f" || { echo "$f: dead links not marked"; exit 1; }; test -z "$(grep -iE "(implements|implemented:?|conformant with|conforms to) (the )?(IBP )?(FR |Fr )?v3\.2" "$f")" || { echo "$f: claims v3.2 implemented"; exit 1; }; done && grep -q "f130b3d66522ec0f5692e87e361b628b4941ce9748d1ed5519a49734854bd001" docs/references/README.md && grep -q "2026-09-26" docs/specs/ibp-form-spec.md && grep -q "BUG-1\|CH-1" docs/specs/ibp-form-spec.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Each file has a line naming v3.0 as implemented, links ADR-003, carries the v3.2 URL and mentions phase 01.8
    - Neither file contains a sentence claiming the app implements or conforms to v3.2
    - The v3.0 links are still present and labelled as no longer online
    - The spec's v3.0 thresholds are unchanged (`git diff docs/specs/ibp-form-spec.md` shows only added lines plus edits in §3, §12 and the "Recommended default" / scope lines)
  </acceptance_criteria>
  <done>Criterion 5 holds: both files cite the implemented version (v3.0) and the adopted target (v3.2, ADR-003, 01.8).</done>
</task>

<task type="auto">
  <name>Task 2: Validation matrix: v3.2 impact per case and v3.2 target cases</name>
  <files>docs/technical/ibp-validation-matrix-v1.md</files>
  <read_first>
    - docs/technical/ibp-validation-matrix-v1.md (whole file)
    - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-RESEARCH.md lines 404-436
  </read_first>
  <action>Keep the 17 existing `| MAT-… |` rows and the table header byte-for-byte. Add a `## Method version` section right after `## Status`: the cases encode IBP Fr v3.0 as implemented by the app; ADR-003 adopts FR v3.2 for phase 01.8, which produces `ibp-validation-matrix-v2.md` (CH-10); link `ibp-version-comparison-v3.0-v3.2.md` and `adr-003-ibp-method-version-v1.md`. After the Case Matrix table, add `## Impact of v3.2 on these cases` as a table `| Case | Under v3.2 | Reference |` with rows MAT-A-01, MAT-A-02, MAT-B-01, MAT-G-01, MAT-SUBMIT-01, MAT-SUBMIT-02 per the interfaces block (reference the comparison row IDs, e.g. A-1/B-1, CLS-2) and one sentence that the other 11 cases keep their expected values under v3.2. Then add `## v3.2 target cases (not implemented yet)` opening with a bold warning that these cases are specified for phase 01.8, fail against today's v3.0 engine by design, and must not be added to `api/test/ibp-rules.spec.ts` or the mobile tests before 01.8; then a table with the same four columns as the Case Matrix (`| ID | Context | Input | Expected |`) holding MAT-A-03, MAT-A-04, MAT-A-05, MAT-C-02, MAT-E-02, MAT-G-02, MAT-G-03, MAT-H-02, MAT-VER-01, using v3.2 field names only where they exist today and otherwise naming the input in words (for example "A native cover 40 %"), since the payload shape is 01.8's decision. In `## Notes`, under "Allowed scores", add a sub-bullet (do not delete the existing line, it describes the current engine): both CNPF versions allow only 0, 2, 5 for G and H; the app accepts 1 (BUG-2), fixed in 01.8 (CH-5).</action>
  <verify>
    <automated>cd /home/user/cortege && m=docs/technical/ibp-validation-matrix-v1.md && t=$(mktemp) && git show a674354:$m | grep '^| MAT-' > "$t" && test "$(wc -l < "$t")" -eq 17 && while IFS= read -r r; do grep -Fxq -- "$r" "$m" || { echo "row changed: $r"; exit 1; }; done < "$t" && rm -f "$t" && test "$(sed -n '/^## Case Matrix/,/^## /p' "$m" | grep -c '^| MAT-')" -eq 17 && grep -qE '^## Method version' "$m" && grep -qE '^## Impact of v3\.2' "$m" && grep -qE '^## v3\.2 target cases \(not implemented yet\)' "$m" && for c in MAT-A-03 MAT-A-04 MAT-A-05 MAT-C-02 MAT-E-02 MAT-G-02 MAT-G-03 MAT-H-02 MAT-VER-01; do sed -n '/^## v3\.2 target cases/,$p' "$m" | grep -qE "^\| *$c *\|" || { echo "missing $c"; exit 1; }; done && grep -q "ibp-version-comparison-v3.0-v3.2.md" "$m" && grep -q "BUG-2" "$m" && grep -q 'A..H: `0 | 1 | 2 | 5`' "$m" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The 17 original case rows are unchanged and remain the only rows in the Case Matrix section
    - The v3.2 impact table covers MAT-A-01, A-02, B-01, G-01, SUBMIT-01, SUBMIT-02 and states the other 11 are unchanged
    - The v3.2 target section is headed "(not implemented yet)", warns against adding the cases to current tests, and lists the nine proposed cases
    - `npm run test:unit` is unaffected (no code touched; verified again in plan 01.1-04)
  </acceptance_criteria>
  <done>The matrix shows which v3.0 cases move under v3.2 and carries the v3.2 target cases for 01.8, without changing the current baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → developers and phase 01.8 | Docs that misstate the implemented method mislead implementers and observers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-06 | Repudiation | Citation files claiming v3.2 conformance before 01.8 | mitigate | Verify step fails on any "implements/conformant … v3.2" sentence and requires an explicit "implemented v3.0" line in both files |
| T-01.1-07 | Tampering | Baseline matrix rows silently altered | mitigate | Verify step compares every original MAT row with `git show a674354:` (the pre-phase research commit) byte-for-byte; v3.2 cases live in a separate, warned section |
| T-01.1-08 | Information Disclosure | Copying CNPF text into the spec | accept | Only URLs, dates, a hash and a bibliographic reference are added; no method text |
</threat_model>

<verification>
- Both task verify commands pass; `git status --porcelain` lists only the three Markdown files
</verification>

<success_criteria>
ROADMAP 01.1 criterion 5 is true (both files cite v3.0 as implemented and v3.2 as the ADR-003 target) and the matrix carries the v3.2 impact and target cases for 01.8.
</success_criteria>

<output>
Create `.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-03-SUMMARY.md` when done
</output>
