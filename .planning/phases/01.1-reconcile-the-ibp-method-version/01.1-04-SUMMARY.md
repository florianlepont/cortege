---
phase: 01.1-reconcile-the-ibp-method-version
plan: 04
subsystem: docs
tags: [ibp, method-version, roadmap, phase-gate, validation]
requires:
  - "01.1-01 (comparison document)"
  - "01.1-02 (ADR-003, CH-1..CH-12)"
  - "01.1-03 (citation files, matrix v3.2 status)"
provides:
  - "ADR-003 and the v3.0/v3.2 comparison indexed in CLAUDE.md and docs/README.md (D-05)"
  - "ROADMAP binding: phase 01.8 input line + criterion 6 (v3.2 per ADR-003), phase 2 input line (CH-12)"
  - "Re-runnable D-08 check script 01.1-doc-checks.sh"
  - "01.1-VALIDATION.md at status: validated with gate results and owner items"
affects:
  - "phase 01.8 (must implement CH-1..CH-11 and dispatch by method version)"
  - "phase 2 (Factor A genus list from v3.2, CH-12)"
tech-stack:
  added: []
  patterns: ["Phase gate as a bash check script that prints FAIL lines and ends with ALL DOC CHECKS PASSED"]
key-files:
  created:
    - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh
  modified:
    - CLAUDE.md
    - docs/README.md
    - .planning/ROADMAP.md
    - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md
decisions:
  - "Phase 01.8 success criterion 6: IBP rules implement FR v3.2 per ADR-003, v3.0 rules kept for surveys tagged v3.0 or untagged; citation files then say v3.2 is implemented"
  - "The check script gains a §9 cross-reference check (row, BUG/DRIFT, CH and MAT IDs must be defined) beyond the plan's checks 1-8"
metrics:
  duration: "~25 min"
  completed: 2026-09-26
  tasks: 2
  files: 5
requirements: [REQ-QA-ibp-version]
---

# Phase 01.1 Plan 04: Indexing, ROADMAP binding and phase gate Summary

ADR-003 and the comparison are now indexed in CLAUDE.md and docs/README.md. The ROADMAP requires phase 01.8 to deliver the v3.2 migration (an input line and criterion 6) and phase 2 to build the genus list from v3.2 (CH-12). A re-runnable check script passes every D-08 check, and the full suite is green. 01.1-VALIDATION.md is now `status: validated`.

## Tasks

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Index ADR-003 and the comparison; write the migration into the ROADMAP | 6342557 | CLAUDE.md, docs/README.md, .planning/ROADMAP.md |
| 2 | Phase gate: doc checks, full suite, VALIDATION.md validated | 1f57141 | 01.1-doc-checks.sh, 01.1-VALIDATION.md |

## What was done

- **CLAUDE.md** (+3 lines, 0 removed): two tree entries between ADR-002 and the spike doc, with the `#` comments aligned to column 49 like their neighbours. It also gains a "Method version:" line after the Factor validation matrix line in the IBP domain section.
- **docs/README.md**: one sentence under `docs/technical/` linking ADR-003 and the comparison.
- **ROADMAP**:
  - Phase 01.8: an `**Input from Phase 01.1**` line after **Source** (CH-1..CH-11, CH-6 dispatch, matrix v2 CH-10), and criterion `  6.` after criterion 5. Criteria 1-5 are byte-identical to a674354.
  - Phase 2: an `**Input from Phase 01.1**` line after **Requirements** (CH-12).
- **01.1-doc-checks.sh**: implements checks 1-8 from the plan's interfaces block, plus §9 cross-references (see Deviations).
- **01.1-VALIDATION.md**: status validated. The results and statuses are filled in, as are the gate results (check-script output, suite results with counts, hygiene evidence) and the four owner items.

## Gate results

- `bash .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh` → `ALL DOC CHECKS PASSED`.
- `npm run lint`, `npm run typecheck`, `npm run format:check`: exit 0.
- `npm run test:unit`: exit 0. API: 29 suites, 638 tests. Mobile: 81 suites, 1050 tests.
- `git diff --name-only a674354 -- api mobile packages .github infra package.json package-lock.json`: empty. The matching `git status --porcelain` is empty once the node_modules symlinks were removed. `git ls-files '*.pdf'`: empty.
- Wave-1 cross-links: all relative links in the comparison, ADR-003, ADR-002, the spike doc, the matrix, the references README, the form spec and docs/README.md resolve.
- Wave-1 IDs: plan 03's citations (CLS-1, CLS-2, A-1, B-1, G-2, GS-1, BUG-1/2/4, CH-1/5/6/10) are all defined in the comparison or in ADR-003. Each was read against its row, and the content matches: for example, MAT-G-01 cites G-2 (G scale through the context), and MAT-SUBMIT-02 cites GS-1 (same grouping).

## Deviations from Plan

1. **[Rule 1 - Bug, in the new script] Indented SQL fence.** The first run failed with "ADR-003: no sql block". The owner SQL fence in ADR-003 is indented inside a list item, so it does not start at column 0. The fault was in the script, not in the ADR. The awk now accepts leading spaces. Commit 1f57141.
2. **[Rule 2 - Completeness] §9 cross-reference check added.** The coordinator asked to confirm that wave-1 IDs match. The script therefore also checks that every row, BUG/DRIFT and CH ID cited in the phase docs and the ROADMAP is defined, and that every MAT ID cited by the comparison or the ADR exists in the matrix.
3. **[Rule 3 - Blocking] Verify commands run against the worktree.** The plan's commands `cd /home/user/cortege`, which the isolation guard blocks. They were run from the worktree root instead, via scratchpad scripts, with the same checks.
4. **Tree comment alignment.** The plan's suggested spacing put the new `#` comments at a different column. The spacing was adjusted so they align with the ADR-002 and spike lines (column 49).

**Fixes to wave-1 files:** none needed. Every check passed on the wave-1 deliverables as merged.

## Owner items (not verification gaps)

1. Review ADR-003's autonomous decision (migrate to v3.2 in 01.8; submitted surveys keep their v3.0 score and a tag; drafts are re-scored; a missing version means v3.0). Overruling it also means revising the new ROADMAP lines.
2. Run the read-only production SQL in ADR-003 on the VPS to fill in the D-07 impact numbers. Its G/H = 1 count is needed before CH-5 ships.
3. Decide the "/10" score display (BUG-3, CH-11).
4. Optional: ask CNPF about Ficus, Pistacia and the linear C/D table.

## Known Stubs

None.

## Threat Flags

None. The changes are documentation and planning only; T-01.1-09, T-01.1-10 and T-01.1-11 are mitigated as the gate shows.

## Self-Check: PASSED

- FOUND: .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh
- FOUND: .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md (status: validated)
- FOUND: commits 6342557, 1f57141
