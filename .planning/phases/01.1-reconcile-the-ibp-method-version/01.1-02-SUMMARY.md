---
phase: 01.1-reconcile-the-ibp-method-version
plan: 02
subsystem: docs
tags: [ibp, adr, method-version, cnpf, documentation]
requires: []
provides:
  - "ADR-003: decision to migrate to IBP FR v3.2 in phase 01.8"
  - "D-07 treatment of recorded surveys (submitted keep v3.0 score + tag; drafts re-scored)"
  - "Change list CH-1..CH-12 for phase 01.8 (CH-12 phase 2)"
  - "Owner read-only production impact SQL"
affects:
  - "phase 01.8 (shared IBP domain package consumes CH-1..CH-11)"
  - "phase 2 (genus list from v3.2, CH-12)"
tech-stack:
  added: []
  patterns: ["ADR house style of ADR-002"]
key-files:
  created:
    - docs/technical/adr-003-ibp-method-version-v1.md
  modified:
    - docs/technical/adr-002-on-device-species-recognition-v1.md
    - docs/technical/species-recognition-spike-measurements-v1.md
decisions:
  - "Migrate the app to IBP FR v3.2 in this milestone, implemented in phase 01.8; until then the app computes Fr v3.0 scores"
  - "Submitted surveys keep their v3.0 score and are tagged cnpf_ibp_fr_v3_0_2023-03-23; never recomputed"
  - "Drafts are re-scored under v3.2 with a mechanical migration; ambiguous subalpin / flagged supra_mediterraneen leave the cas blank and block submit"
  - "01.8 must dispatch rules by method version; a missing method version means v3.0 (prepareUpsert re-validates replays)"
metrics:
  duration: "~20 min"
  completed: 2026-09-26
  tasks: 2
  files: 3
requirements: [REQ-QA-ibp-version]
---

# Phase 01.1 Plan 02: ADR-003 IBP method version Summary

ADR-003 records the decision to migrate the app from IBP Fr v3.0 to FR v3.2 in phase 01.8. It sets
out how surveys already recorded are treated (submitted surveys keep their v3.0 score and a
method-version tag; drafts are re-scored). It also requires 01.8 to dispatch the rules by method
version, with a missing version meaning v3.0, and gives 01.8 the change list CH-1..CH-12, each with
a test. ADR-002 and the spike measurements document now point to ADR-003, and the spike document's
v3.2 page citations now match the printed page numbers.

## Tasks

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Write ADR-003 | 0d9437c | docs/technical/adr-003-ibp-method-version-v1.md (248 lines) |
| 2 | Point ADR-002 and the spike doc at ADR-003; fix v3.2 page citations | 5fb2cf8 | docs/technical/adr-002-on-device-species-recognition-v1.md, docs/technical/species-recognition-spike-measurements-v1.md |

## What was built

- **ADR-003** (`docs/technical/adr-003-ibp-method-version-v1.md`):
  - **Status:** decided autonomously per D-06, with owner review pending (T-01.1-04).
  - **Context:** the v3.0 links return 404; the v3.2 URL and SHA-256 are recorded. From the comparison: 4 engine-level changes, 20 field-definition changes plus 2 that apply only to linear stands, the genus-list changes, and BUG-1.
  - **Decision:** the literal `**Decision:**` line, followed by "Why migrate" (the three D-06 reasons, and why the exception does not apply).
  - **D-07 section:** answers whether scores would change if recomputed, and sets the treatment for submitted surveys, drafts, new surveys and the pages that display scores.
  - **Required method-version dispatch:** cites `prepareUpsert` (l. 209–251) running before the read-only check for submitted surveys (l. 387–399), and states "A missing method version means v3.0." (T-01.1-05).
  - **Other sections:** the CH-1..CH-12 table with tests (CH-11 needs an owner/design call; CH-12 belongs to phase 2), the cas mapping table, four open questions, alternatives (a)/(b)/(c) and consequences.
  - **Owner SQL:** copied verbatim from RESEARCH (checked with `diff`: identical). It only runs SELECT statements; the write/DDL keyword grep finds 0 matches (T-01.1-03).
- **ADR-002:** three pointers to ADR-003: the end of the "Phase 1.1 bears on Phase 2" paragraph, the out-of-scope bullet, and the Phase 2 hand-off. Its Status, Decision and evidence are unchanged.
- **Spike doc:**
  - §2 page numbers corrected: genus list p. 2 → p. 3 (three places), Tableau 1 p. 9–10 → p. 10–11 (two places), and the table cells for archaeophytes and poplars p. 9 → p. 10 (five cells; one padding space removed from each, so every table row stays 170 characters).
  - "p. 6" and "p. 11" were not changed.
  - The §2 "Version drift" paragraph and §9 gap 9 each gained a closing sentence pointing to ADR-003.
  - The two "p.2" citations in §9 gap 8 are now "p. 3".
  - No paragraph was reflowed: the diff is 17 lines added and 12 removed.

## Verification

- Task 1 `<automated>`: OK. Acceptance: the SQL block has 0 write/DDL keywords; the file has 248 lines (minimum 120).
- Task 2 `<automated>`: OK (the spike diff against a674354 removes 12 lines, within the limit of 24; ADR-002 cites ADR-003 3 times).
- The plan's commands contain `cd /home/user/cortege`. They were run with the worktree path instead, from a script file in the scratchpad, because the sandbox refuses long compound commands.
- Link check skipped: ADR-003 links to `ibp-version-comparison-v3.0-v3.2.md`, which plan 01.1-01 creates in parallel. The link text exists and the grep passes, but whether the target resolves could not be checked in this worktree.
- `git status --porcelain`: clean after the task commits. Only the three Markdown files (plus this SUMMARY) changed; there are no code changes.

## Deviations from Plan

None in content. The ADR-002 hand-off pointer (around l. 316) was added inline, inside the existing parenthesis, and the line was split in two rather than the paragraph being reflowed.

## Known Stubs

None. The production impact numbers are deliberately pending (the owner runs the SQL), as the plan intends.

## Self-Check: PASSED

- FOUND: docs/technical/adr-003-ibp-method-version-v1.md
- FOUND: commits 0d9437c, 5fb2cf8
