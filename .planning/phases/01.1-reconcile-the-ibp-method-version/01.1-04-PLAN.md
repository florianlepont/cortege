---
phase: 01.1-reconcile-the-ibp-method-version
plan: 04
type: execute
wave: 2
depends_on: [01.1-01, 01.1-02, 01.1-03]
files_modified:
  - CLAUDE.md
  - docs/README.md
  - .planning/ROADMAP.md
  - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh
  - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md
  # Only if a gate check fails on a wave-1 deliverable (Task 2 may fix it):
  - docs/technical/ibp-version-comparison-v3.0-v3.2.md
  - docs/technical/adr-003-ibp-method-version-v1.md
  - docs/technical/adr-002-on-device-species-recognition-v1.md
  - docs/technical/species-recognition-spike-measurements-v1.md
  - docs/references/README.md
  - docs/specs/ibp-form-spec.md
  - docs/technical/ibp-validation-matrix-v1.md
autonomous: true
requirements: [REQ-QA-ibp-version]

must_haves:
  truths:
    - "ADR-003 and the comparison document are listed in CLAUDE.md's documentation tree and in docs/README.md (D-05), and CLAUDE.md's IBP domain section says which method version the app implements and which it targets"
    - "The migration is enforced in the ROADMAP: phase 01.8 has an 'Input from Phase 01.1' line (CH-1..CH-11, CH-6 dispatch) and a new success criterion 6 requiring the IBP rules to implement FR v3.2 per ADR-003 with v3.0 kept for surveys tagged v3.0 or untagged, after which the citation files say 'implemented v3.2'; phase 2 has an 'Input from Phase 01.1' line for CH-12 (genus list). 01.8's criteria 1-5 are unchanged"
    - "Every D-08 check passes mechanically: each of the ten factors has its own section and the genus list has one; every difference row has API, mobile and matrix impact; ADR-003 has the Decision line, the D-07 statement and the missing-version rule; both citation files name the implemented version; relative links in the touched docs resolve"
    - "No code, package or PDF file changed in the phase (committed or in the working tree), and lint, typecheck, unit tests and format:check pass (D-08)"
    - "01.1-VALIDATION.md (draft created at planning time) is updated to status: validated with each criterion's command and result, the per-task statuses and the owner items"
  artifacts:
    - path: .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh
      provides: "Re-runnable D-08 document checks for the phase"
      contains: "ALL DOC CHECKS PASSED"
    - path: .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md
      provides: "Phase gate record"
      contains: "status: validated"
  key_links:
    - from: "CLAUDE.md documentation index"
      to: "docs/technical/adr-003-ibp-method-version-v1.md"
      via: "tree entry under technical/"
      pattern: "adr-003-ibp-method-version-v1\\.md"
    - from: ".planning/ROADMAP.md Phase 01.8"
      to: "docs/technical/adr-003-ibp-method-version-v1.md"
      via: "'Input from Phase 01.1' line and success criterion 6"
      pattern: "Input from Phase 01\\.1"
---

<objective>
Index the new documents, write the migration into the ROADMAP entries of phases 01.8 and 2, then run the phase gate (D-08) and complete 01.1-VALIDATION.md.

Purpose: D-05 requires ADR-003 to be indexed in CLAUDE.md and docs/README.md. ADR-003 decides to migrate "in this milestone (phase 01.8)"; that decision only holds if 01.8 is required to deliver it, so 01.8 gets an input line and a success criterion, and phase 2 gets the genus-list input (the owner authorised this autonomous phase; the coordinator confirmed these ROADMAP edits are in scope). The gate proves every ROADMAP 01.1 criterion and that no code regressed.
Output: two index edits, three ROADMAP additions, a re-runnable check script and the completed validation record. STATE.md is not edited.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-CONTEXT.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-RESEARCH.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-01-SUMMARY.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-02-SUMMARY.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-03-SUMMARY.md

<interfaces>
CLAUDE.md facts: "## Documentation index" has a fenced tree; under `├── technical/` the last two entries are `│   ├── adr-002-on-device-species-recognition-v1.md      # Species recognition go/no-go (Phase 1)` and `│   └── species-recognition-spike-measurements-v1.md     # Spike evidence log for ADR-002`. The "### IBP domain" section ends with "Factor validation matrix: `docs/technical/ibp-validation-matrix-v1.md`".
docs/README.md: bullet "`docs/technical/`" with the line "Technical contracts, ADRs, validation rules, architecture and sync behavior."
ROADMAP facts:
- Phase 01.8 entry ("### Phase 01.8: Shared IBP domain package and test completeness (INSERTED)"): **Goal**, **Depends on**, **Requirements**, **Source**, **Success Criteria** with items `  1.` to `  5.` (item 5 is the public-surveys index / advisory lock / `auth_sessions` criterion), then **Plans**: TBD.
- Phase 2 entry ("### Phase 2: Factor A Genus List & Data-Contract Corrections"): **Goal**, **Depends on** (already names Phase 1.1 and 1.8), **Requirements**, then **Success Criteria**. No **Source** line.

Phase base commit: `a674354` (docs(01.1): research). Code/package paths: `api`, `mobile`, `packages`, `.github`, `infra`, `package.json`, `package-lock.json`.

01.1-VALIDATION.md already exists (draft, created at planning time) with sections Test Infrastructure, Sampling Rate, Criterion → Command Map (Result column "pending"), Per-Task Verification Map (Status "pending"), Wave 0 Requirements, Manual-Only Verifications, Gate results (placeholder), Owner items (placeholder). Update it; do not recreate it.

Check script (`01.1-doc-checks.sh`, bash, `set -u`, run from the repo root, accumulates failures and prints `FAIL: <reason>` lines, exits 1 if any failed, otherwise prints `ALL DOC CHECKS PASSED` as its last line). It implements RESEARCH "Deliverable checks" plus the stronger per-plan checks:
1. Comparison: a `^## Factor X — ` heading for each of A..J (separate C and D sections, D-04); a genus heading; a global heading; the 33 genera; the v3.2 SHA-256; at least 56 rows matching `^\| *(CLS|A|B|CD|E|F|G|H|I|J|GS)-[0-9]+ *\|`; no `\|` anywhere in the file; the awk seven-non-empty-cells check on those rows plus BUG-/DRIFT- rows; the awk "p. N in the v3.2 cell" check; all 17 original MAT IDs; no «…» quote of 120+ characters.
2. ADR-003: a line matching `^\*\*Decision:\*\* Migrate the app to IBP FR v3\.2 in this milestone \(phase 01\.8\)`; "method version", "submitted", "drafts", "prepareUpsert"; the missing-version rule `grep -qiE "missing (method[- ])?version (means|=) ?v3\.0"`; CH-1..CH-12; the sql block has no INSERT/UPDATE/DELETE/ALTER/DROP.
3. Criterion 5, per file (`docs/references/README.md`, `docs/specs/ibp-form-spec.md`): a line with v3.0 and "implement"; "ADR-003"; the v3.2 URL; no "implements/conformant … v3.2" sentence.
4. Matrix: the 17 rows of `git show a674354:docs/technical/ibp-validation-matrix-v1.md` still present verbatim (use `mktemp` for the temporary file); the "v3.2 target cases (not implemented yet)" heading.
5. Indexes and ROADMAP: `adr-003-ibp-method-version-v1.md` and `ibp-version-comparison-v3.0-v3.2.md` in CLAUDE.md and docs/README.md; in ROADMAP, the 01.8 section has the "Input from Phase 01.1" line and criterion 6, the Phase 2 section has its "Input from Phase 01.1" line.
6. Spike doc: no "(p. 2", "(p. 9", "p. 9–10" left in §2; no "v3.2 p.2" / "on p.2" left in §9; ADR-003 linked in §2, §9 gap 9 and three times in ADR-002.
7. Relative Markdown links in the touched docs resolve: for each deliverable file plus ADR-002 and the spike doc, extract `](target)` links that do not start with `http`, `mailto:` or `#`, strip any `#anchor`, and test that the path exists relative to the file's directory.
8. Hygiene: both `git diff --name-only a674354 -- api mobile packages .github infra package.json package-lock.json` (committed plus working-tree changes) and `git status --porcelain -- api mobile packages .github infra package.json package-lock.json` (untracked included) print nothing; `git ls-files '*.pdf'` prints nothing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Index ADR-003 and the comparison; write the migration into the ROADMAP (01.8 input + criterion, phase 2 input)</name>
  <files>CLAUDE.md, docs/README.md, .planning/ROADMAP.md</files>
  <read_first>
    - CLAUDE.md lines 225-240 and 360-380
    - docs/README.md
    - .planning/ROADMAP.md (Phase 01.8 and Phase 2 entries only, found with grep "### Phase 01.8:" and "### Phase 2:")
    - docs/technical/adr-003-ibp-method-version-v1.md (the Decision line and the change-list section, to word the pointers)
  </read_first>
  <action>Per D-05: in CLAUDE.md's documentation tree insert, between the adr-002 line and the spike line, `│   ├── adr-003-ibp-method-version-v1.md               # IBP method version: v3.0 implemented, v3.2 adopted (Phase 1.1)` and `│   ├── ibp-version-comparison-v3.0-v3.2.md            # IBP Fr v3.0 vs FR v3.2, factor by factor`, aligning the `#` comments with the neighbouring lines as closely as the tree allows. In CLAUDE.md's "### IBP domain" section, after the "Factor validation matrix" line, add one line: "Method version: the app implements IBP Fr v3.0; ADR-003 (`docs/technical/adr-003-ibp-method-version-v1.md`) adopts IBP FR v3.2, implemented in phase 01.8 (comparison: `docs/technical/ibp-version-comparison-v3.0-v3.2.md`)." Change nothing else in CLAUDE.md. In docs/README.md, under the `docs/technical/` bullet, add a sentence naming ADR-003 (IBP method version decision) and the v3.0/v3.2 comparison with relative links (`technical/adr-003-ibp-method-version-v1.md`, `technical/ibp-version-comparison-v3.0-v3.2.md`). In .planning/ROADMAP.md: (a) Phase 01.8 entry, after the **Source** line, add a line beginning `**Input from Phase 01.1**:` saying ADR-003 adopts IBP FR v3.2 and 01.8 implements change list CH-1..CH-11 from `docs/technical/adr-003-ibp-method-version-v1.md` (CH-12 is phase 2), including the method-version dispatch (CH-6: a missing method version means v3.0, so identical replays of submitted v3.0 surveys stay valid) and `ibp-validation-matrix-v2.md` (CH-10); (b) in the same entry, after criterion 5, add criterion `  6.` in the same indentation: "The IBP rules in `packages/ibp-domain` implement IBP FR v3.2 per ADR-003 (CH-1..CH-11), with the v3.0 rules kept for surveys tagged v3.0 or carrying no method version; `docs/references/README.md` and `docs/specs/ibp-form-spec.md` then say the app implements v3.2." (c) Phase 2 entry, after its **Requirements** line, add `**Input from Phase 01.1**: CH-12 in ADR-003 (docs/technical/adr-003-ibp-method-version-v1.md): the Factor A genus list comes from IBP FR v3.2 p. 3 and Table 1, keyed by cas (supplementary genera for cas 4 and 2, coastal-only Juniperus species, Ficus not counted, Pistacia per the CNPF answer).` Do not change 01.8's criteria 1-5, Goal or Requirements, Phase 2's criteria, the phase 01.1 entry (its plan list is maintained by the orchestrator), or STATE.md.</action>
  <verify>
    <automated>cd /home/user/cortege && grep -q "adr-003-ibp-method-version-v1.md" CLAUDE.md && grep -q "ibp-version-comparison-v3.0-v3.2.md" CLAUDE.md && grep -q "^Method version: the app implements IBP Fr v3.0" CLAUDE.md && grep -q "technical/adr-003-ibp-method-version-v1.md" docs/README.md && grep -q "technical/ibp-version-comparison-v3.0-v3.2.md" docs/README.md && e18="$(sed -n '/^### Phase 01.8:/,/^### Phase 01.9:/p' .planning/ROADMAP.md)" && printf '%s\n' "$e18" | grep -q '^\*\*Input from Phase 01.1\*\*:.*CH-6' && printf '%s\n' "$e18" | grep -qE '^  6\. .*IBP FR v3\.2.*ADR-003' && test "$(printf '%s\n' "$e18" | grep -E '^  [1-5]\. ')" = "$(git show a674354:.planning/ROADMAP.md | sed -n '/^### Phase 01.8:/,/^### Phase 01.9:/p' | grep -E '^  [1-5]\. ')" && sed -n '/^### Phase 2:/,/^### Phase 3/p' .planning/ROADMAP.md | grep -q '^\*\*Input from Phase 01.1\*\*:.*CH-12' && test "$(git diff --numstat a674354 -- CLAUDE.md | awk '{print $2}')" -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md gains exactly three lines and loses none (numstat deletions against a674354 = 0)
    - docs/README.md links both new documents
    - The 01.8 entry has the "Input from Phase 01.1" line and criterion 6; its criteria 1-5 are byte-identical to a674354
    - The Phase 2 entry has its "Input from Phase 01.1" line naming CH-12
  </acceptance_criteria>
  <done>The new docs are discoverable from both indexes, and the ROADMAP obliges 01.8 to deliver the v3.2 migration and phase 2 to build the genus list from v3.2.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate: document checks, full suite, update 01.1-VALIDATION.md to validated</name>
  <files>.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh, .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md (and, only when a check fails on them, the wave-1 deliverables listed in files_modified)</files>
  <read_first>
    - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md (the draft to update)
    - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-RESEARCH.md lines 676-692 and 737-764 (deliverable checks, validation architecture)
    - the three wave-1 SUMMARY files (deviations to report)
  </read_first>
  <action>Write `01.1-doc-checks.sh` implementing checks 1-8 of the interfaces block (make it executable; bash only, no new dependency) and run it from the repo root. This task is explicitly allowed to fix the deliverables of plans 01.1-01, 01.1-02 and 01.1-03 (the seven docs listed in files_modified): if a check fails because a deliverable is wrong, make the smallest edit in that file under the same rules as the plan that created it (D-03 quoting limit, no v3.2-implemented claim, matrix baseline rows untouched, no paragraph reflow in the spike doc), re-run, and list every such fix in the SUMMARY. If the check itself is wrong, fix the script and say why in the SUMMARY. Then run the full suite `npm run lint && npm run typecheck && npm run test:unit && npm run format:check` and note the API and mobile test counts. Update the existing `01.1-VALIDATION.md` (do not recreate it): set frontmatter `status: validated`; fill the Result column of the Criterion → Command Map and the Status column of the Per-Task Verification Map (✅ green, or the fix made); fill "Gate results" with the check-script output, the suite results and counts, and the hygiene evidence (empty diff and status for code/package paths since a674354, no tracked PDF); fill "Owner items (not verification gaps)": review ADR-003's autonomous decision, run the read-only production SQL in ADR-003 so its D-07 numbers can be filled in, decide the "/10" score display (BUG-3, CH-11), optionally ask CNPF about Ficus, Pistacia and the linear C/D table.</action>
  <verify>
    <automated>cd /home/user/cortege && bash .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh | tail -1 | grep -qx "ALL DOC CHECKS PASSED" && npm run lint >/dev/null 2>&1 && npm run typecheck >/dev/null 2>&1 && npm run test:unit >/dev/null 2>&1 && npm run format:check >/dev/null 2>&1 && v=.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-VALIDATION.md && grep -q "^status: validated" "$v" && ! grep -qE "\| pending \|$" "$v" && test -z "$(git diff --name-only a674354 -- api mobile packages .github infra package.json package-lock.json)" && test -z "$(git status --porcelain -- api mobile packages .github infra package.json package-lock.json)" && test -z "$(git ls-files '*.pdf')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The check script prints `ALL DOC CHECKS PASSED` and exits 0
    - lint, typecheck, test:unit and format:check all exit 0
    - 01.1-VALIDATION.md is `status: validated`, has no "pending" result or status left, and lists the owner items
    - No committed or working-tree change under api/, mobile/, packages/, .github/, infra/ or the root package files since a674354; no PDF is tracked
  </acceptance_criteria>
  <done>The phase is verified by re-runnable checks and the gate record is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → phase 01.8 and phase 2 planners | The ROADMAP lines and criterion steer what those phases build |
| Repository → CI / VPS deployment | A change under api/** on main would rebuild and redeploy the API image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-09 | Tampering | ROADMAP 01.8 and Phase 2 entries | mitigate | Only additions (two input lines, one criterion); the verify step compares 01.8's criteria 1-5 with a674354 byte-for-byte |
| T-01.1-10 | Denial of Service | Accidental code/package change triggering an image rebuild and VPS restart | mitigate | Gate asserts an empty `git diff` (working tree vs a674354) and an empty `git status` for api/, mobile/, packages/, .github/, infra/ and the root package files |
| T-01.1-11 | Information Disclosure | CNPF PDF committed | mitigate | Gate asserts `git ls-files '*.pdf'` is empty |
</threat_model>

<verification>
- `bash .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh` prints ALL DOC CHECKS PASSED
- Full suite green; 01.1-VALIDATION.md updated to validated
</verification>

<success_criteria>
All five ROADMAP 01.1 criteria and D-05/D-08 are proven by re-runnable commands; the new docs are indexed; the ROADMAP binds 01.8 to the v3.2 migration and phase 2 to the v3.2 genus list; nothing outside documentation changed.
</success_criteria>

<output>
Create `.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-04-SUMMARY.md` when done
</output>
