---
phase: 01.1
slug: reconcile-the-ibp-method-version
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-26
validated: 2026-09-26
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract. Created at planning time as a draft; plan 01.1-04 Task 2 ran the gate on 2026-09-26, filled the results and set `status: validated`.

`nyquist_compliant: true` because every task of the four plans has an `<automated>` command. The phase changes documentation only, so the commands are shell checks (grep/awk/sed/git) on the deliverables plus the existing Jest suites as a no-regression gate. No new test file is needed (Wave 0: none). The final verification is Claude-only (D-08).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Shell document checks (grep, awk, sed, git); existing Jest 29 + ts-jest suites for regressions |
| **Config file** | `api/jest.unit.config.js`, `mobile/jest.unit.config.js` (unchanged) |
| **Quick run command** | the task's `<automated>` command; IBP engine suites: `cd api && npx jest -c jest.unit.config.js test/ibp-rules.spec.ts && cd ../mobile && npx jest -c jest.unit.config.js src/app/ibp-scoring.test.ts` (18 + 4 tests green on 2026-09-26) |
| **Full suite command** | `npm run lint && npm run typecheck && npm run test:unit && npm run format:check` |
| **Phase check script** | `bash .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh` (created by plan 01.1-04 Task 2) |
| **E2E** | Not needed: no API code changes (gate asserts an empty diff under api/, mobile/, packages/, .github/, infra/ and the root package files since `a674354`) |

---

## Sampling Rate

- **After every task commit:** the task's `<automated>` command.
- **After wave 1:** plan 01.1-04 runs the full suite and the check script.
- **Phase gate:** the check script prints `ALL DOC CHECKS PASSED` and the full suite is green.

---

## Criterion → Command Map

From RESEARCH "Validation Architecture" (Phase Requirements → Test Map).

| Req / criterion | Behavior | Test Type | Automated Command | Result |
|-----------------|----------|-----------|-------------------|--------|
| REQ-QA-ibp-version / crit. 1 | Comparison covers A–J (one section per factor), the genus list, the global score; source hash present | doc check | plan 01 Task 2 `<automated>`; check script §1 | ✅ 10 `## Factor X — ` sections, genus and global sections, 33 genera, v3.2 SHA-256, 56 difference rows |
| crit. 2 | Every difference row has API, mobile and matrix impact cells and a v3.2 page | doc check | awk row and page checks (plan 01 Task 2; check script §1) | ✅ all 56 rows and 6 BUG/DRIFT rows have 7 non-empty cells; every v3.2 cell has a page |
| crit. 3 | ADR-003 exists with a Decision line ("Migrate the app to IBP FR v3.2 in this milestone (phase 01.8)") | doc check | `grep -qE '^\*\*Decision:\*\*' docs/technical/adr-003-ibp-method-version-v1.md` (plan 02 Task 1; check script §2) | ✅ Decision line present verbatim |
| crit. 4 | D-07 statement (submitted keep score + version tag; drafts re-scored; missing version means v3.0) | doc check | plan 02 Task 1 `<automated>`; check script §2 | ✅ method version, submitted, drafts, prepareUpsert, missing-version rule, CH-1..CH-12; owner SQL read-only |
| crit. 5 | README and spec state the implemented version (v3.0) and the target (v3.2, ADR-003); no v3.2-implemented claim | doc check | plan 03 Task 1 `<automated>`; check script §3 | ✅ both files: v3.0 implemented, ADR-003, v3.2 URL, no v3.2-implemented claim |
| D-05 | ADR-003 and the comparison indexed in CLAUDE.md and docs/README.md | doc check | plan 04 Task 1 `<automated>`; check script §5 | ✅ both indexes list both documents; ROADMAP 01.8 input line + criterion 6, phase 2 input line |
| D-08 | No code regressions; no code/package/PDF change | unit/lint/type/format + git | full suite command; check script §8 | ✅ lint, typecheck, test:unit (API 638, mobile 1050), format:check green; empty code/package diff; no PDF |

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.1-01-T1 | 01 | 1 | REQ-QA-ibp-version | T-01.1-01, T-01.1-02 | Sources hashed; 2021 sheet labelled; no pasted tables | doc check | plan 01 Task 1 `<automated>` | new doc | ✅ green |
| 01.1-01-T2 | 01 | 1 | REQ-QA-ibp-version | T-01.1-01 | Quotes under 120 characters; no PDF | doc check | plan 01 Task 2 `<automated>` | new doc | ✅ green |
| 01.1-02-T1 | 02 | 1 | REQ-QA-ibp-version | T-01.1-03, T-01.1-04, T-01.1-05 | Owner SQL read-only; autonomous status stated; dispatch rule required | doc check | plan 02 Task 1 `<automated>` | new doc | ✅ green |
| 01.1-02-T2 | 02 | 1 | REQ-QA-ibp-version | — | Citation-only edits | doc check | plan 02 Task 2 `<automated>` | edit | ✅ green |
| 01.1-03-T1 | 03 | 1 | REQ-QA-ibp-version | T-01.1-06, T-01.1-08 | No v3.2-implemented claim | doc check | plan 03 Task 1 `<automated>` | edit | ✅ green |
| 01.1-03-T2 | 03 | 1 | REQ-QA-ibp-version | T-01.1-07 | 17 baseline rows byte-for-byte | doc check | plan 03 Task 2 `<automated>` | edit | ✅ green |
| 01.1-04-T1 | 04 | 2 | REQ-QA-ibp-version | T-01.1-09 | ROADMAP edits limited to the 01.8 input line and criterion and the phase 2 input line | doc check | plan 04 Task 1 `<automated>` | edit | ✅ green |
| 01.1-04-T2 | 04 | 2 | REQ-QA-ibp-version | T-01.1-10, T-01.1-11 | No code/package/PDF change | script + full suite | plan 04 Task 2 `<automated>` | W0: none (script created in-task) | ✅ green (script fix: indented sql fence, see Gate results) |

---

## Wave 0 Requirements

None. The document checks are shell commands and need no test files; the existing Jest suites cover code regressions.

---

## Manual-Only Verifications

None (D-08: Claude-only verification; no device check, documentation-only phase).

---

## Gate results

Run on 2026-09-26 from the plan 01.1-04 worktree (wave-1 plans 01, 02 and 03 merged).

**Document checks** (`bash .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-doc-checks.sh`), sections §1-§9:

```
ALL DOC CHECKS PASSED
```

- §1-§6 implement RESEARCH "Deliverable checks" and the per-plan checks. §7 resolves every relative Markdown link in the comparison, ADR-003, ADR-002, the spike doc, the matrix, the references README, the form spec and docs/README.md: the cross-links between the three wave-1 documents, which wave 1 could not check, all resolve. §8 is the hygiene check.
- §9 (added beyond the plan) checks cross-references: every row ID (CLS-, A- … GS-), BUG-/DRIFT- ID and CH- ID cited in the phase documents and the ROADMAP is defined in the comparison or in ADR-003's change list, and every MAT- case cited by the comparison or the ADR exists in the matrix. The IDs plan 03 cites (CLS-1, CLS-2, A-1, B-1, G-2, GS-1, BUG-1, BUG-2, BUG-4, CH-1, CH-5, CH-6, CH-10) exist, and each citation was also read against its row: the content matches.
- One script fix during the gate: the first run reported "ADR-003: no sql block" because the owner SQL fence in ADR-003 is indented inside a list item; the awk now accepts leading spaces. No wave-1 document needed a fix.

**Full suite** (`npm run lint && npm run typecheck && npm run test:unit && npm run format:check`, run with `node_modules` symlinked from the main checkout; the symlinks were removed afterwards):

| Command | Result |
|---------|--------|
| `npm run lint` | exit 0 |
| `npm run typecheck` | exit 0 |
| `npm run test:unit` | exit 0: API 29 suites / 638 tests passed; mobile 81 suites / 1050 tests passed |
| `npm run format:check` | exit 0 ("All matched files use Prettier code style!") |

**Hygiene** (T-01.1-10, T-01.1-11):

- `git diff --name-only a674354 -- api mobile packages .github infra package.json package-lock.json`: empty (0 files).
- `git status --porcelain -- api mobile packages .github infra package.json package-lock.json`: empty once the `node_modules` symlinks were removed (they show as untracked while present, so the check script must run without them).
- `git ls-files '*.pdf'`: empty.

---

## Owner items (not verification gaps)

These are decisions or data only the owner can supply. None blocks the phase: every criterion is verified above.

1. **Review ADR-003's autonomous decision**: migrate to IBP FR v3.2 in phase 01.8; submitted surveys keep their v3.0 score and a method-version tag; drafts are re-scored; a missing method version means v3.0. ADR-003's Status says it was decided autonomously per D-06, pending owner review. Overruling it also means revising the 01.8 input line and criterion 6 and the phase 2 input line that plan 01.1-04 added to the ROADMAP.
2. **Run the read-only production SQL** in ADR-003 "Open questions" on the VPS database, so the D-07 impact numbers can be filled in. It only runs SELECT statements. Its G/H = 1 count is also needed before CH-5 ships.
3. **Decide the score display** (BUG-3, CH-11): the 0-50 total is shown as "/10" with thresholds 7 and 5. Choose /50, and whether to use the interpretation-chart bands (GS-2). Likely home: phase 01.8 or a quick task.
4. **Optional questions to CNPF** (comparison, "Questions for CNPF and assumptions"): Ficus in Table 1 (not counted for now), Pistacia (decided in phase 2, CH-12), and the linear-stand C/D table, which does not use the (BMg + BMm) sum that p. 3 uses.
