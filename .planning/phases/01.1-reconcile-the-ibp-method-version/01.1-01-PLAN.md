---
phase: 01.1-reconcile-the-ibp-method-version
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/technical/ibp-version-comparison-v3.0-v3.2.md
autonomous: true
requirements: [REQ-QA-ibp-version]

must_haves:
  truths:
    - "A reader can see, factor by factor (A to J, each in its own section, plus the classification model, the Factor A genus list and the global score), how the app's v3.0 rules differ from IBP FR v3.2, with the v3.2 page for each difference (ROADMAP criterion 1, D-04)"
    - "Every difference row states its severity (changes a score / changes a field / wording only / none) and its impact on the API rules, the mobile rules and the validation-matrix cases (ROADMAP criterion 2, D-04, D-08)"
    - "The document states which v3.0 baseline it used (the app's own rules, per D-02) and records the v3.2 PDF's URL, size, SHA-256, retrieval date and cited pages so the comparison can be reproduced (D-01, D-02)"
    - "All 17 validation-matrix cases are recomputed under v3.2 and the proposed new v3.2 cases are listed"
    - "App bugs that diverge from both versions (BUG-1 to BUG-5, DRIFT-1) are recorded separately, including the '/10' display bug"
    - "No v3.2 page or table is pasted: facts in our own words, short quotes only, each with a page (D-03)"
  artifacts:
    - path: docs/technical/ibp-version-comparison-v3.0-v3.2.md
      provides: "Factor-by-factor v3.0 (app) vs v3.2 comparison with impact columns"
      contains: "f130b3d66522ec0f5692e87e361b628b4941ce9748d1ed5519a49734854bd001"
      min_lines: 250
  key_links:
    - from: "docs/technical/ibp-version-comparison-v3.0-v3.2.md"
      to: "docs/technical/adr-003-ibp-method-version-v1.md"
      via: "relative Markdown link in the header and in the change-list pointer"
      pattern: "adr-003-ibp-method-version-v1\\.md"
    - from: "docs/technical/ibp-version-comparison-v3.0-v3.2.md"
      to: "docs/technical/ibp-validation-matrix-v1.md"
      via: "matrix recomputation section cites every MAT-* case"
      pattern: "MAT-SUBMIT-02"
---

<objective>
Write `docs/technical/ibp-version-comparison-v3.0-v3.2.md`, the factor-by-factor comparison of what the app implements (IBP Fr v3.0 as transcribed in the repo) against the official IBP FR v3.2 (02/02/2026).

Purpose: ROADMAP criteria 1 and 2 of phase 01.1 and REQ-QA-ibp-version. The difference register already exists, page-cited, in `01.1-RESEARCH.md`. This plan turns it into the durable, self-contained reference document that ADR-003 (plan 01.1-02) decides on and that phase 01.8 implements against.
Output: one new Markdown file under `docs/technical/`. No code change.
</objective>

<execution_context>
@/home/user/cortege/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/cortege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-CONTEXT.md
@.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-RESEARCH.md
@CLAUDE.md

<interfaces>
Source material (all in 01.1-RESEARCH.md; do not re-derive it with new tooling, per its "Don't Hand-Roll"):
- "Sources used and reproducibility" table: v3.2 PDF identity (URL `https://www.cnpf.fr/sites/socle/files/2026-04/IBP_FR_v3_2_260202.pdf`, 28 pp., 1,264,005 bytes, SHA-256 `f130b3d66522ec0f5692e87e361b628b4941ce9748d1ed5519a49734854bd001`, retrieved 2026-09-26); the 2021 CNPF "IBP Fr v3" ACA sheet mirror (SHA-256 `3751b6f6a0bfa68591a84fbbeb80aefc0eb004a90d8ff3cdaaff9a770b28b8e3`, 8 pp., 724,689 bytes) and its caveat; the dead 2023-03-23 v3.0 links.
- "What the app implements today": the v3.0 baseline table with file:line references.
- "Classification model": CLS-1..CLS-6 rows and the region/stage → cas mapping table.
- "Difference register": Factor A (A-1..A-10), genus-list diff, Table 1 species per genus, Table 2 note, Factor B (B-1..B-4), C/D (CD-1..CD-6), E (E-1..E-5), F (F-1..F-7), G (G-1..G-5), H (H-1..H-4), I (I-1..I-3), J (J-1..J-3), Global (GS-1..GS-3), "Counts for the headline".
- "App bugs that diverge from BOTH versions": BUG-1..BUG-5, DRIFT-1.
- "Validation matrix under v3.2": 17 recomputed cases and the proposed new cases MAT-A-03..05, MAT-C-02, MAT-E-02, MAT-G-02, MAT-G-03, MAT-H-02, MAT-VER-01.
- "Open Questions" 2 (CNPF inconsistencies: Ficus, Pistacia, linear C/D table) and "Assumptions Log" A1..A6.

The v3.2 text extraction is at `/tmp/claude-0/-home-user-cortege/2b3789f4-1097-5c7e-920c-66fb05f3a881/scratchpad/ibp/v32.txt` (pages marked "===== PAGE N =====", printed page = physical page). Use it only to spot-check a page number or a short quote; if the scratchpad is gone, the RESEARCH register is authoritative. Never copy it or the PDF into the repo.

Required document layout (headings are checked mechanically by plan 01.1-04; keep the exact heading words):
- `# IBP method version comparison: Fr v3.0 (as implemented) vs FR v3.2`
- Header block: Status (reference document, phase 01.1), Date 2026-09-26, decision link to `adr-003-ibp-method-version-v1.md`, one-paragraph summary (not v3.2-conformant; 4 engine-level score changes; the field-definition changes counted from the rows themselves, see `## Summary counts`; unchanged aggregation, thresholds and chart).
- `## Sources and baseline` (provenance table + D-02 baseline statement + the 2021-sheet caveat + "How to reproduce": download URL, `sha256sum`, printed page numbers match physical pages).
- `## How to read the tables` (severity key and the seven column names).
- `## Classification model (regions and stages vs cas 1 to 4)` with CLS rows and the mapping table.
- `## Factor A — Native tree genera`, then `### Factor A genus list (v3.0 family vs v3.2)` with the genus diff table, the Table 1 species list (bare Latin names are factual data) and the Table 2 exclusion note.
- `## Factor B — Vertical vegetation structure`
- `## Factor C — Large standing deadwood` (holds the CD-1..CD-6 rows, which v3.2 states once for both deadwood factors)
- `## Factor D — Large downed deadwood` (its own section per D-04: one sentence saying rows CD-1..CD-6 apply identically to D, then D's own points: the downed-wood length and measuring rules in CD-5 and its matrix case MAT-D-01; no duplicate CD rows)
- `## Factor E — Very large living trees`
- `## Factor F — Living trees bearing dendromicrohabitats`
- `## Factor G — Open flowering habitats`
- `## Factor H — Temporal continuity of forest cover`
- `## Factor I — Aquatic habitats`
- `## Factor J — Rocky habitats`
- `## Global score and categories`
- `## Summary counts`
- `## App bugs that diverge from both versions`
- `## Validation matrix under v3.2`
- `## Questions for CNPF and assumptions`
- `## Change list`: a short pointer that CH-1..CH-12 live in ADR-003 (do not duplicate the list).

Difference-row format (every row, every factor section): `| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |`, all seven cells non-empty (write "none" rather than leaving a cell blank; "—" is allowed only in the v3.0 column for rows v3.0 did not address), and the v3.2 cell always contains a printed page as "p. N". Never put a pipe character inside a table cell, escaped or not (RESEARCH writes `ACA\|M` and `0\|1\|2\|5`; write "ACA or M", "0, 1, 2 or 5" instead), because the gate splits rows on `|`. Keep the row IDs exactly as in RESEARCH (CLS-1..6, A-1..10, B-1..4, CD-1..6, E-1..5, F-1..7, G-1..5, H-1..4, I-1..3, J-1..3, GS-1..3 = 56 rows). Bug table format: `| ID | Bug | Evidence | API impact | Mobile impact | Matrix impact | Recommendation |` (7 non-empty cells).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sources, classification model, Factors A to D with the genus-list diff</name>
  <files>docs/technical/ibp-version-comparison-v3.0-v3.2.md</files>
  <read_first>
    - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-RESEARCH.md lines 104-318 (summary, sources, baseline, classification, Factors A, genus diff, B, C/D)
    - docs/technical/ibp-validation-matrix-v1.md (case IDs referenced in the matrix-impact column)
    - docs/technical/adr-002-on-device-species-recognition-v1.md lines 1-50 (house style for technical docs)
  </read_first>
  <action>Create the file with the title, header block, `## Sources and baseline`, `## How to read the tables`, `## Classification model (regions and stages vs cas 1 to 4)`, `## Factor A — Native tree genera` with `### Factor A genus list (v3.0 family vs v3.2)`, `## Factor B — Vertical vegetation structure`, `## Factor C — Large standing deadwood` and `## Factor D — Large downed deadwood`, in the layout given in the interfaces block. Content comes from the RESEARCH register rows with the same IDs, rewritten as clean English prose/table cells in our own words (D-03): short French quotes only where the wording is the point (for example « Score plafonné à 2 », p. 3), each with its page. State the baseline plainly per D-02: the v3.0 column describes what the app and the repo's spec/matrix implement, cross-checked where marked against the 2021 CNPF v3 ACA sheet (label those cells "2021 v3 sheet", never "v3.0 (2023)"), because both official 2023-03-23 PDFs return HTTP 404 and have no Wayback snapshot. In the genus section, list all 28 main v3.2 genera plus the Quercus deciduous/evergreen split and the 5 supplementary genera (Ceratonia, Cercis, Olea, Phillyrea, Pistacia; cas 4 and cas 2), the changes (Cupressus and Tamarix added, Juniperus widened with the coastal-only species, supplementary genera extended to cas 2, Ficus in Table 1 but not counted, Pistacia shrub-only), and the rule that only listed genera count (p. 6). The Table 1 species list uses bare Latin names only (no French vernacular names, D-03). Mark BUG-1 on rows A-1 and B-1 (cap on B is wrong under both versions). Do not claim the app is v3.2-conformant anywhere.</action>
  <verify>
    <automated>cd /home/user/cortege && f=docs/technical/ibp-version-comparison-v3.0-v3.2.md && test -f "$f" && for x in A B C D; do grep -qE "^## Factor $x — " "$f" || { echo "missing Factor $x"; exit 1; }; done && grep -qiE "^##+ .*genus" "$f" && grep -qE "^##+ .*Classification" "$f" && grep -q f130b3d66522ec0f5692e87e361b628b4941ce9748d1ed5519a49734854bd001 "$f" && grep -q 3751b6f6a0bfa68591a84fbbeb80aefc0eb004a90d8ff3cdaaff9a770b28b8e3 "$f" && for g in Abies Acer Alnus Arbutus Betula Carpinus Castanea Celtis Cupressus Fagus Fraxinus Juglans Juniperus Larix Malus Ostrya Pinus Picea Populus Prunus Pyrus Quercus Salix Sorbus Tamarix Taxus Tilia Ulmus Ceratonia Cercis Olea Phillyrea Pistacia Ficus; do grep -q "$g" "$f" || { echo "genus $g missing"; exit 1; }; done && test "$(grep -cE '^\| *(CLS|A|B|CD)-[0-9]+ *\|' "$f")" -ge 26 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Headings for the classification model, Factor A, the genus list, Factor B, Factor C and Factor D exist (separate `## Factor C — ` and `## Factor D — ` sections, D-04)
    - No difference row contains a pipe inside a cell (no `\|` anywhere in the file)
    - Rows CLS-1..6, A-1..10, B-1..4, CD-1..6 (26 rows) exist with seven non-empty cells and a "p. N" in the v3.2 cell
    - Both SHA-256 values, the v3.2 URL, the retrieval date 2026-09-26 and the D-02 baseline statement are present
    - All 33 genera and Ficus appear in the genus section
  </acceptance_criteria>
  <done>The first half of the comparison (provenance, classification, A to D, genus list) is written and page-cited.</done>
</task>

<task type="auto">
  <name>Task 2: Factors E to J, global score, bugs, matrix recomputation, CNPF questions</name>
  <files>docs/technical/ibp-version-comparison-v3.0-v3.2.md</files>
  <read_first>
    - .planning/phases/01.1-reconcile-the-ibp-method-version/01.1-RESEARCH.md lines 320-436 (E to J, global score, counts, bugs, matrix under v3.2) and lines 703-725 (assumptions, open questions)
    - docs/technical/ibp-validation-matrix-v1.md (the 17 cases, verbatim IDs)
  </read_first>
  <action>Append the sections `## Factor E — Very large living trees`, `## Factor F — Living trees bearing dendromicrohabitats`, `## Factor G — Open flowering habitats`, `## Factor H — Temporal continuity of forest cover`, `## Factor I — Aquatic habitats`, `## Factor J — Rocky habitats`, `## Global score and categories`, `## Summary counts`, `## App bugs that diverge from both versions`, `## Validation matrix under v3.2`, `## Questions for CNPF and assumptions` and `## Change list`, following the interfaces block and the RESEARCH rows of the same IDs (E-1..5, F-1..7, G-1..5, H-1..4, I-1..3, J-1..3, GS-1..3). For Factor I and J describe the new types in our own words with at most the short type names quoted (« Mer ou océan », p. 4). Summary counts: 4 engine-level score changes (A-1/B-1, CD-1, E-1, CLS-2/CLS-4/G-2); then the field-definition, minor, wording-only and no-change groups with their row IDs as in RESEARCH "Counts for the headline", but state the numbers by counting the IDs you list, not by copying RESEARCH's "21": the field-definition group is 20 main rows (CLS-1, CLS-3, CLS-6, A-2, A-3, A-4, A-5, B-2, B-3, CD-2, CD-3, E-2, E-3, F-1, F-2, G-1, H-1, H-2, I-1, J-1) plus 2 rows that matter for linear stands only (F-5, G-3). Bug table: BUG-1 (cap on B), BUG-2 (G/H accept 1; tightening it affects replays of submitted surveys because `prepareUpsert` re-validates), BUG-3 (the 0–50 total shown as "/10" with colour thresholds 7 and 5 in `IbpScoreBadge`, `SectorScoreCard`, `brand-tokens.ts`; presentation, owner/design decision), BUG-4 (`ibp_method_version` specified but never implemented), BUG-5 (`montagnard_mediterraneen`), DRIFT-1 (`factor_f_group_capped` API-only, already 01.8), each with 7 non-empty cells. Matrix section: a table of all 17 MAT-* cases (Case, v3.0 context/input, v3.0 expected, v3.2 context, v3.2 expected, Changes?) stating that only MAT-B-01 changes value (B 2 → 5), MAT-A-02 depends on the subalpine level, and the context rewrites; then the proposed new cases with IDs and expected values, noting they are specified in the matrix doc as "v3.2 target" cases (plan 01.1-03) and implemented in 01.8. Questions section: Ficus, Pistacia, the linear C/D table still using BMm alone (CD-6), and assumptions A1–A6 in one line each. Change list section: one paragraph pointing to ADR-003's CH-1..CH-12.</action>
  <verify>
    <automated>cd /home/user/cortege && f=docs/technical/ibp-version-comparison-v3.0-v3.2.md && for x in A B C D E F G H I J; do grep -qE "^## Factor $x — " "$f" || { echo "missing Factor $x"; exit 1; }; done && ! grep -qF '\|' "$f" && grep -qiE "^##+ .*global" "$f" && grep -qiE "^##+ .*bugs" "$f" && test "$(grep -cE '^\| *(CLS|A|B|CD|E|F|G|H|I|J|GS)-[0-9]+ *\|' "$f")" -ge 56 && test -z "$(awk -F'|' '/^\| *(CLS|A|B|CD|E|F|G|H|I|J|GS|BUG|DRIFT)-[0-9]+ *\|/ { n=0; for(i=2;i<NF;i++){ c=$i; gsub(/^ +| +$/,"",c); if(c!="") n++ } if(n<7) print "incomplete row: " $2 }' "$f")" && test -z "$(awk -F'|' '/^\| *(CLS|A|B|CD|E|F|G|H|I|J|GS)-[0-9]+ *\|/ { if ($4 !~ /p\. ?[0-9]/) print "no page: " $2 }' "$f")" && for m in MAT-A-01 MAT-A-02 MAT-B-01 MAT-C-01 MAT-D-01 MAT-E-01 MAT-F-01 MAT-F-02 MAT-G-01 MAT-H-01 MAT-I-01 MAT-I-02 MAT-J-01 MAT-CONS-01 MAT-CONS-02 MAT-SUBMIT-01 MAT-SUBMIT-02 MAT-A-03 MAT-C-02 MAT-E-02 MAT-VER-01; do grep -q "$m" "$f" || { echo "missing $m"; exit 1; }; done && for b in BUG-1 BUG-2 BUG-3 BUG-4 BUG-5 DRIFT-1; do grep -qE "^\| *$b *\|" "$f" || { echo "missing $b"; exit 1; }; done && grep -q "/10" "$f" && grep -q "adr-003-ibp-method-version-v1.md" "$f" && test -z "$(grep -oE '«[^»]{120,}»' "$f")" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Every factor A to J has its own `## Factor X — ` section; global-score and bug sections exist
    - The summary counts say 20 field-definition rows plus 2 linear-only rows (F-5, G-3)
    - At least 56 difference rows, all with seven non-empty cells and a "p. N" in the v3.2 column; six bug/drift rows with seven cells
    - All 17 MAT-* cases and the new proposed cases appear in the matrix section
    - No quotation longer than 120 characters (D-03); no PDF added (`git status --porcelain | grep -c '\.pdf'` is 0)
  </acceptance_criteria>
  <done>The comparison document is complete: every factor, every difference with its three impacts, the bugs and the recomputed matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CNPF copyrighted PDF → repository | Third-party method text must not be redistributed (docs/references/README.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Information Disclosure | Redistribution of the v3.2 PDF or its tables | mitigate | No PDF or extraction file is added (the verify step checks `git status` for .pdf); facts paraphrased, quotes capped at 120 characters and page-cited (D-03) |
| T-01.1-02 | Tampering | Misattributed source (2021 sheet presented as the 2023 v3.0) | mitigate | Every such cell is labelled "2021 v3 sheet"; both source hashes recorded so the reading can be reproduced |
</threat_model>

<verification>
- The Task 2 verify command passes (all factor headings, 56+ complete page-cited rows, bugs, 17 cases, no long quotes)
- `git status --porcelain` shows only the new Markdown file
</verification>

<success_criteria>
ROADMAP 01.1 criteria 1 and 2 are met by one document: every factor and the genus list compared against v3.2 with page references, and every difference carries its API, mobile and matrix impact.
</success_criteria>

<output>
Create `.planning/phases/01.1-reconcile-the-ibp-method-version/01.1-01-SUMMARY.md` when done
</output>
