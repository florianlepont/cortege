# Phase 01.1: Reconcile the IBP method version - Research

**Researched:** 2026-09-26
**Domain:** CNPF IBP methodology (Fr v3.0 vs FR v3.2), IBP scoring rules in the API and the mobile app, survey data model
**Confidence:** HIGH for the v3.2 side and for what the app implements; MEDIUM for the official v3.0 text (the 2023-03-23 PDFs could not be retrieved; an earlier official v3 sheet dated 2021 was used as a cross-check)

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions

**Phase boundary (from `<domain>`):**

This phase answers one question: does the app still implement the current CNPF IBP method, and what exactly changes if it does not?

**Deliverables** (all documentation; see ROADMAP criteria 1–5):

1. A written comparison, factor by factor, of IBP Fr v3.0 (the version the repo cites) against IBP FR v3.2 (the version CNPF currently publishes, dated 2026-02-02). It covers all ten factors: field definitions, class thresholds, and the Factor A native-genus list.
2. For each difference: its concrete impact on these three places:
   - `api/src/surveys/ibp-rules.service.ts`
   - `mobile/src/app/ibp-scoring.ts` (and any other mobile IBP rule files)
   - the 17 reference cases in `docs/technical/ibp-validation-matrix-v1.md`
3. An explicit decision in `docs/technical/`: migrate to v3.2 in this milestone, or stay on v3.0, with the reasoning.
4. If the decision is to migrate: what happens to surveys already recorded under v3.0 (do their scores change, and what happens to them).
5. `docs/references/README.md` and `docs/specs/ibp-form-spec.md` cite the version the app actually implements after this phase.

**Out of scope:** rewriting the scoring code.
- If migration is chosen, the code change belongs to phase 01.8, which extracts the IBP rules into one shared package. This phase must hand 01.8 an exact, testable change list.
- Exception: a change is allowed here if it is purely a citation or comment, or a bug found in the current v3.0 implementation. A real v3.0 bug gets a short fix only if it is small and clearly isolated. Otherwise it is recorded for 01.8.

**Implementation Decisions (Claude, autonomous):**

### Sources
- **D-01, v3.2 source.** The official PDF `https://www.cnpf.fr/sites/socle/files/2026-04/IBP_FR_v3_2_260202.pdf` (28 pages, 1,264,005 bytes).
  - It was downloaded on 2026-09-26 to the session scratchpad: `/tmp/claude-0/-home-user-cortege/2b3789f4-1097-5c7e-920c-66fb05f3a881/scratchpad/ibp/IBP_FR_v3_2_260202.pdf`.
  - Read it with the Read tool and its `pages` parameter. Python PDF libraries are broken in this container.
  - Do not commit the PDF: `docs/references/README.md` forbids redistribution, and `*.pdf` is gitignored there.
  - Record the SHA-256 of the file, the date it was retrieved and the page numbers cited, so the comparison can be reproduced.
- **D-02, v3.0 source.** The two v3.0 PDFs linked from `docs/references/README.md` now return HTTP 404 on cnpf.fr. The CNPF IBP page no longer links any v3.0 document. The Wayback Machine was unreachable from this container on 2026-09-26.
  - The v3.0 baseline is therefore what the repository actually implements and documents, which was transcribed from v3.0:
    - `docs/specs/ibp-form-spec.md`
    - `docs/technical/ibp-validation-matrix-v1.md`
    - `api/src/surveys/ibp-rules.service.ts`
    - the mobile IBP rules (`mobile/src/app/`)
  - The researcher may try to find an official v3.0 copy once more (archive.org, other CNPF paths). If one is found, check the repo transcription against it too.
  - The comparison states which baseline it used. The question that matters is "does the app match v3.2?", and the app's own rules answer the v3.0 side of that.
- **D-03, copyright.** Transcribe facts only: thresholds, class limits, genus names, field definitions in our own words. Short quotes are allowed where a wording difference matters, with page references. Never paste whole pages or tables verbatim.

### Deliverable locations
- **D-04, comparison document.** Goes in `docs/technical/ibp-version-comparison-v3.0-v3.2.md`, with one section per factor A–J plus the global score and categories.
  - Each difference is a row: what v3.0/the app does, what v3.2 says (with page), severity (changes a score / changes a field / wording only), and the impact on the API rules, mobile rules and validation-matrix cases.
- **D-05, decision.** Recorded as an ADR: `docs/technical/adr-003-ibp-method-version-v1.md` (ADR-002 exists from phase 1). It is indexed in CLAUDE.md's documentation tree and in `docs/README.md`.

### Decision rule (D-06)
- **D-06, how Claude decides.**
  - **If v3.2 changes nothing that affects scoring** (only wording or presentation), the app is declared "v3.2-conformant". The citations move to v3.2 and no migration is needed.
  - **If v3.2 changes scoring rules or the Factor A genus list,** the default decision is to **migrate to v3.2 in this milestone**, for three reasons:
    - v3.2 is the method CNPF publishes today, and new observers are trained on it.
    - The app's value to ecologists depends on computing the current official index.
    - Phase 2 builds the Factor A genus list from the v3.2 PDF (ADR-002), so staying on v3.0 would put two versions in the app.
  - **Where migration happens:** the migration is executed in phase 01.8. 01.8 already extracts the IBP rules into a shared package, so the rules change once, in one place, against the change list from this phase.
  - **When staying on v3.0 is chosen instead:** only if migrating would break comparability with already-recorded surveys in a way that cannot be handled (see D-07). The ADR must then explain why.
- **D-07, surveys already recorded.** If migrating, the ADR states the effect on them plainly:
  - whether their stored scores would change if recomputed;
  - the proposed treatment. The default: a submitted survey keeps the score it was submitted with and records the method version it used. Drafts are recomputed under v3.2.
  - This implies storing a method-version field. Only the need is recorded here; the schema change belongs to 01.8 or phase 2.
  - The researcher checks what the data model already stores (the `region_version` field on surveys, IBP result fields) before proposing anything.

### Verification
- **D-08, checking the phase.** The final verification is Claude-only; no device check is needed, since this phase changes documentation. It checks:
  - every one of the ten factors, plus the Factor A genus list, has a section in the comparison;
  - every difference has an impact line for the API, the mobile app and the matrix;
  - the ADR exists with a clear "Decision:" line, and the D-07 statement if the decision is to migrate;
  - both citation files name the implemented version;
  - lint, typecheck, unit tests and format still pass (no code regressions).

### Claude's Discretion
CONTEXT.md has no separate "Claude's Discretion" section: every decision above was taken by Claude autonomously and the owner reviews them at the end of the phase (CONTEXT.md "Source" line).

### Deferred Ideas (OUT OF SCOPE)
- Implementing v3.2 scoring rules in code: phase 01.8
- The Factor A genus-list data model: phase 2
- A method-version column on surveys: phase 01.8 or phase 2, decided by 01.8's planner
</user_constraints>

<phase_requirements>
## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| REQ-QA-ibp-version | The IBP method version the app implements is established and ratified. The repo cites IBP Fr v3.0 (PDFs dated 2023-03-23); the CNPF's current publication is FR v3.2 (dated 2026-02-02). All ten factors are compared, every divergence is recorded against `ibp-rules.service.ts`, `ibp-scoring.ts` and the 17-case validation matrix, and a documented decision either migrates to v3.2 or stays on v3.0 for stated reasons. | "Difference register" (every factor A–J, classification, global score, with API/mobile/matrix impact); "Factor A genus-list diff"; "Validation matrix under v3.2" (all 17 cases recomputed); "Data model facts (D-07)"; "Decision (D-06) and draft ADR reasoning"; "Change list for 01.8" |
</phase_requirements>

## Project Constraints (from CLAUDE.md)

- Docs live under `docs/`; ADRs in `docs/technical/`; the documentation tree in CLAUDE.md ("Documentation index") must list new docs (D-05 also requires `docs/README.md`).
- `docs/references/README.md`: IBP PDFs are third-party works and are **not redistributed**; `*.pdf` is gitignored there. Do not commit the v3.2 PDF.
- Before committing: `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run format:check` must pass. `format:check` covers only `**/*.{ts,tsx,json}` (root `package.json` line 23), so Markdown is not Prettier-checked — but any `.ts` touched (e.g. a comment/citation fix) is.
- Code conventions if any code is touched: double quotes, no semicolons, trailing commas, 2-space indent, 100-char lines; unused vars are errors.
- All user-facing text lives in the typed French catalogue `mobile/src/i18n/fr/` (relevant to 01.8: the factor help/hints are in `mobile/src/i18n/fr/labels.ts`).
- `IbpRulesService` enforces the rules server-side and "the same rules exist on mobile" — both must change together (01.8 unifies them in `packages/ibp-domain`).
- Branch workflow: no push; commit only when asked (this research commit was asked for).

## Summary

**The app is not v3.2-conformant.** Comparing the rules the app actually runs (API `ibp-rules.service.ts`, mobile `ibp-scoring.ts`, form spec, validation matrix) against the official IBP FR v3.2 PDF (02/02/2026, 28 pp., SHA-256 `f130b3d6…d001`), v3.2 changes the **scoring engine itself in four places** and changes **what the observer counts** in about twenty more:

1. **The native-cover cap belongs to Factor A, not Factor B** (v3.2 p. 3 and p. 20). The app caps **B** at 2 when native cover < 50 %. An official CNPF v3 survey sheet from 2021 also puts the cap on **A**, so this is almost certainly a v3.0 transcription bug that v3.2 does not fix — it is wrong under both versions.
2. **C and D, score 1**: v3.2 tests `(BMm + BMg)/ha ≥ 1`; the app tests `BMm/ha ≥ 1` alone.
3. **E, score 1**: v3.2 tests `(GB + TGB)/ha ≥ 1`; the app tests `GB/ha ≥ 1` alone.
4. **The regional model changed**: v3.0's "region (ACA / M) + vegetation stage" became v3.2's "cas 1 / 2 / 3 / 4". The cas-3 ("subalpine") scales for A and G now apply only to the **middle and upper subalpine** — the lower subalpine is cas 1. They also apply to cas-2 stands in cas-3 zones and to lapiaz, dune, peat-bog and *Juniperus thurifera* habitats. The app applies the subalpine scales to any `ACA` + `subalpin` survey and has no way to record cas 2 or those habitats.

The field-definition changes include:
- **Strata heights (B):** cas 1 is 7–18 m / > 18 m where v3.0 ACA used 7–20 m / > 20 m. Cas 2/3/4 is 5–12 m / > 12 m where v3.0 M used 5–15 m / > 15 m.
- **Diameter thresholds for cas 3 and cas 2 (C/D/E).**
- **Dendromicrohabitat groups (F):** ferns join group 12 and old resin flows join group 15.
- **Open flowering habitats (G):** flowering vegetation in the intermediate and high strata is excluded.
- **Grazing and orchard rules for H.**
- **Factor I:** a new aquatic type, "Mer ou océan".
- **Factor J:** twelve rocky types instead of nine.
- **The Factor A genus list:** Cupressus and Tamarix added, Juniperus widened beyond *J. thurifera*, the five supplementary genera extended to cas 2, and the species-level Table 1 with archaeophytes and every poplar cultivar.

Global aggregation (A–G max 35, H–J max 15, total max 50), all count thresholds, the G percentage bands and the interpretation chart are unchanged.

Of the 17 validation-matrix cases:
- **One changes value:** MAT-B-01, B = 2 → 5, because the cap moves to A.
- **One has an ambiguous context:** MAT-A-02, "subalpin" must become cas 3 to keep A = 2; the lower subalpine gives A = 1.
- **Four need their region/stage context rewritten as a cas:** MAT-A-01, MAT-A-02, MAT-G-01 and the SUBMIT cases.

The research also found app bugs that diverge from both versions:
- G and H accept a score of 1; both versions allow only 0/2/5.
- The home screen and nearby-parcel badge show the 0–50 total as "/10", with colour thresholds 7 and 5.
- The `ibp_method_version` field required by the spec was never implemented.

**Primary recommendation:** D-06's "migrate" branch applies. ADR-003 should decide **Migrate to IBP FR v3.2, implemented in phase 01.8**:
- Submitted surveys keep their v3.0 scores and are tagged `ibp_method_version = v3.0`. Recomputing them is not meaningful: their raw counts were made under v3.0 field definitions.
- Drafts are re-scored under v3.2. The mechanical parts can be migrated: B's native-cover value moves to A, and the cas is derived where the stage is unambiguous.
- Until 01.8 lands, the citation files must say the app **implements v3.0** and **targets v3.2 (ADR-003)**. Otherwise criterion 5 ("cite the version the app actually implements") would be false.

## Sources used and reproducibility

| Source | Role | Identity | Pages cited |
|--------|------|----------|-------------|
| IBP FR v3.2, CNPF / INRAE Dynafor, 02/02/2026 | v3.2 (authoritative) | `https://www.cnpf.fr/sites/socle/files/2026-04/IBP_FR_v3_2_260202.pdf`, 28 pp., 1,264,005 bytes, SHA-256 `f130b3d66522ec0f5692e87e361b628b4941ce9748d1ed5519a49734854bd001`, retrieved 2026-09-26 [VERIFIED: sha256sum] | p. 2 (cas classification), p. 3–4 (definition sheet A–J), p. 5–9 (complements/FAQ), p. 10–11 (Tables 1–2), p. 12–13 (dmh typology), p. 14–15 (aquatic), p. 16–17 (rocky), p. 18–19 (survey instructions), p. 20–21 (survey sheet), p. 22–25 (stand description, results chart), p. 28 (authorship; "La version précédente FR v3.0…") |
| App implementation | v3.0 baseline per D-02 (decisive) | `api/src/surveys/ibp-rules.service.ts`, `mobile/src/app/ibp-scoring.ts`, `mobile/src/app/vegetation.ts`, `mobile/src/i18n/fr/labels.ts`, `docs/specs/ibp-form-spec.md`, `docs/technical/ibp-validation-matrix-v1.md`, `api/test/ibp-rules.spec.ts` (18 tests pass), `mobile/src/app/ibp-scoring.test.ts` (4 tests pass) [VERIFIED: read + jest run 2026-09-26] | file:line cited below |
| CNPF "IBP Fr v3" survey sheets, dated 16/08/2021, sheet footer "version IBP Fr-ACA v3.210722" | Cross-check of official v3.0-family text (ACA sheet only) | `https://kebbe.iefc.net/wp-content/uploads/2022/03/Explication_general_IBP.pdf` (third-party mirror of a CNPF document), 8 pp., 724,689 bytes, SHA-256 `3751b6f6a0bfa68591a84fbbeb80aefc0eb004a90d8ff3cdaaff9a770b28b8e3` [VERIFIED: downloaded, sha256sum] | p. 3–4 (FAQ, linear table), p. 7–8 (ACA survey sheet) |
| Official v3.0 PDFs dated 2023-03-23 | Not available | Both repo links return HTTP 404 (re-checked 2026-09-26); Wayback availability API returns no snapshot for them or for CNPF's "tab_modif" / "modif entre v3.0 et v2.9" change tables (also 404) [VERIFIED: curl] | — |

**Caveat on the 2021 cross-check:** it is the first v3 release (July 2021), not the 2023-03-23 update the repo cites, and it has only the ACA sheet (the Mediterranean sheet is announced in its table of contents but absent from the 8-page mirror). Where the 2021 sheet and v3.2 agree against the app, the app is wrong under v3.0 with high probability. Where only the 2021 sheet and v3.2 differ, the 2023 v3.0 update *might* already have made that change, so those rows are "v3.0 (2021 sheet)" rather than "v3.0 (2023)".

**PDF text extraction note (for the executor):** the Read tool cannot render these PDFs here (`pdftoppm` missing, apt install fails). `pypdf` works if the broken `cryptography` import is stubbed:
```bash
python3 -c "
import sys; sys.modules['cryptography']=None
import pypdf; r=pypdf.PdfReader('IBP_FR_v3_2_260202.pdf')
open('v32.txt','w').write(''.join(f'\n===== PAGE {i+1} =====\n'+(p.extract_text() or '') for i,p in enumerate(r.pages)))"
```
Printed page headers ("IBP FR v3.2 (02/02/26) - p. N") match the physical page index, so page numbers in this document are the printed ones.

**Page-number hygiene finding:** `docs/technical/species-recognition-spike-measurements-v1.md` §2 cites the v3.2 genus list at "p. 2", archaeophytes at "p. 9" and Tableau 1 at "p. 9–10". The printed pages are **p. 3** (genus list; repeated on p. 20), **p. 10** (archaeophytes) and **p. 10–11** (Tableau 1). Its "p. 6" (Quercus note) and "p. 11" (Tableau 2) are correct. That is a citation-only fix the executor may make here (allowed by the phase boundary).

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Authoritative IBP scoring at write/submit | API (`IbpRulesService`, called from `SurveysService.prepareUpsert` and `submitSurvey`) | — | Server recomputes `factor_results` and `scores` on every upsert and at submit; stored JSONB is what the public map and parcel history read |
| Live score preview, submit readiness | Mobile (`ibp-scoring.ts`, used by `useSurveyForm`, `useLocalDraftSummary`, `FactorsList`) | — | Offline-first: drafts are scored on device; must match the API |
| Observer guidance (thresholds, strata heights, type lists) | Mobile i18n catalogue (`labels.ts` `factorHelp` / `factorInputHints`) | Spec (`ibp-form-spec.md`) | The app only takes counts/percentages; most v3.2 field-definition changes land here, not in the engine |
| Regional context (ACA/M + stage → v3.2 cas) | Mobile form + API DTO + DB column | Rules engine (A/G scale selection) | Stored as `region_version`, `vegetation_stage` TEXT columns |
| Method-version tagging | Database / API contract | Mobile payload | Needed by D-07; does not exist today |
| Decision record, comparison, citations | Docs (`docs/technical/`, `docs/references/`, `docs/specs/`) | CLAUDE.md, `docs/README.md` | This phase's actual output |

## What the app implements today (v3.0 baseline, per D-02)

All facts below: [VERIFIED: file read 2026-09-26].

| Aspect | API `ibp-rules.service.ts` | Mobile `ibp-scoring.ts` | Spec / matrix |
|---|---|---|---|
| Region model | `RegionVersion = "ACA" \| "M"` (l. 26); `normalizeRegion` (l. 443–448); `montagnard_mediterraneen` → ACA (l. 433–441); submit requires region (l. 82–91) and stage (l. 93–95) | same (l. 34–49); submit readiness requires ACA/M (l. 333–335) | spec §5; stages in `mobile/src/app/vegetation.ts` l. 12–27 (ACA: planitiaire, collineen, montagnard, subalpin; M: thermo/meso/supra; `montagnard_mediterraneen` only normalised, l. 39–41); DTO `@IsEnum(["ACA","M"])` `api/src/surveys/dtos/survey-upsert.dto.ts` l. 61–63 |
| A | count → 0/1/2/5; subalpin scale iff `ACA`+`subalpin` (l. 220–240, test at l. 228) | l. 58–78 (l. 66) | no genus list anywhere in the repo (grep: no Quercus/Fagus/Sorbus in code or product docs) — the observer types a number (`native_genus_count`) |
| B | strata count → 0/1/2/5; **capped at 2 if `covered_autochthonous_percent` < 50** (l. 242–262, cap l. 257–259) | l. 80–96 (cap l. 91–93) | spec §6 B; MAT-B-01; hint `labels.ts` l. 52 |
| C/D | `bmg/ha<1 && bmm/ha<1` → 0; `bmg/ha<1 && bmm/ha≥1` → 1; `<3` → 2; else 5 (l. 264–278) | l. 98–110 | spec §6 C/D |
| E | same shape with TGB/GB, 5/ha top (l. 280–294) | l. 112–124 | spec §6 E |
| F | `trees_per_ha` or `dmh_group_counts` capped at 2 per group + warning `factor_f_group_capped` (l. 296–329) | l. 126–149 (no warning — the known 01.8 drift) | MAT-F-02 |
| G | percent; 0 → 0; subalpin: <1 → 2 else 5; others: <1 or >5 → 2, else 5 (l. 331–357) | l. 151–175 | spec §6 G |
| H | `class_score` numeric passed through, or `recent/partial/ancient` (l. 359–370) | l. 177–187 | form restricts to {0,2,5} (`mobile/src/hooks/useSurveyForm.ts` l. 184–185, 453) |
| I/J | count → 0/2/5 (l. 372–378) | l. 189–195 | spec §6 I/J |
| Allowed scores | `STANDARD_ALLOWED={0,1,2,5}` for A–H, `IJ_ALLOWED={0,2,5}` (l. 34–35, 450–452); any finite number passes `resolveFactorScore` directly (l. 187–190) | l. 4–5, 51–56, 203–206 | matrix notes "A..H: 0\|1\|2\|5" |
| Aggregates | A–G, H–J, total (l. 419–431) | l. 267–287 | spec §4, §9 |
| Consistency warnings (app heuristic, not CNPF) | `consistency_a_b` (B≥2 && A=0), `consistency_e_f` (F≥5 && E=0) (l. 380–405) | none | MAT-CONS-01/02 |

## Classification model: v3.0 region/stage vs v3.2 "cas"

v3.2 p. 2 replaces the biogeographic-region model with four "cas" defined by growth constraints:
- **Cas 1:** low-to-medium constraints. This covers the temperate planitiaire, collinéen, montagnard and **lower subalpine** stages, plus well-watered riparian forests and humid supra-Mediterranean zones inside the Mediterranean region.
- **Cas 3:** high constraints from cold and a short growing season, i.e. the **middle and upper subalpine**.
- **Cas 4:** high constraints from heat and dry summers, i.e. the thermo-, meso- and supra-Mediterranean stages, except the cas-1 special cases.
- **Cas 2:** an overlay for very infertile stations, in any macroclimatic context. There, trees cannot reach the very-large-tree diameter: 67.5 cm in cas 1, 57.5 cm in cas 3/4. Heights generally stay ≤ 12 m (cas 1) or ≤ 8 m (cas 3/4). v3.2 calls this rare (p. 2, p. 5).

For factors A and G, cas-3 thresholds replace cas-2 ones in cas-3 zones and in forest habitats on lapiaz, dunes or peat bogs, or dominated by *Juniperus thurifera*. The footnote on p. 4 ties this to cas 2; p. 7 states it generally.

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| CLS-1 | Context = `region_version` ACA\|M + `vegetation_stage` (spec §5) | Context = cas 1/2/3/4 checkbox on the survey sheet ("Cas", p. 20); macroclimatic cas from thermotype / SER maps, confirmed in the field (p. 2, p. 5–6) | changes a field | `RegionVersion` type l. 26, `normalizeRegion` l. 443–448, `resolveThresholdRegion` l. 433–441, `region_version_required` l. 82–91; DTO enum `survey-upsert.dto.ts` l. 61–63, `survey-patch.dto.ts` l. 48–52 | `ibp-scoring.ts` l. 34–49, 333–335; `vegetation.ts` l. 7–44; `types.ts` l. 95–103; `labels.ts` l. 6–18; `constants.ts` l. 33–34 | Contexts of MAT-A-01, MAT-A-02, MAT-G-01, MAT-SUBMIT-01/02 must be expressed as cas |
| CLS-2 | `subalpin` (any) → subalpine A/G scales; diameters/strata as ACA | Only middle/upper subalpine = cas 3; lower subalpine = cas 1 (p. 2, p. 6 "Où se situe le cas 3 ?") | **changes a score** | `scoreFactorA` l. 228, `scoreFactorG` l. 349 | l. 66, l. 169 | MAT-A-02 ambiguous (cas 3 → A=2 unchanged; lower subalpine → A=1) |
| CLS-3 | No fertility field (the 2021 v3 sheet had "Fertilité: fertile / très peu fertile"; it only changed C/D/E diameters, observer-side) | Cas 2 also changes the A genus list (supplementary genera), B strata heights and C/D/E BMm/GB ranges (p. 3) | changes a field | new input needed (see change list) | new form input | new cases needed |
| CLS-4 | Not representable | Cas-3 A/G scales in cas-3 zones for cas-2 stands and in lapiaz/dune/peat-bog/*J. thurifera* habitats (p. 4 footnote \*, p. 7) | **changes a score** | A/G scale selection l. 228, 349 | l. 66, 169 | new cases needed |
| CLS-5 | 2021 sheet: IBP not intended for non-arborescent stages or agroforestry | Usable in fruticées and mixed wooded/open habitats (orchards, wood-pastures, "accrus"), mainly when tree cover > 10 % (p. 5) | wording only (scope) | none | none (maybe help text) | none |
| CLS-6 | `montagnard_mediterraneen` → ACA thresholds | No such stage; Mediterranean-boundary stands with stronger growth, riparian zones and humid supra-Mediterranean → cas 1 (p. 2, p. 6) | changes a field | l. 437–439 becomes obsolete | `vegetation.ts` l. 39–41 | none |

**Mapping for migrating existing context values** (for 01.8; one row is [ASSUMED], see Assumptions Log):

| v3.0 `region_version` / `vegetation_stage` | v3.2 cas | Unambiguous? |
|---|---|---|
| ACA / planitiaire, collineen, montagnard | 1 | yes |
| ACA / subalpin | 1 (lower) or 3 (middle/upper) | **no — ask the observer** |
| ACA / montagnard_mediterraneen | 1 | [ASSUMED] (v3.2 names no such stage) |
| M / thermo_mediterraneen, meso_mediterraneen | 4 | yes (unless riparian → 1) |
| M / supra_mediterraneen | 4, or 1 if humid supra / riparian | mostly 4; flag for review |
| any + very infertile station | 2 | not recorded today |

## Difference register, factor by factor

Severity key (D-04): **changes a score** = the scoring engine's output changes for the same stored inputs. **Changes a field** = what the observer counts or enters changes, so the score can change through the input. **Wording only** = no effect on scoring.

### Factor A — Native tree genera

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| A-1 | No cap on A; the < 50 % native-cover cap is applied to **B** | "Score plafonné à 2" when the cover of all native species is < 50 % of the described stand — on **A** (p. 3, p. 20). The 2021 v3 sheet also puts it on A (p. 7). | **changes a score** (and an app bug under v3.0, see BUG-1) | `scoreFactorA` l. 220–240 gains the cap and a cover input; `scoreFactorB` l. 257–259 loses it | `scoreFactorA` l. 58–78 / `scoreFactorB` l. 91–93; `labels.ts` l. 44–48 and l. 52; form fields `constants.ts` l. 40–41, `useSurveyForm.ts` l. 149–158; `LEGACY_DEFAULT_FACTOR_VALUES` `storage/db.ts` l. 39–41 | MAT-B-01 (B 2 → 5); new A-cap cases |
| A-2 | App: no list (count only); 2021 v3 ACA sheet list = 26 genera + Quercus split (see genus diff) | 28 genera + Quercus split = 29 countable taxa (p. 3) | changes a field | none (count only) | help text l. 45; phase 2 genus picker | none |
| A-3 | 2021 sheet: only *Juniperus thurifera* ("Genévrier thurifère") | Genus Juniperus, restricted by Table 1 to *J. thurifera* plus *J. macrocarpa* and *J. phoenicea*, the last two **coastal zone only** (p. 3, p. 10, footnote p. 11) | changes a field | none | help text / genus picker | none |
| A-4 | App/spec: "CNPF list for regional version" (no list); v3.0 M list not verifiable | Supplementary genera Ceratonia, Cercis, Olea, Phillyrea, Pistacia count in **cas 4 and cas 2** (p. 3) — cas 2 includes very infertile temperate stations | changes a field | none (count only) | genus picker must depend on cas | none |
| A-5 | 2021 sheet: nativity judged from the natural range (Flore forestière / Atlas) | Species-level Table 1 (p. 10–11): tree species only (normally > 7 m adult in closed forest), native at the survey location. Archaeophytes count within their ancient range (*Castanea sativa*, *Cupressus sempervirens*, *Juglans regia*, *Pinus pinea*). **All poplar cultivars count as native.** Shrub congeners do not count (e.g. *Salix alba* yes, *S. cinerea* no; Table 2) | changes a field | none | help text / genus picker | none |
| A-6 | — | *Ficus carica* is in Table 1 (newly native per Fady et al. 2025, p. 10–11) but Ficus is **not** in the p. 3 genus list, and p. 6 limits counting to listed genera → Ficus not counted | wording only (source inconsistency) | none | genus picker: exclude Ficus; document | none |
| A-7 | — | Pistacia is a supplementary genus (p. 3) but its native species (*P. lentiscus*, *P. terebinthus*) appear only in Table 2 (shrubs, not counted, p. 11) | wording only (source inconsistency) | none | genus picker decision (phase 2) | none |
| A-8 | Quercus split (2021 sheet: "Chêne à feuilles caduques / persistantes") | Same split, now explicit: evergreen includes pseudo-evergreen (*Q. suber*); deciduous includes marcescent (p. 6) | wording only | none | none | none |
| A-9 | Scales: cas 1/4 (and ACA non-subalpine, M) 0–1/2/3–4/≥5 → 0/1/2/5; subalpine 0/1/2/≥3 → 0/1/2/5 | Identical numbers; cas 1, 4 and 2\* use the first scale, cas 3 the second (p. 3) | none | none | none | none |
| A-10 | Living h > 50 cm or dead | Same (p. 3) | none | none | none | none |

### Factor A genus-list diff (complete)

**App list:** none. The app stores only `native_genus_count` and ships no genus list (the spec says "from CNPF list for regional version"). A genus list exists only in the phase-1 spike documentation (`species-recognition-spike-measurements-v1.md` §2, v3.2-derived, 34 classes).

**v3.0-family official list (2021 CNPF ACA sheet p. 7, French names → genus):** Sorbus (Alisier, Cormier, Sorbier), Arbutus, Alnus, Betula, Carpinus, Ostrya, Castanea, Quercus deciduous, Quercus evergreen, Picea, Acer, Fraxinus, *Juniperus thurifera* only, Fagus, Taxus, Prunus (Merisier and Cerisier à grappes), Larix, Celtis, Juglans (Noyer commun), Ulmus, Populus (Peuplier and Tremble), Pinus, Pyrus, Malus, Abies, Salix, Tilia. That is **26 genera + the Quercus split = 27 countable taxa**. The 2021 Mediterranean sheet was not available.

**v3.2 list (p. 3 and p. 20):**
- Main list: Abies, Acer, Alnus, Arbutus, Betula, Carpinus, Castanea, Celtis, Cupressus, Fagus, Fraxinus, Juglans, Juniperus, Larix, Malus, Ostrya, Pinus, Picea, Populus, Prunus, Pyrus, Quercus deciduae, Quercus sempervirens, Salix, Sorbus, Tamarix, Taxus, Tilia, Ulmus. That is **28 genera + the Quercus split = 29 countable taxa**.
- Supplementary genera for **cas 4 and cas 2**: Ceratonia, Cercis, Olea, Phillyrea, Pistacia (5).
- **Total: 33 genera, 34 countable taxa.** This matches ADR-002's 34 classes.

| Genus | 2021 v3 ACA sheet | v3.2 | Change |
|---|---|---|---|
| Abies, Acer, Alnus, Arbutus, Betula, Carpinus, Castanea, Celtis, Fagus, Fraxinus, Juglans, Larix, Malus, Ostrya, Picea, Pinus, Populus, Prunus, Pyrus, Salix, Sorbus, Taxus, Tilia, Ulmus (24) | listed | listed | none at genus level (Table 1 now restricts species, e.g. Prunus = *P. avium*, *P. padus*; Salix = *S. alba*, *S. caprea*, *S. fragilis*; Juglans = *J. regia*) |
| Quercus deciduae / sempervirens | listed (caduques / persistantes) | listed | wording only |
| Juniperus | *J. thurifera* only | genus; *J. thurifera* + coastal *J. macrocarpa*, *J. phoenicea* | **widened** |
| Cupressus | absent | listed (*C. sempervirens*, archaeophyte) | **added** |
| Tamarix | absent | listed (*Tamarix* spp.) | **added** |
| Ceratonia, Cercis, Olea, Phillyrea, Pistacia | not on the ACA sheet (M sheet unavailable) | supplementary, cas 4 **and cas 2** | added to temperate cas-2 stands; Mediterranean status in v3.0 unverified |
| Ficus | absent | in Table 1 but not in the genus list | not counted (flag to CNPF) |

**Table 1 species per genus (v3.2, p. 10–11), for phase 2:**
- *Abies alba*
- *Acer campestre*, *A. monspessulanum*, *A. opalus*, *A. platanoides*, *A. pseudoplatanus*
- *Alnus cordata*, *A. glutinosa*, *A. incana*
- *Arbutus unedo*
- *Betula pendula*, *B. pubescens*
- *Carpinus betulus*
- *Castanea sativa*
- *Celtis australis*
- *Ceratonia siliqua*
- *Cercis siliquastrum*
- *Cupressus sempervirens*
- *Fagus sylvatica*
- *Ficus carica* (see A-6)
- *Fraxinus angustifolia*, *F. excelsior*, *F. ornus*
- *Juglans regia*
- *Juniperus macrocarpa* (coastal only), *J. phoenicea* (coastal only), *J. thurifera*
- *Larix decidua*
- *Malus sylvestris*
- *Olea europaea*
- *Ostrya carpinifolia*
- *Phillyrea latifolia*
- *Picea abies*
- *Pinus cembra*, *P. halepensis*, *P. mugo*, *P. nigra* subsp. *laricio*, *P. nigra* subsp. *salzmannii*, *P. pinaster*, *P. pinea*, *P. sylvestris*, *P. uncinata*
- *Populus alba*, *Populus* cultivars, *P. nigra*, *P. tremula*, *P. × canescens*
- *Prunus avium*, *P. padus*
- *Pyrus communis* subsp. *pyraster*
- *Quercus cerris*, *Q. petraea*, *Q. pubescens*, *Q. pyrenaica*, *Q. robur* (deciduous)
- *Quercus ilex*, *Q. rotundifolia*, *Q. suber* (evergreen)
- *Salix alba*, *S. caprea*, *S. fragilis*
- *Sorbus aria*, *S. aucuparia*, *S. domestica*, *S. torminalis*
- *Tamarix* spp.
- *Taxus baccata*
- *Tilia cordata*, *T. platyphyllos*
- *Ulmus glabra*, *U. laevis*, *U. minor*

**Table 2 (shrubs, never counted, p. 11)** includes *Alnus alnobetula*, *Buxus*, *Corylus avellana*, *Crataegus* spp., *Ilex aquifolium*, *Juniperus communis*, *J. oxycedrus*, *Laurus nobilis*, *Pistacia lentiscus*, *P. terebinthus*, *Prunus mahaleb*, *P. spinosa*, *Pyrus spinosa*, *Quercus coccifera*, *Salix atrocinerea*, *S. cinerea*, *S. eleagnos*, *Sambucus nigra* and others. It is the list phase 2 needs to exclude.

### Factor B — Vertical vegetation structure

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| B-1 | Cap at 2 if native cover < 50 % | No cap on B (the cap is on A, p. 3) | **changes a score** | remove l. 257–259; drop `covered_autochthonous_percent` requirement at l. 244–249 | l. 91–93, l. 82–83; `labels.ts` l. 51–52; form | **MAT-B-01: expected B = 5** (was 2) |
| B-2 | ACA strata: very low < 1.5 m, low 1.5–7 m, intermediate 7–20 m, high > 20 m (spec §6 B; 2021 sheet p. 7 agrees) | Cas 1: < 1.5 / 1.5–7 / **7–18 / > 18 m** (p. 3) | changes a field | none (count only) | help text (`labels.ts` l. 49–53 has no heights today; spec §6 B) | none |
| B-3 | M strata: < 1.5 / 1.5–5 / 5–15 / > 15 m (spec; v3.0 M sheet unverified). ACA subalpine and very infertile stations used ACA heights | Cas 2, 3, 4: < 1.5 / 1.5–5 / **5–12 / > 12 m** (p. 3) | changes a field | none | help text; spec | none |
| B-4 | Strata count with ≥ 20 % cover; one woody plant in several strata; 5 strata; scale 1/2/3–4/5 → 0/1/2/5 | Same (p. 3, p. 6–7); mosses not counted (p. 7) | none / wording only | none | none | none |

### Factors C and D — Large standing / downed deadwood

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| CD-1 | 0: BMg/ha < 1 **and BMm/ha < 1**; 1: BMg/ha < 1 **and BMm/ha ≥ 1** (app l. 274–275; spec; 2021 sheet p. 7 agrees) | 0: BMg < 1/ha and **(BMm+BMg) < 1/ha**; 1: BMg < 1/ha and **(BMm+BMg) ≥ 1/ha** (p. 3, p. 20) | **changes a score** | `scoreFactorCD` l. 274–275 | l. 106–107; `labels.ts` l. 57, 62 | MAT-C-01, MAT-D-01 unchanged; new case: bmg=1, bmm=1, surface=2 → v3.0 0, v3.2 1 |
| CD-2 | ACA: BMg D > 37.5, BMm 17.5–37.5 (subalpine used ACA); M: BMg > 27.5, BMm 17.5–27.5 | Cas 1: > 37.5 / 17.5–37.5; **cas 3, 4: > 27.5 / 17.5–27.5** (p. 3) | changes a field (cas 3) | none | help text/spec | none |
| CD-3 | Special cases (very infertile, slow-growing species): BMg > 17.5; BMm range not redefined (2021 sheet) | Cas 2 and slow-growing species: BMg > 17.5, **BMm 7.5–17.5** (p. 3) | changes a field | none | help text/spec | none |
| CD-4 | "Essences à faible croissance" not listed | Listed: *Acer monspessulanum*, *Alnus incana*, *Arbutus*, *Malus*, *Prunus padus*, *Pyrus*, *Sorbus* other than *S. torminalis* and *S. domestica* (p. 4 footnote \*\*) | wording only | none | help text | none |
| CD-5 | Standing ≥ 1 m high; downed ≥ 1 m long; diameter at 1.3 m / 1 m from the large end. The 2021 FAQ counts the pieces of one tree once "unless decay stages clearly differ" | Same thresholds. Diameter at 1 m when a snag is 1–1.3 m tall. A leaning dead tree counts as downed if its branches touch the ground. Pieces count once, **with no decay-stage exception**. Freshly felled trees awaiting removal are not counted (p. 7) | changes a field (minor) | none | help text | none |
| CD-6 | Linear stands (< 15 m wide) per km: BMg/km < 9 and BMm/km < 9 → 0, etc. (spec §7) | Same linear table, still written with BMm alone (p. 6) — inconsistent with CD-1 | wording only (source inconsistency) | none (linear not implemented) | none | none |

### Factor E — Very large living trees

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| E-1 | 0: TGB/ha < 1 and **GB/ha < 1**; 1: TGB/ha < 1 and **GB/ha ≥ 1** (l. 290–291) | 0: TGB < 1/ha and **(GB+TGB) < 1/ha**; 1: TGB < 1/ha and **(GB+TGB) ≥ 1/ha** (p. 3); p. 18 caps GB at 1/ha, or 1 (GB+TGB)/ha on plots > 1 ha | **changes a score** | `scoreFactorE` l. 290–291 | l. 120–121; `labels.ts` l. 67 | MAT-E-01 unchanged; new case: tgb=1, gb=1, surface=2 → v3.0 0, v3.2 1 |
| E-2 | ACA: TGB > 67.5, GB 47.5–67.5 (subalpine used ACA); M: TGB > 57.5, GB 37.5–57.5 | Cas 1: > 67.5 / 47.5–67.5; **cas 3, 4: > 57.5 / 37.5–57.5** (p. 3) | changes a field (cas 3) | none | help text/spec | none |
| E-3 | Special cases: ACA TGB > 47.5 (spec; 2021 sheet), M TGB > 37.5 (spec) | Cas 2 and slow-growing species: **TGB > 37.5, GB 17.5–37.5** (p. 3) | changes a field (ACA infertile) | none | help text/spec | none |
| E-4 | — | Diameter at 1.3 m upslope; stems split below 1.3 m count separately in forests, but an orchard tree counts once (p. 7) | changes a field (minor) | none | help text | none |
| E-5 | Scale 1 / 5 per ha | Same (p. 3) | none | none | none | none |

### Factor F — Living trees bearing dendromicrohabitats (dmh)

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| F-1 | Group 12 (2021 sheet): mosses/lichens/ivy-lianas > 20 % of trunk, or mistletoe | Adds **ferns (> 5 fronds)**; the 20 % applies to at least one of the types (p. 3, p. 13) | changes a field | none | help text | none |
| F-2 | Group 15 (2021): active flow > 20 cm | Sap **and resin, fresh or old**, > 20 cm (p. 4, p. 13) | changes a field | none | help text | none |
| F-3 | Group 6 (2021): limb break at trunk "ø > 20 cm ou S > 300 cm²" | Limb break at trunk S > 300 cm² only; a cut branch is group 6 if flush and > 300 cm², group 7 if the stub is > 50 cm long, > 20 cm diameter and fully dead (p. 3, p. 7) | changes a field (minor) | none | help text | none |
| F-4 | Cap 2 trees/ha per group; app does not cap the total | Cap 2/ha per group, total max 8/ha on capped surveys (p. 20, p. 18) — the 2021 sheet also says 8 | none (≥ 8 already scores 5) | none | none | MAT-F-02 unchanged |
| F-5 | Linear stands: max **9** trees/km per group (spec §7; 2021 FAQ p. 3) | Max **7** trees/km per group (p. 6) | changes a field (linear only) | none (not implemented) | none | none |
| F-6 | — | Orchards: one tree whatever the number of stems (p. 7) | changes a field (minor) | none | help text | none |
| F-7 | Thresholds < 2 / [2,3[ / [3,8[ / ≥ 8 per ha; 15 groups | Same (p. 3) | none | none | none | none |

### Factor G — Open flowering habitats

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| G-1 | Flowering open habitats: gaps, edges at a standard 2 m width, sparse stands (spec) | Same three forms, but flowering vegetation of the **intermediate and high strata is excluded** (p. 4) | changes a field | none | help text (`labels.ts` l. 74–78) | none |
| G-2 | Subalpine scale iff ACA+subalpin | Cas-3 scale (see CLS-2, CLS-4) | **changes a score** (via context) | l. 349 | l. 169 | MAT-G-01 context → cas 1 (value 5 unchanged) |
| G-3 | Linear: nothing specific | Linear stands: at least one flowering edge → score 2, otherwise compute the % of the line (p. 6) | changes a field (linear only) | none | none | none |
| G-4 | 0 → 0; < 1 % or > 5 % → 2; 1–5 % → 5; subalpine: < 1 % → 2, ≥ 1 % → 5 | Identical (p. 4) | none | none | none | none |
| G-5 | Allowed scores {0,1,2,5} | G is scored 0/2/5 only (p. 4) | app bug (BUG-2) | l. 34, 450–452 | l. 4, 51–56 | matrix note "A..H" |

### Factor H — Temporal continuity of forest cover

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| H-1 | 0 recent / 2 partial or reforested with full-surface soil work / 5 ancient (État-major map + later documents + field) | Same classes (p. 4). Adds: ancient **grazed** forests drop 5 → 2 when cover was heavily reduced for grazing or grazing pressure is strong; **orchards** (chestnut, carob, walnut, olive) always drop to 2 for grazing, and for installation or upkeep work over nearly the whole surface (p. 8–9, fig. 4) | changes a field (can change the class) | none | help text (`labels.ts` l. 79–83) | none |
| H-2 | — | Cover had to be ≥ 10 % at the reference date to count as forest; otherwise the forest is recent (0) (p. 8) | changes a field | none | help text | none |
| H-3 | Cassini map / Napoleonic cadastre usable "to lift uncertainty" (2021) | Not used in IBP (p. 8) | wording only | none | none | none |
| H-4 | Allowed {0,1,2,5}; `class_score` passed through | 0/2/5 only (p. 4) | app bug (BUG-2) | l. 359–361 + allowed set | l. 177–179 (the form already restricts) | matrix note |

### Factor I — Aquatic habitats

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| I-1 | 10 types (2021 sheet p. 8: spring/seep, rill/ditch/small canal < 1 m, small stream 1–8 m, river > 8 m, oxbow, lake, pond/lagoon, pool, peat bog, marsh) | 11 types: adds **"Mer ou océan"** (p. 4, p. 15) | changes a field (coastal stands can gain a type → score) | none | help text (`labels.ts` l. 84–88) | none |
| I-2 | Temporary water counts if it persists beyond floods | Same; **water-filled ruts are excluded** explicitly (p. 9) | changes a field (minor) | none | help text | none |
| I-3 | 0 / 1 / ≥ 2 types → 0 / 2 / 5 | Same (p. 4) | none | none | none | MAT-I-01, MAT-I-02 unchanged |

### Factor J — Rocky habitats

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| J-1 | 9 types (2021 sheet p. 8): cliff higher than the stand; slab; lapiaz/diaclase; cave; stable boulder heap; pebble bank; unstable scree; boulder chaos > 2 m; and one type "rock lower than the stand" that bundled large blocks, low walls or ledges and outcrops | **12 types** (p. 4, p. 16–17): the "lower rock" type is split into **"paroi rocheuse de hauteur inférieure à celle des arbres adultes"** and **"gros blocs (> 20 cm) ou affleurements autres que dalle ou lapiaz"**. Two types are added: **"dépôt de sédiments fins, peu végétalisé"** (alluvial deposit outside the minor bed, or dune) and **"berge verticale meuble ou paroi de matériau meuble, peu végétalisée"** | changes a field (can raise the type count → score) | none | help text (`labels.ts` l. 89–93) | none |
| J-2 | Cliff "higher than the stand" | Higher "than adult trees" (p. 4) | wording only | none | none | none |
| J-3 | Cumulative area > 20 m²; rock in the minor riverbed excluded; 0/1/≥ 2 → 0/2/5 | Same (p. 4, p. 9) | none | none | none | MAT-J-01 unchanged |

### Global score and categories

| ID | v3.0 / app | v3.2 (page) | Severity | API impact | Mobile impact | Matrix impact |
|---|---|---|---|---|---|---|
| GS-1 | "IBP peuplement et gestion" = A+…+G (max 35); "IBP contexte" = H+I+J (max 15); total max 50 | Same grouping (p. 3–4 headings, p. 21 sheet) | none | none (l. 419–431) | none (l. 267–287) | MAT-SUBMIT-02 unchanged |
| GS-2 | Interpretation chart (2021 sheet p. 8): stand axis 0–35 in 5 bands at 7/14/21/28 (faible, assez faible, moyenne, assez forte, forte); context axis 0–15 in 3 bands at 5/10 (faible, moyenne, forte) | Identical chart (p. 24–25) | none | none | App implements no bands; it shows the total as "/10" (BUG-3) | none |
| GS-3 | Transect band 10–15 m each side (2021) | 10–20 m each side; capped/uncapped surveys; plot-group computation (p. 18) | wording only (method) | none | none | none |

### Counts for the headline

- **Engine-level score-changing differences: 4.** A-1/B-1 (the cap moves from B to A), CD-1, E-1, and CLS-2/CLS-4/G-2 (the A/G scale is selected by cas).
- **Field-definition differences that can change a score through the input: 21 main rows.** CLS-1, CLS-3, CLS-6, A-2, A-3, A-4, A-5, B-2, B-3, CD-2, CD-3, E-2, E-3, F-1, F-2, G-1, H-1, H-2, I-1, J-1, plus F-5/G-3 (linear stands only).
- **Minor field changes:** CD-5, E-4, F-3, F-6, I-2.
- **Wording-only differences:** CLS-5, A-6, A-7, A-8, CD-4, CD-6, H-3, J-2, GS-3.
- **No change:** the count thresholds for every factor (A-9, B-4, E-5, F-7, G-4, I-3, J-3), the aggregation (GS-1) and the interpretation chart (GS-2).

## App bugs that diverge from BOTH versions (flag separately)

| ID | Bug | Evidence | Where | Recommendation |
|---|---|---|---|---|
| BUG-1 | Native-cover < 50 % cap applied to **B** instead of **A** | v3.2 p. 3/p. 20 and the 2021 v3 sheet p. 7 both put it on A. The 2023 v3.0 text is unverified but sits between two versions that agree, so MEDIUM-HIGH confidence | spec §6 B and §8.1; API l. 242–262; mobile l. 80–96; `labels.ts` l. 52; `useSurveyForm.ts` l. 152–158; MAT-B-01; `api/test/ibp-rules.spec.ts` l. 41–45 | **Record for 01.8** (CH-1). Not small: it moves an input from B to A in the form, payload, DTO-free JSON and both engines |
| BUG-2 | G and H accept score 1 (direct numeric `{G:1}`, `{H:1}` or `{H:{class_score:1}}` pass); both versions score G and H 0/2/5 only | `STANDARD_ALLOWED` covers A–H (API l. 34, 450–452; mobile l. 4, 51–56); `scoreFactorH` passes `class_score` through (API l. 360–361; mobile l. 178–179). The mobile form blocks H=1 (`useSurveyForm.ts` l. 185, 453) but the API accepts it | API, mobile engine, matrix Notes ("A..H: 0\|1\|2\|5") | Small, but it tightens validation that also runs on **replays of submitted surveys** (`prepareUpsert` re-validates every upsert, `surveys.service.ts` l. 222–233). A stored G=1/H=1 row, reachable only through direct API calls, would then 422 on replay. **Record for 01.8 (CH-5)**, with a production count query first. The matrix Notes line may be corrected here, since it is documentation |
| BUG-3 | Nearby-parcel badge and "sector" card show the 0–50 `ibp_total` as "/10", coloured by thresholds 7 (high) and 5 (mid) | `mobile/src/ui/IbpScoreBadge.tsx` l. 10–15, 30–34; `mobile/src/app/brand-tokens.ts` l. 149–157; `mobile/src/screens/home/SectorScoreCard.tsx` l. 11–34 (10 dots); `mobile/src/i18n/fr/components.ts` l. 46; `mobile/src/i18n/fr/home.ts` l. 34. The input is `latest_ibp_total` = `scores->>'ibp_total'` (`api/src/surveys/public-map.queries.ts` l. 139, 199) | Presentation, not scoring. Both versions use /50, or /35 and /15 with the bands in GS-2 | Record in the comparison doc (outside the method question). Propose a separate fix: denominator /50 and bands from GS-2. That needs an owner/design decision, so it is not fixed here |
| BUG-4 | `ibp_method_version` is "required" in the spec (§3, §10.1, §10.3, §10.4), but no column, DTO field or mobile field exists | `api/migrations/001_init.sql` l. 14–35 (no such column); DTOs have none; grep finds it only in the spec | Spec/implementation drift | Superseded by D-07's method-version need → 01.8 (CH-6). The spec should say it is not implemented yet |
| BUG-5 | Spec §5 offers ACA stage `montagnard_mediterraneen`; the mobile form does not (normalises it to `montagnard`) | `vegetation.ts` l. 16–21, 39–41 | Harmless (both map to ACA thresholds) | Moot under v3.2 (cas 1) |
| DRIFT-1 | `factor_f_group_capped` warning exists only in the API | known, ROADMAP 01.8 criterion 2 | — | Already in 01.8 |

## Validation matrix under v3.2 (all 17 cases recomputed)

| Case | v3.0 input / context | v3.0 expected | v3.2 context | v3.2 expected | Changes? |
|---|---|---|---|---|---|
| MAT-A-01 | ACA/collineen, A count 2 | A=1 | cas 1 (native cover ≥ 50 % needed as new input) | A=1 | context only |
| MAT-A-02 | ACA/subalpin, A count 2 | A=2 | cas 3 (middle/upper subalpine) | A=2 (**A=1 if lower subalpine = cas 1**) | context; value depends on subalpine level |
| MAT-B-01 | strata 5, cover 40 | B=2 (cap) | cover moves to A | **B=5** (and A capped at 2 if A's raw score > 2) | **value changes** |
| MAT-C-01 | bmg 0, bmm 2, 1 ha | C=1 | same | C=1 (sum 2 ≥ 1) | no |
| MAT-D-01 | bmg 4, bmm 0, 1 ha | D=5 | same | D=5 | no |
| MAT-E-01 | tgb 0, gb 2, 1 ha | E=1 | same | E=1 (sum 2 ≥ 1) | no |
| MAT-F-01 | trees/ha 8 | F=5 | same | F=5 | no |
| MAT-F-02 | groups [3,3,3,3] | F=5 + warning | same | F=5 + warning (cap 2/group, total 8) | no |
| MAT-G-01 | ACA/collineen, 2 % | G=5 | cas 1 | G=5 | context only |
| MAT-H-01 | class partial | H=2 | same | H=2 | no |
| MAT-I-01 | 1 type | I=2 | same | I=2 | no |
| MAT-I-02 | direct I=1 | blocking | same | blocking | no |
| MAT-J-01 | 2 types | J=5 | same | J=5 | no |
| MAT-CONS-01 | A=0, B=2 | warning `consistency_a_b` | same (app heuristic) | warning | no |
| MAT-CONS-02 | E=0, F=5 | warning `consistency_e_f` | same | warning | no |
| MAT-SUBMIT-01 | expired + incomplete | `survey_expired` + missing factors | same | same, plus a missing cas (and method-version) error if the new fields are required | expected error list grows |
| MAT-SUBMIT-02 | complete A..J (direct scores) | ok, totals | cas instead of region/stage | ok, same totals | context only |

The 17 cases do not exercise CD-1 or E-1 (all use surface = 1 ha, where the sum and BMm-alone rules agree). **v3.2 needs new cases** (proposed IDs):
- **MAT-A-03:** cas 1, count 5, cover 40 → A = 2.
- **MAT-A-04:** cas 1, count 5, cover 50 → A = 5.
- **MAT-A-05:** cas 2 in a cas-3 zone or special habitat, count 2 → A = 2.
- **MAT-C-02:** bmg 1, bmm 1, surface 2 → C = 1 (v3.0 gave 0).
- **MAT-E-02:** tgb 1, gb 1, surface 2 → E = 1 (v3.0 gave 0).
- **MAT-G-02:** cas 3, 0.5 % → G = 2; 6 % → G = 5.
- **MAT-G-03:** direct G = 1 → blocking.
- **MAT-H-02:** direct H = 1 or `class_score` 1 → blocking.
- **MAT-VER-01:** a v3.0-tagged payload is scored with v3.0 rules; a replay of a submitted v3.0 survey stays accepted.

## Data model facts (D-07)

[VERIFIED: migrations, services and mobile storage read 2026-09-26]

**API (PostgreSQL) — the `surveys` table:**
- Stored per survey (`api/migrations/001_init.sql` l. 14–35; `002_factor_results.sql`):
  - `region_version TEXT`, `vegetation_stage TEXT`.
  - `factors JSONB`: the raw observer inputs as sent, e.g. `{"B":{"strata_count":5,"covered_autochthonous_percent":40}}`, or a bare number for a direct score.
  - `factor_results JSONB`: per factor `factor_id`, `observed_value_raw`, `selected_class`, `score_points`, `warnings`.
  - `scores JSONB`: `{ibp_peuplement_gestion, ibp_contexte, ibp_total}`.
- `version_number` (migration 008) is the **parcel follow-up version**, not a method version. **No method-version column exists.**
- The DTO restricts `region_version` to `ACA|M` (`survey-upsert.dto.ts` l. 61–63).

**When scores are computed:**
- On every upsert: `prepareUpsert` → `validateDraft` (`surveys.service.ts` l. 211–248). A failed validation returns 422 **before** the submitted-read-only check.
- On patch: l. 651–664.
- At submit: `validateSubmit`, l. 972–977.
- Submitted surveys are read-only for `region_version`, `vegetation_stage` and `factors`. An identical replay is accepted, but only `visibility`/`sync_version` are written; `scores`/`factor_results` are never rewritten (l. 380–399; `api-contract-v1.md` l. 786).

**Readers of the stored scores:**
- The public map, nearby parcels and parcel history read the stored `scores` (`public-map.queries.ts` l. 89–139, 199; `parcels.service.ts` l. 86–110).
- The public-map `region` filter matches `region_version` exactly (`api-contract-v1.md` l. 944).

**No recompute or backfill job exists.**

**Mobile (SQLite `cortege-local.db`):**
- `local_surveys.payload_json` holds the draft: `region_version`, `vegetation_stage`, `factors`, `parcel_ids`, `expires_at`, … (`mobile/src/storage/db.ts` l. 180–194). `payload_completion` is precomputed.
- The `sync_queue.payload` rows carry the same shape.
- Scores are **recomputed live** from the raw factors for the form and the local draft view (`useSurveyForm.ts` l. 250; `useLocalDraftSummary.ts` l. 64–86).
- `LEGACY_DEFAULT_FACTOR_VALUES` (`db.ts` l. 39–50) includes `B.covered_autochthonous_percent: 70`.

**Drafts expire 7 days after creation.** `expires_at` is set server-side and never moved by an upsert (`surveys.service.ts` l. 244–246). No v3.0 draft can outlive the switch-over by more than 7 days.

**What happens if the rules are changed in place, without versioning:**
- Submitted surveys keep their stored v3.0 scores, because nothing recomputes them.
- But a **replay** of a submitted v3.0 survey goes through `validateDraft` again. If the v3.2 engine requires a cas or an A native-cover input, those replays return 422. Installed apps replay pulled surveys, so their sync blocks. **Hence 01.8 must dispatch the rules by method version, with "no version" meaning v3.0.**

**Would stored scores change if recomputed under v3.2?**
- Deterministically, for the engine-level rules only:
  - B = 5 with native cover < 50 % → B becomes 5, and A drops to 2 when A was 5.
  - C/D/E with the lower class < 1/ha but lower + upper ≥ 1/ha → 0 becomes 1.
  - Subalpine surveys → the A/G scale depends on the subalpine level (unknown).
- Not at all for the field-definition changes. The raw counts (strata, types, dmh trees, genera, diameters) were made under v3.0 definitions and cannot be re-derived. **A v3.2 recomputation of a v3.0 survey would therefore be neither a v3.0 nor a v3.2 score.** This supports D-07's default: keep the submitted score and tag the version.

**Impact size is unknown from here.** Production data lives on the VPS. The planner should include a **read-only query** for the owner, so that ADR-003 can state numbers (see Code Examples).

## Decision (D-06) and draft ADR reasoning

**Branch:** v3.2 changes the scoring rules (four engine-level changes) and the Factor A genus list, so D-06's default applies: **migrate to IBP FR v3.2 in this milestone, implemented in phase 01.8**.

The "stay on v3.0" exception requires comparability that cannot be handled. It does not apply:
- Comparability is handled by tagging each survey with its method version and never mixing versions in one score.
- The milestone is internal-only (`PROJECT.md` l. 11), so the population of recorded surveys is small field-test data ([ASSUMED] until the query runs).
- The 7-day draft expiry bounds the transition.

**Draft ADR-003 content (the executor turns this into the ADR):**
- **Status:** Accepted (autonomous; the owner reviews at phase end). **Date:** 2026-09-26.
- **Context:**
  - The repo cites Fr v3.0 (links now 404); CNPF publishes FR v3.2 (02/02/2026). Phase 1 derived the genus list from v3.2.
  - The comparison doc lists 4 engine-level and ~21 field-definition differences.
  - The app also carries a v3.0-era bug: the cover cap is on B instead of A.
- **Decision:** "Migrate the app to IBP FR v3.2. The rules change once, in `packages/ibp-domain`, in phase 01.8, against the change list in the comparison document. Until 01.8 ships, the app computes Fr v3.0 scores and the documentation says so."
- **Effect on recorded surveys (D-07):**
  - Submitted surveys keep the score they were submitted with and are tagged `cnpf_ibp_fr_v3_0_2023-03-23`. They are not recomputed: their raw counts follow v3.0 definitions, so a recomputation would be neither version.
  - Drafts are re-scored under v3.2 when opened after the switch, using mechanical migration where possible:
    - B's `covered_autochthonous_percent` becomes A's native cover (it is the same quantity: the share of native species in the stand's cover).
    - The cas is derived from region/stage where unambiguous (see the mapping table).
    - Where the stage is ambiguous (`subalpin`, and optionally `supra_mediterraneen`), the cas is left blank and blocks submit until the observer picks it.
  - Because drafts expire after 7 days, this affects at most one week of drafts.
  - New surveys are tagged `cnpf_ibp_fr_v3_2_2026-02-02`.
  - Anything that shows or averages scores (public map, nearby-parcel average, parcel history) must either show the version or not mix versions in one average. 01.8 decides which.
- **Alternatives rejected:**
  - (a) Stay on v3.0: the documents are no longer published or retrievable, observers are trained on v3.2, and the phase-2 genus list would come from v3.2 while the app scored v3.0.
  - (b) Recompute all history under v3.2: impossible for field-definition changes, and misleading.
  - (c) Migrate now in this phase: out of scope; 01.8 owns the rules package.
- **Consequences:**
  - A method-version field is needed (01.8 or phase 2, per CONTEXT deferred list).
  - `region_version`/`vegetation_stage` give way to a cas input; contracts change (`api-contract-v1.md`, `data-contract-v1.md` l. 54–55).
  - The validation matrix becomes v2.
  - Phase 2 builds the genus list from v3.2 Table 1, keyed by cas.
- **Open points for CNPF:**
  - Ficus: in Table 1 but not in the genus list.
  - Pistacia: supplementary genus with only shrub species.
  - The linear C/D table still uses BMm alone.

**Criterion 5 wording (important):** after this phase, the code still implements v3.0. `docs/references/README.md` and `docs/specs/ibp-form-spec.md` must therefore say, in substance: "Implemented: IBP Fr v3.0 (as transcribed in this spec; the official v3.0 PDFs are no longer online). Target: IBP FR v3.2 (02/02/2026), adopted by ADR-003, lands in phase 01.8." They should add the v3.2 URL and record the dead v3.0 links as dead (not delete them silently). 01.8 flips "implemented" to v3.2. Writing "the app implements v3.2" now would falsify criterion 5.

## Change list for phase 01.8 (exact, testable)

| # | Change | Test that proves it |
|---|---|---|
| CH-1 | Move the native-cover cap from B to A. A = min(score from count, 2) when native cover < 50 %; exactly 50 % is not capped. B has no cap. Accept a percent or a `< 50 / ≥ 50` boolean (v3.2's capped survey records only the class, p. 20). Input moves from `factors.B` to `factors.A` | A cas 1 count 5 cover 40 → 2; cover 50 → 5; count 1 cover 10 → 0; B strata 5 (no cover) → 5 (MAT-B-01 v2) |
| CH-2 | C/D score 1 condition = BMg/ha < 1 and (BMg+BMm)/ha ≥ 1; score 0 = BMg/ha < 1 and (BMg+BMm)/ha < 1 | bmg 1, bmm 1, 2 ha → 1; bmg 0, bmm 0.9/ha → 0; existing MAT-C-01/D-01 unchanged |
| CH-3 | E score 1 condition = TGB/ha < 1 and (TGB+GB)/ha ≥ 1 | tgb 1, gb 1, 2 ha → 1; MAT-E-01 unchanged |
| CH-4 | Replace ACA/M + stage with a v3.2 case input. The engine needs one derived fact: does the A/G scale use cas 3? True for cas 3; true for cas 2 in a cas-3 zone or on lapiaz/dune/peat-bog/*J. thurifera* habitat (p. 4, p. 7); false otherwise. Recommended stored shape: macroclimatic cas (1\|3\|4), a very-infertile flag (→ cas 2) and a special-habitat flag, from which the effective cas and the A/G scale derive | A count 2: cas 1 → 1, cas 3 → 2, cas 4 → 1, cas 2 + cas-3 zone → 2; G 0.5 %: cas 3 → 2, cas 1 → 2; G 6 %: cas 3 → 5, cas 1 → 2 |
| CH-5 | Allowed scores: G and H ∈ {0,2,5} (BUG-2). Before shipping, count production rows with G/H = 1 | `{G:1}` → blocking `factor_invalid_score`; `{H:{class_score:1}}` → blocking |
| CH-6 | Method version: `ibp_method_version` on every survey (DB, DTO, mobile payload, sync contract). Missing means v3.0. The engine dispatches by version; v3.0 rules are kept for validating legacy replays | a v3.0 submitted survey replayed identically → `synced`, not 422; a v3.0 payload scores with v3.0 rules; a v3.2 payload with v3.2 rules |
| CH-7 | Draft migration (mobile SQLite `payload_json` + queue payloads + server drafts): move B's cover to A; derive the cas from region/stage (mapping table); leave ambiguous stages blank → submit readiness reports it missing | migration unit test on sample payloads; `evaluateSubmitReadinessFromDraft` reports the missing cas for `subalpin` |
| CH-8 | Guidance text in `mobile/src/i18n/fr/labels.ts` (`factorHelp`, `factorInputHints` l. 31–94):<br>• the A cap and B no cap<br>• strata heights per cas (B-2/B-3)<br>• diameters per cas (CD-2/3, E-2/3)<br>• the slow-growing species list<br>• dmh changes (F-1..F-3)<br>• G exclusion<br>• H grazing, orchard and 10 % rules<br>• I "Mer ou océan"<br>• J 12 types<br>• cas selector labels instead of `regions`/`vegetationStages` | catalogue unit test / grep; the existing i18n tests still pass |
| CH-9 | Contracts and filters: `api-contract-v1.md`, `data-contract-v1.md` (region enum), public-map `region` filter semantics, `mobile/src/app/types.ts` `RegionVersion`/`VegetationStage` | API E2E for the upsert/submit/public-map filter |
| CH-10 | `docs/technical/ibp-validation-matrix-v2.md`:<br>• revise MAT-B-01, A-01/02, G-01, SUBMIT-01/02<br>• add MAT-A-03..05, C-02, E-02, G-02/03, H-02, VER-01<br>• fix the Notes (G, H ∈ {0,2,5})<br>One parity fixture in `packages/ibp-domain` runs every case (01.8 criterion 2) | parity fixture green in the API and mobile |
| CH-11 | Score display (BUG-3): denominator /50, and bands per GS-2 if wanted. Needs an owner/design call | component tests for `IbpScoreBadge`/`SectorScoreCard` |
| CH-12 | Phase 2 (not 01.8): the genus list from v3.2 p. 3 + Table 1, keyed by cas (supplementary genera for cas 4/2; Juniperus coastal species; Ficus excluded; Pistacia per the CNPF answer) | phase 2's own tests |

## Standard Stack

No libraries are installed or added. This phase writes Markdown and may edit comments or citations.

| Tool | Version | Purpose |
|------|---------|---------|
| Existing Jest 29 + ts-jest | repo | Regression gate only (`npm run test:unit`) |
| `pypdf` (already installed system-wide) | present | Optional re-extraction of the v3.2 text with the cryptography stub above; not a project dependency |

## Package Legitimacy Audit

No external packages are recommended or installed by this phase. The slopcheck gate is not applicable.

| Package | Registry | Age | Downloads | Source Repo | slopcheck | Disposition |
|---------|----------|-----|-----------|-------------|-----------|-------------|
| (none) | — | — | — | — | — | — |

**Packages removed due to slopcheck [SLOP] verdict:** none
**Packages flagged as suspicious [SUS]:** none

## Architecture Patterns

### Information flow for this phase

```
v3.2 PDF (scratchpad, hashed) ──┐
2021 CNPF v3 ACA sheet (hashed)─┤
App rules (API + mobile + spec  ├──> difference register (this file)
+ matrix + tests)               ┘            │
                                             ├──> docs/technical/ibp-version-comparison-v3.0-v3.2.md  (D-04)
                                             ├──> docs/technical/adr-003-ibp-method-version-v1.md      (D-05/D-06/D-07)
                                             │         └──> change list consumed by phase 01.8
                                             ├──> citation updates: docs/references/README.md, docs/specs/ibp-form-spec.md (criterion 5)
                                             └──> index updates: CLAUDE.md doc tree, docs/README.md
```

### Files to create / modify

| File | Action | Content |
|---|---|---|
| `docs/technical/ibp-version-comparison-v3.0-v3.2.md` | create | Source table (hashes, dates, pages, baseline statement per D-02); classification section; one section per factor A–J; genus-list diff; global score and categories; "App bugs diverging from both versions"; matrix recomputation; each difference a row with columns `ID \| v3.0 / app \| v3.2 (page) \| Severity \| API impact \| Mobile impact \| Matrix impact` |
| `docs/technical/adr-003-ibp-method-version-v1.md` | create | ADR-002-style headings (Status, Date, Context, Decision, Consequences, Alternatives). A literal `**Decision:**` line. The D-07 statement. A link to the comparison doc and the 01.8 change list |
| `docs/references/README.md` | modify | "Implemented: Fr v3.0 … Target: FR v3.2 (ADR-003, phase 01.8)"; the v3.2 URL, date and SHA-256; mark the v3.0 links as no longer online (checked 2026-09-26); update "Cite as" to give both references (v3.2 reference string from p. 28: Gonin P., Larrieu L., Marty P., Baiges T., Corezzola S., Miozzo M., Palero N., 2026) |
| `docs/specs/ibp-form-spec.md` | modify | §2 Scope and §12 references: implemented v3.0, target v3.2 per ADR-003; §3 note that `ibp_method_version` is not implemented yet (BUG-4); a change-log entry. Do **not** rewrite the thresholds to v3.2 now (that would make the spec disagree with the code) — or, if the planner prefers, add clearly separated "v3.2 (target)" notes pointing to the comparison doc |
| `docs/technical/ibp-validation-matrix-v1.md` | optional modify | A header note: "v3.0 baseline; v3.2 changes in the comparison doc; MAT-B-01 changes under v3.2". Fixing the Notes line (G, H 0/2/5) is a documentation correction of BUG-2, allowed here |
| `docs/technical/species-recognition-spike-measurements-v1.md` | optional modify | Citation-only page-number fixes (p. 2 → p. 3, p. 9 → p. 10, p. 9–10 → p. 10–11); its "Version drift (open)" paragraph can point to ADR-003 |
| `docs/technical/adr-002-on-device-species-recognition-v1.md` | optional modify | One cross-reference line to ADR-003 (citation only) |
| `CLAUDE.md` | modify | Documentation index: add the `adr-003-…` and `ibp-version-comparison-…` lines under `technical/` |
| `docs/README.md` | modify | Mention ADR-003 / the comparison doc under `docs/technical/` (D-05) |
| `api/src/surveys/ibp-rules.service.ts`, `mobile/src/app/ibp-scoring.ts` | optional comment only | e.g. a header comment "Implements IBP Fr v3.0; v3.2 migration per ADR-003 (phase 01.8)". Allowed as a citation/comment; must pass lint/format |

### Anti-patterns to avoid
- **Claiming v3.2 conformance in the citation files before 01.8.** Criterion 5 asks for the *implemented* version.
- **Pasting v3.2 tables** (genus table, dmh typology, aquatic/rocky typology descriptions). D-03: facts in our own words; the species list as bare Latin names is factual data, but the French descriptive text is not to be copied.
- **Treating the 2021 sheet as "v3.0 (2023)".** Label it as the 2021 v3 sheet everywhere.
- **Fixing BUG-1 here.** It is not small (the input moves between factors and payload shapes change) and it overlaps 01.8's CH-1.

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Re-deriving v3.2 facts during execution | A new PDF reading pass with different tooling | This file's register (page-cited) plus the scratchpad `v32.txt` extraction | Keeps one transcription; avoids divergent page numbers |
| Verifying the deliverables | Manual eyeballing | The grep/awk checks in Validation Architecture | D-08 lists mechanical checks |
| Counting affected production surveys | Guessing in the ADR | The read-only SQL below, run by the owner | D-07 wants plain statements; numbers beat guesses |

## Runtime State Inventory (for the 01.8 migration; this phase changes none of it)

| Category | Items Found | Action Required (01.8) |
|----------|-------------|------------------|
| Stored data | `surveys.region_version`, `vegetation_stage`, `factors` (B cover inside `factors.B`), `factor_results`, `scores` in PostgreSQL; mobile `local_surveys.payload_json` and `sync_queue.payload` | Data migration: tag existing rows v3.0; draft payload migration (CH-7); no rescoring of submitted rows |
| Live service config | None — the rules are code, not config | None |
| OS-registered state | None | None |
| Secrets/env vars | None reference the IBP version | None |
| Build artifacts | The API Docker image (`ghcr.io/florianlepont/cortege:latest`) embeds the v3.0 rules; installed mobile apps embed v3.0 `ibp-scoring.ts` and will keep replaying v3.0-shaped payloads | Version-dispatching engine (CH-6); the API must accept old-app payloads until every device has updated |

## Common Pitfalls

### Pitfall 1: Criterion 5 made false by an optimistic citation
**What goes wrong:** the README/spec say "v3.2" while the code still runs v3.0.
**How to avoid:** use the wording "implemented v3.0 / target v3.2 (ADR-003, 01.8)".
**Warning signs:** `grep -n "v3.2" docs/references/README.md` shows v3.2 without the word "target" or "ADR-003".

### Pitfall 2: Replay breakage when rules change
**What goes wrong:** `prepareUpsert` re-validates every upsert, including identical replays of submitted surveys. A stricter v3.2 engine then 422s old surveys and blocks device sync.
**How to avoid:** the ADR's consequences and CH-6 require version dispatch, with legacy meaning v3.0.

### Pitfall 3: Page numbers off by one
**What goes wrong:** the spike doc already mis-cites v3.2 pages.
**How to avoid:** cite the printed "p. N" header. Genus list p. 3 (and p. 20); Table 1 p. 10–11; Table 2 p. 11; cas definitions p. 2; complements p. 5–9.

### Pitfall 4: Over-quoting
**What goes wrong:** D-03 is breached by copying the typology descriptions.
**How to avoid:** at most short quoted phrases (e.g. « Score plafonné à 2 », « Mer ou océan ») with page references.

### Pitfall 5: Counting Ficus or shrub congeners in the genus list
**What goes wrong:** Table 1 includes Ficus, but the genus list does not; Table 2 species never count.
**How to avoid:** state the rule (p. 6): only listed genera, restricted to Table 1 species native at the location.

### Pitfall 6: Forgetting that I/J/G/H scores are 0/2/5
**What goes wrong:** the matrix Notes say "A..H: 0|1|2|5".
**How to avoid:** the comparison records G and H as 0/2/5 in both versions (BUG-2).

## Code Examples

### Read-only impact query for the owner (VPS, for ADR-003's D-07 numbers)
```sql
-- Source: derived from api/migrations/001_init.sql column shapes; factors may hold a bare number (direct score)
WITH s AS (
  SELECT status, vegetation_stage, factors,
    CASE WHEN jsonb_typeof(factors->'B') = 'object' THEN factors->'B' END AS b,
    CASE WHEN jsonb_typeof(factors->'C') = 'object' THEN factors->'C' END AS c,
    CASE WHEN jsonb_typeof(factors->'D') = 'object' THEN factors->'D' END AS d,
    CASE WHEN jsonb_typeof(factors->'E') = 'object' THEN factors->'E' END AS e
  FROM surveys WHERE deleted_at IS NULL
)
SELECT status,
  count(*) AS surveys,
  count(*) FILTER (WHERE (b->>'strata_count')::numeric >= 5
    AND COALESCE(b->>'covered_autochthonous_percent', b->>'native_cover_percent')::numeric < 50) AS b_cap_hits,
  count(*) FILTER (WHERE (c->>'bmg_count')::numeric / NULLIF((c->>'surface_ha')::numeric,0) < 1
    AND (c->>'bmm_count')::numeric / NULLIF((c->>'surface_ha')::numeric,0) < 1
    AND ((c->>'bmg_count')::numeric + (c->>'bmm_count')::numeric) / NULLIF((c->>'surface_ha')::numeric,0) >= 1) AS c_sum_rule_hits,
  count(*) FILTER (WHERE (d->>'bmg_count')::numeric / NULLIF((d->>'surface_ha')::numeric,0) < 1
    AND (d->>'bmm_count')::numeric / NULLIF((d->>'surface_ha')::numeric,0) < 1
    AND ((d->>'bmg_count')::numeric + (d->>'bmm_count')::numeric) / NULLIF((d->>'surface_ha')::numeric,0) >= 1) AS d_sum_rule_hits,
  count(*) FILTER (WHERE (e->>'tgb_count')::numeric / NULLIF((e->>'surface_ha')::numeric,0) < 1
    AND (e->>'gb_count')::numeric / NULLIF((e->>'surface_ha')::numeric,0) < 1
    AND ((e->>'tgb_count')::numeric + (e->>'gb_count')::numeric) / NULLIF((e->>'surface_ha')::numeric,0) >= 1) AS e_sum_rule_hits,
  count(*) FILTER (WHERE vegetation_stage = 'subalpin') AS subalpine,
  count(*) FILTER (WHERE factors->>'G' = '1' OR factors->>'H' = '1'
    OR factors->'H'->>'class_score' = '1') AS g_h_score_1
FROM s GROUP BY status;
```
Values are stored as JSON numbers by the mobile app. If a cast fails on a string value, wrap the casts in a `~ '^[0-9.]+$'` guard. This is a task for the owner; the executor cannot reach the VPS.

### Deliverable checks (see Validation Architecture)
```bash
f=docs/technical/ibp-version-comparison-v3.0-v3.2.md
for x in A B C D E F G H I J; do grep -qE "^##+ .*Factor $x\b" "$f" || echo "missing Factor $x"; done
grep -qiE "^##+ .*(genus|genre)" "$f" || echo "missing genus-list section"
grep -qiE "^##+ .*global" "$f" || echo "missing global score section"
for g in Abies Acer Alnus Arbutus Betula Carpinus Castanea Celtis Cupressus Fagus Fraxinus Juglans Juniperus Larix Malus Ostrya Pinus Picea Populus Prunus Pyrus Quercus Salix Sorbus Tamarix Taxus Tilia Ulmus Ceratonia Cercis Olea Phillyrea Pistacia; do grep -q "$g" "$f" || echo "genus $g missing"; done
grep -q f130b3d66522ec0f5692e87e361b628b4941ce9748d1ed5519a49734854bd001 "$f" || echo "hash missing"
# every difference row (IDs like A-1, CD-2, CLS-3, GS-1, BUG-1) has 7 non-empty cells
awk -F'|' '/^\| *(CLS|A|B|CD|E|F|G|H|I|J|GS)-[0-9]+ *\|/ { n=0; for(i=2;i<NF;i++){ gsub(/^ +| +$/,"",$i); if($i!="") n++ } if(n<7) print "incomplete row: " $2 }' "$f"
a=docs/technical/adr-003-ibp-method-version-v1.md
grep -qE '^\*\*Decision:\*\*|^Decision:' "$a" || echo "no Decision line"
grep -qi "method version" "$a" && grep -qi "submitted" "$a" || echo "D-07 statement missing"
grep -n "v3.0" docs/references/README.md docs/specs/ibp-form-spec.md | grep -qi "implement" || echo "implemented version not stated"
grep -q "adr-003-ibp-method-version-v1.md" CLAUDE.md || echo "CLAUDE.md index missing ADR-003"
grep -qi "adr-003" docs/README.md || echo "docs/README.md index missing ADR-003"
```

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| IBP Fr v3 (2021) / v3.0 (2023-03-23): biogeographic regions (atlantic, continental, alpine, Mediterranean) + stages + fertility | IBP FR v3.2 (02/02/2026): cas 1/2/3/4 by growth constraint, based on EUR.MED v3.2 (09/09/2025) (p. 28) | 2025–2026 | Region/stage inputs must become a cas input |
| v3.0 documents on `cnpf.fr/sites/socle/files/cnpf-old/...` | v3.2 at `cnpf.fr/sites/socle/files/2026-04/IBP_FR_v3_2_260202.pdf`; v3.0 no longer online | by 2026-09 | The repo's references are dead links |

**Deprecated/outdated:** the v3.0 PDFs cited by the repo (HTTP 404). v3.2 p. 28 names FR v3.0 as "la version précédente".

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | The official 2023-03-23 v3.0 text matches the 2021 v3 sheet on the points used as cross-checks (cap on A, strata heights, type lists, genus list, C/D/E score-1 rule) | Sources; register rows marked "2021 sheet" | Some rows labelled "v3.0 → v3.2 change" may have changed already in 2023. The **app-vs-v3.2** comparison (what matters, per D-02) is unaffected |
| A2 | The v3.0 Mediterranean sheet used strata 5–15 / > 15 m and had its own genus additions | Factor B, genus diff | Only affects how the v3.0 column is described for M; the v3.2 target is certain |
| A3 | `montagnard_mediterraneen` maps to cas 1 | Classification mapping | A wrong default cas for a rare stage; the mobile form does not offer this stage anyway |
| A4 | Few surveys are recorded (internal field tests), so version tagging is enough | Decision | If there are many, the ADR still holds; it only adds urgency to the version field |
| A5 | "(BMm+BMg) ≥ 1/ha" (p. 3) is a deliberate v3.2 rule change, not a notation variant, even though the p. 6 linear table still uses BMm alone | CD-1, E-1 | If CNPF meant BMm alone, CD-1/E-1 drop from score-changing to wording. p. 3, p. 20 and p. 18 ("1 [GB+TGB]/ha") consistently use the sum, so the risk is low |
| A6 | Survey-sheet footnote \* (cas-3 thresholds for A/G) applies to the special habitats whatever the cas, as p. 7 states | CLS-4 | Narrower scope if it is limited to cas 2 |

## Open Questions (RESOLVED)

1. **Lower vs middle/upper subalpine for existing `subalpin` surveys.**
   - What we know: v3.2 splits the subalpine (p. 6); the app stores only `subalpin`.
   - What's unclear: which level each stored survey used.
   - Recommendation: leave submitted surveys as v3.0; drafts ask the observer.
   - RESOLVED: submitted `subalpin` surveys stay v3.0 and are not recomputed; drafts leave the cas blank and submit readiness asks the observer (ADR-003 "Surveys already recorded" and mapping table, plan 01.1-02; CH-7 for 01.8).
2. **CNPF source inconsistencies** (Ficus in Table 1; Pistacia shrub-only; linear C/D table without the sum).
   - Recommendation: list them in the comparison doc as "questions for CNPF" and keep the conservative reading: count only listed genera; the Pistacia decision goes to phase 2.
   - RESOLVED: recorded in the comparison's "Questions for CNPF and assumptions" section (plan 01.1-01) and in ADR-003 "Open questions" (plan 01.1-02); Ficus is not counted; Pistacia is decided in phase 2 (CH-12).
3. **Production impact numbers.**
   - Recommendation: the planner adds an owner task to run the read-only SQL. ADR-003 can be written without the numbers, stating that they are pending.
   - RESOLVED: the read-only SQL ships in ADR-003 "Open questions" (plan 01.1-02) with the numbers marked pending, and is listed as an owner item in 01.1-VALIDATION.md (plan 01.1-04). It does not block the decision (D-07 holds whatever the counts).
4. **Should BUG-3 (the "/10" display) be fixed in this milestone, and to what design?**
   - Recommendation: record it; the owner decides; the likely home is 01.8 or a quick task.
   - RESOLVED: recorded as BUG-3 in the comparison (plan 01.1-01) and as CH-11 / an open question in ADR-003 (plan 01.1-02); listed as an owner decision in 01.1-VALIDATION.md. Not fixed in 01.1.

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| Node / npm | lint, typecheck, unit tests | ✓ | v22.22.2 / 10.9.7 | — |
| `node_modules` | tests | ✓ (installed) | — | — |
| v3.2 PDF in the scratchpad | citation checks | ✓ | SHA-256 verified | re-download from D-01 URL |
| `pdftoppm` (Read tool PDF rendering) | reading the PDF | ✗ | — | `pypdf` with the cryptography stub (works) |
| Production DB | D-07 impact numbers | ✗ (not reachable from here) | — | owner runs the SQL on the VPS |

## Validation Architecture

### Test Framework
| Property | Value |
|----------|-------|
| Framework | Jest 29 + ts-jest (existing); shell grep/awk for the documents |
| Config file | `api/jest.unit.config.js`, `mobile/jest.unit.config.js` |
| Quick run command | `cd api && npx jest -c jest.unit.config.js test/ibp-rules.spec.ts && cd ../mobile && npx jest -c jest.unit.config.js src/app/ibp-scoring.test.ts` (both green on 2026-09-26: 18 + 4 tests) |
| Full suite command | `npm run lint && npm run typecheck && npm run test:unit && npm run format:check` |

### Phase Requirements → Test Map
| Req / criterion | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| REQ-QA-ibp-version / crit. 1 | Comparison covers A–J, the genus list, global score; source hash present | doc check | first block of "Deliverable checks" | ❌ doc created in this phase |
| crit. 2 | Every difference row has API, mobile and matrix impact cells | doc check | the awk row check | ❌ |
| crit. 3 | ADR-003 exists with a Decision line | doc check | `grep -qE '^\*\*Decision:\*\*' docs/technical/adr-003-ibp-method-version-v1.md` | ❌ |
| crit. 4 | D-07 statement (submitted keep score + version tag; drafts) | doc check | `grep -qi "method version"` + `grep -qi "submitted"` on the ADR | ❌ |
| crit. 5 | README and spec state the implemented version (v3.0) and the target (v3.2, ADR-003) | doc check | `grep -n "v3.0\|v3.2\|ADR-003" docs/references/README.md docs/specs/ibp-form-spec.md` | ✅ files exist |
| D-05 | Indexed in CLAUDE.md and docs/README.md | doc check | the grep lines above | ✅ files exist |
| D-08 | No code regressions | unit/lint/type/format | full suite command | ✅ |

### Sampling Rate
- **Per task commit:** the doc checks for the file touched + `npm run format:check` if any `.ts`/`.json` changed.
- **Per wave merge:** full suite command.
- **Phase gate:** all doc checks print nothing; the full suite is green.

### Wave 0 Gaps
None. The doc checks are shell one-liners and need no test files. The existing Jest suites cover code regressions.

## Security Domain

This phase changes documentation only.

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | no | — |
| V3 Session Management | no | — |
| V4 Access Control | no | — |
| V5 Input Validation | yes (finding only) | BUG-2: the server accepts G/H = 1, which violates the domain enum. Fix in 01.8 via the allowed sets in the rules engine (server-side is the trust boundary) |
| V6 Cryptography | no | SHA-256 is used only for source reproducibility |

| Pattern | STRIDE | Mitigation |
|---------|--------|------------|
| Out-of-domain factor scores submitted directly to the API (bypassing the mobile form) | Tampering | Enforce {0,2,5} for G, H, I, J server-side (CH-5) |
| Redistribution of third-party PDFs | (legal) | Never commit PDFs; D-03 paraphrase-only rule |

## Sources

### Primary (HIGH confidence)
- IBP FR v3.2 PDF (CNPF, 02/02/2026), SHA-256 `f130b3d6…d001`, read in full (all 28 pages) via text extraction: p. 2–28 as cited per row.
- Repository code and docs read 2026-09-26: `api/src/surveys/ibp-rules.service.ts`, `api/test/ibp-rules.spec.ts`, `api/src/surveys/surveys.service.ts`, `api/src/surveys/dtos/*.ts`, `api/migrations/001_init.sql`, `002_factor_results.sql`, `008_parcels_and_versioning.sql`, `api/src/surveys/public-map.queries.ts`, `mobile/src/app/ibp-scoring.ts` (+ test), `mobile/src/app/vegetation.ts`, `constants.ts`, `types.ts`, `brand-tokens.ts`, `mobile/src/i18n/fr/labels.ts`, `factor-detail.ts`, `home.ts`, `components.ts`, `mobile/src/ui/IbpScoreBadge.tsx`, `mobile/src/screens/home/SectorScoreCard.tsx`, `mobile/src/hooks/useSurveyForm.ts`, `mobile/src/hooks/useNearbyParcels.ts`, `mobile/src/screens/survey-detail/useLocalDraftSummary.ts`, `mobile/src/storage/db.ts`, `docs/specs/ibp-form-spec.md`, `docs/technical/ibp-validation-matrix-v1.md`, `docs/references/README.md`, `docs/technical/species-recognition-spike-measurements-v1.md` §2, ADR-002 header.

### Secondary (MEDIUM confidence)
- CNPF "IBP Fr v3" survey sheets 16/08/2021 (sheet "IBP Fr-ACA v3.210722"), mirrored at `https://kebbe.iefc.net/wp-content/uploads/2022/03/Explication_general_IBP.pdf`, SHA-256 `3751b6f6…b8e3`. An official v3-family text, but an earlier edition than the repo's 2023-03-23 citation, and ACA sheet only.

### Tertiary (LOW confidence / negative results)
- Web searches for the 2023 v3.0 PDFs and CNPF's version-change tables (`2023_10_01_IBP tab_modif.pdf`, `ibp_modif_entre_v3_0_et_v2_9.pdf`): URLs found, all return 404; Wayback availability API: no snapshots.

## Metadata

**Confidence breakdown:**
- v3.2 facts: HIGH. Read in full from the hashed official PDF.
- App baseline: HIGH. Read from the code, and the existing tests were run.
- v3.0 official-text column: MEDIUM. Taken from the 2021 v3 ACA sheet; the 2023 edition and the Mediterranean sheet were not available.
- Decision / D-07 analysis: HIGH. It follows mechanically from D-06 and the verified data-model facts.
- Pitfalls: HIGH. Each one is grounded in a cited file or line.

**Research date:** 2026-09-26
**Valid until:** stable. Re-check only if CNPF publishes a v3.3 or restores the v3.0 PDFs (check `https://www.cnpf.fr/ibp`).
