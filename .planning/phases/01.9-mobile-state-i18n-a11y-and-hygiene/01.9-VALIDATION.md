---
phase: 01.9
slug: mobile-state-i18n-a11y-and-hygiene
status: validated
pending: 01.9-32 (closing plan, blocked until phase 01.8 is merged)
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-26
---

# Phase 01.9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Plans 01.9-01 to 01.9-31 fill it; 01.9-32 (after phase 01.8) adds its closing section.
>
> The phase PR is #159 (https://github.com/florianlepont/cortege/pull/159). Some plans were written with "#158"; every mention in this file now says #159.

`nyquist_compliant: true` because every task of all 32 plans has an `<automated>` command, including the orchestrator-evidence checkpoints, which grep the recorded evidence. Every test those commands run either exists already or is created by a Wave 0 item below, or by the same task before the code it tests. The only manual verification is the owner's iPhone check (D-11). It is listed under Manual-Only and is not a sampling gap.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Mobile: Jest 29 + ts-jest (`testEnvironment: node`; not jest-expo), `@testing-library/react-native` 14 (`/pure`, async renderHook), `react-test-renderer` 19.2.3, `node:sqlite`-backed expo-sqlite mock. API: Jest 29 + ts-jest unit specs (`api/test/*.spec.ts`), Jest + Supertest E2E against PostgreSQL 16 (`api/test/*.e2e-spec.ts`, `*_test` databases only) |
| **Config file** | `mobile/jest.unit.config.js` (coverage ratchet per directory; new rows `./App.tsx` (01.9-01), `./src/state/` + `./src/i18n/` (01.9-09), `./src/navigation/` (01.9-18)), `api/jest.unit.config.js`, `api/jest.config.js` (E2E) |
| **Quick run command** | `npm --workspace mobile run test:unit -- <path-or-pattern>`; `npm --workspace api run test:unit -- <spec>` |
| **Full suite command** | `npm run lint && npm run typecheck && npm run test:unit && npm run format:check`, plus `env -u ACCESS_TOKEN_SECRET POSTGRES_DB=ibp_p19_NN_test OBJECT_STORAGE_MODE=local ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-NN flock /tmp/ibp-e2e.lock npm --workspace api run test:e2e` for plans touching the API (one database per plan, `pg_ctlcluster 16 main start`, role `ibp`/`ibp`) |
| **Phase tools** | `node mobile/scripts/structure-report.js <unused-styles\|long-files\|literals\|status-ids> [paths] [--max N]` (01.9-04; `literals` includes the object-label kind); ESLint D-06/D-07 gates (`cd mobile && npx eslint <paths> --max-warnings 0`); render-count harness `mobile/src/state/render-counts.test.tsx` with five scenarios and `RENDER_COUNTS_OUT=<file>` (01.9-01) |
| **CI and PRs** | Executors never push, open PRs or poll CI. The orchestrator pushes `claude/code-audit-complete-3sn99m` (PR #159) and supplies run evidence at the checkpoints of 01.9-06 (after wave 1) and 01.9-31. For 01.9-32 it opens the closing PR |
| **Native builds** | CI only (D-10): jobs added by 01.9-06 and proven on PR #159 before the dependency changes and again at the gate. Locally: `expo export`, `expo-doctor`, `expo prebuild -p android` |
| **MinIO mode** | Local Docker with the pinned `pgsty/minio` image, as in 01.7-13; CI job "E2E tests — API (MinIO mode)" |
| **Estimated runtime** | Mobile unit ~60-120 s; API unit ~60-90 s; E2E ~4-6 min per mode; native CI jobs ~20-45 min |

---

## Sampling Rate

- **Model identifiers and attribution:** none in code, this file or PR body drafts. Commit trailers are the orchestrator's concern, not copied anywhere else
- **After every task commit:** run the task's `<automated>` command (targeted suite, per-folder scanner and ESLint gates)
- **After every plan:** full mobile unit suite with coverage, lint, typecheck and format. Plans touching the API also run the full E2E suite on their own database
- **Before `/gsd:verify-work`:** 01.9-31 must be done (local gate, measurements, criterion evidence, PR #159 CI with native jobs, post-deploy probe, owner device check from main). 01.9-32 must also be done (after 01.8)
- **Max feedback latency:** 120 seconds (unit); one E2E run for API changes

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.9-01-T1 | 01 | 1 | REQ-AUD-mobile-state | T-01.9-01 | Harness mounts the real App with fake navigators and probes; storage mock returns fresh row copies | render harness | `npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx` | W0: `mobile/src/state/render-counts.test.tsx` (new) | ✅ green |
| 01.9-01-T2 | 01 | 1 | REQ-AUD-mobile-state | T-01.9-02 | Five before-counts pinned and saved (D-02); `./App.tsx` coverage row (B5) | render harness + coverage | plan 01 Task 2 `<automated>` | W0: `01.9-render-counts-before.json` | ✅ green |
| 01.9-02-T1/T2 | 02 | 1 | REQ-AUD-mobile-state | T-01.9-03 | useSurveySync, nearby parcels, parcel statuses, survey list tests on renderHook (D-01, C-4) | unit | plan 02 `<automated>` | ✅ (rewrite) | ✅ green |
| 01.9-03-T1..T3 | 03 | 1 | REQ-AUD-mobile-state | T-01.9-58 | Survey-ops, profile, network, editing, form, explorer tests on renderHook, assertions unchanged (D-01, C-4) | unit | plan 03 `<automated>` | ✅ (rewrite) | ✅ green |
| 01.9-04-T1 | 04 | 1 | REQ-AUD-mobile-state | T-01.9-04, T-01.9-05 | Unused styles, 400 lines, literals incl. object labels, status id leaks; ratchets | unit + script | `npm --workspace mobile run test:unit -- src/__checks__` | W0: `mobile/scripts/structure-report.js`, `mobile/src/__checks__/structure.test.ts` (new) | ✅ green |
| 01.9-04-T2 | 04 | 1 | REQ-AUD-i18n-a11y | T-01.9-05 | D-06 text gate and D-07 Pressable gate (warn) | lint | `npm run lint` | ✅ `mobile/.eslintrc.json` (edit) | ✅ green |
| 01.9-05-T1/T2 | 05 | 1 | REQ-AUD-i18n-a11y | T-01.9-06, T-01.9-07 | Typed catalogue, StatusMessage, sync errors by code, French permissions; src/i18n at 100% (no jest config edit) | unit + coverage | plan 05 `<automated>` | W0: `mobile/src/i18n/catalogue.test.ts` (new) | ✅ green |
| 01.9-06-T1 | 06 | 1 | REQ-AUD-hygiene | T-01.9-08, T-01.9-09 | Native Android/iOS jobs, pinned actions, no secrets | static (actionlint) | plan 06 Task 1 `<automated>` | ✅ `ci.yml` (edit) | ✅ green |
| 01.9-06-T2 | 06 | 1 | REQ-AUD-hygiene | — | Audit Statut rows 01.2–01.7 with PR links (D-12) | docs grep | plan 06 Task 2 `<automated>` | ✅ (edit) | ✅ green |
| 01.9-06-T3 | 06 | 1 | REQ-AUD-hygiene | T-01.9-10 | Native jobs green on PR #159 after wave 1 (orchestrator evidence) | checkpoint + grep | plan 06 Task 3 `<automated>` | n/a | ✅ green |
| 01.9-07-T1/T2 | 07 | 1 | REQ-AUD-mobile-state | T-01.9-11..14 | bbox filter, 400 on malformed/over-long, no echo, no Seq Scan, private excluded; docs; probes | unit + E2E | `test:e2e -- "public-map-bbox\|public-routes-explain"` then full E2E on `ibp_p19_07_test` | W0: `api/test/public-map-bbox.e2e-spec.ts` (new) | ✅ green |
| 01.9-08-T1/T2 | 08 | 1 | REQ-AUD-mobile-state | T-01.9-15, T-01.9-16 | Migration 2 additive/idempotent; list without payload parse; 500 rows no JSON.parse; DraftCard 0-100 | SQLite + render | plan 08 `<automated>` | W0: `surveys.completion.sqlite.test.ts`, `DraftCard.test.tsx` (new) | ✅ green |
| 01.9-09-T1..T3 | 09 | 2 | REQ-AUD-mobile-state | T-01.9-17..19 | Five contexts, one assembler, stable actions (useLayoutEffect ref, same-commit child test), memoised useSurveySync slices; state and i18n coverage rows | unit | plan 09 `<automated>` | W0: `useLatestCallback.test.ts`, `contexts.test.tsx` (new) | ✅ green |
| 01.9-10-T1/T2 | 10 | 2 | REQ-AUD-hygiene | T-01.9-20..22 | EmailService and SMTP gone; prod start ignores leftover keys; INFO line without echo; git grep gate | unit + E2E | plan 10 `<automated>` on `ibp_p19_10_test` | ✅ | ✅ green |
| 01.9-11-T1/T2 | 11 | 2 | REQ-AUD-i18n-a11y | T-01.9-23 | Survey-ops and profile messages French and id-free; test strings swapped only | unit + scanner | plan 11 `<automated>` | ✅ | ✅ green |
| 01.9-12-T1/T2 | 12 | 2 | REQ-AUD-mobile-state, REQ-AUD-i18n-a11y | T-01.9-24, T-01.9-25 | Detail split ≤ 400, 0 unused styles, Debug dev-only, French, Pressable role/label | scanner + render + lint | plan 12 `<automated>` | W0: `DetailTabBar.test.tsx`, `event-labels.test.ts` (new) | ✅ green |
| 01.9-13-T1/T2 | 13 | 2 | REQ-AUD-mobile-state, REQ-AUD-i18n-a11y | T-01.9-26 | Form split, French, Pressable role/label | scanner + render + lint | plan 13 `<automated>` | W0: `FactorsList.test.tsx` (new) | ✅ green |
| 01.9-14-T1/T2 | 14 | 2 | REQ-AUD-mobile-state, REQ-AUD-i18n-a11y | T-01.9-27 | Sign-in split and French | scanner + lint + unit | plan 14 `<automated>` | ✅ `AuthGateScreen.test.ts` (edit) | ✅ green |
| 01.9-15-T1/T2 | 15 | 2 | REQ-AUD-mobile-state, REQ-AUD-i18n-a11y | T-01.9-28 | Account split, French, token only in avatar | scanner + lint + render | plan 15 `<automated>` | W0: `IdentityCard.test.tsx` (new) | ✅ green |
| 01.9-16-T1/T2 | 16 | 2 | REQ-AUD-mobile-state, REQ-AUD-i18n-a11y | T-01.9-29 | Home ≤ 400; components and ui French, no dead styles | scanner + lint | plan 16 `<automated>` | ✅ | ✅ green |
| 01.9-17-T1/T2 | 17 | 2 | REQ-AUD-i18n-a11y | T-01.9-30 | Small screens and label modules French, object labels included | scanner + lint + unit | plan 17 `<automated>` | ✅ | ✅ green |
| 01.9-18-T1/T2 | 18 | 3 | REQ-AUD-mobile-state | T-01.9-31, T-01.9-32 | Memoised route per screen; status update → Settings only; keystroke → form screens only (D-02 after) | typecheck + render harness | plan 18 `<automated>` | ✅ harness (EXPECTED) | ✅ green |
| 01.9-19-T1/T2 | 19 | 3 | REQ-AUD-hygiene | T-01.9-SC, T-01.9-33..35 | Only lockfile plan: removals, supercluster after npm view; native module set unchanged; single copies; image smokes | manifest checks + script + docker | plan 19 `<automated>` | W0: `01.9-autolinking-{before,after}.json` | ✅ green |
| 01.9-20-T1..T3 | 20 | 3 | REQ-AUD-i18n-a11y | T-01.9-36, T-01.9-37 | Session, owner, network, central sync messages French and id-free | unit + scanner | plan 20 `<automated>` | ✅ | ✅ green |
| 01.9-21-T1..T3 | 21 | 4 | REQ-AUD-i18n-a11y | T-01.9-38, T-01.9-39 | Editing, GPS, bootstrap messages; validation catalogue; no React spy left | unit + scanner | plan 21 `<automated>` | ✅ | ✅ green |
| 01.9-22-T1 | 22 | 4 | REQ-AUD-mobile-state | T-01.9-40, T-01.9-41 | FlatList + memo rows; 500 surveys: window only, one-row re-render | render | `test:unit -- src/screens/survey-list` | W0: `survey-list-500.test.tsx` (new) | ✅ green |
| 01.9-22-T2 | 22 | 4 | REQ-AUD-mobile-state | — | Structural sharing in useSurveyList (B6); harness rows: status 0, keystroke 0, keystroke+autosave ≤ 1, one-survey 1 | unit + render harness | plan 22 Task 2 `<automated>` | ✅ | ✅ green |
| 01.9-23-T1/T2 | 23 | 4 | REQ-AUD-mobile-state | T-01.9-42, T-01.9-43 | bbox client, stale guard, dedupe; debounce; supercluster index per items; leaves at max zoom | unit | plan 23 `<automated>` | W0 (new) | ✅ green |
| 01.9-24-T1/T2 | 24 | 5 | REQ-AUD-mobile-state | T-01.9-44, T-01.9-45 | Navigation split ≤ 400, global typing, no `as any`, component= routes, app/styles.ts deleted | typecheck + harness + exports | plan 24 `<automated>` | ✅ | ✅ green |
| 01.9-25-T1/T2 | 25 | 6 | REQ-AUD-mobile-state, REQ-AUD-i18n-a11y | T-01.9-46, T-01.9-47 | 4 tabs, shared hide rule, reason codes (Release native), French titles | unit + render | `test:unit -- src/navigation` | W0: `tab-bar.test.ts`, `native-tabs-availability.test.ts`, `tabs.test.tsx` (new) | ✅ green |
| 01.9-26-T1/T2 | 26 | 7 | REQ-AUD-hygiene | T-01.9-59 | README-native reason codes; US-E2 search story | docs grep | plan 26 `<automated>` | ✅ | ✅ green |
| 01.9-27-T1/T2 | 27 | 7 | REQ-AUD-mobile-state, REQ-AUD-i18n-a11y | T-01.9-48 | List split ≤ 400, French (filter labels included), filter values unchanged | scanner + lint + unit | plan 27 `<automated>` | W0: `filter-options.test.ts` (new) | ✅ green |
| 01.9-28-T1..T3 | 28 | 7 | REQ-AUD-mobile-state, REQ-AUD-i18n-a11y | T-01.9-49, T-01.9-50 | Debounced bbox loading, no refit loop, memo/clustered markers, French, accessible | unit + render + lint | plan 28 `<automated>` | W0: `useMapViewport.test.ts`, `markers.test.tsx` (new) | ✅ green |
| 01.9-30-T1/T2 | 30 | 7 | REQ-AUD-hygiene | T-01.9-60 | CLAUDE.md facts for this phase | docs grep | plan 30 `<automated>` | ✅ | ✅ green |
| 01.9-29-T1/T2 | 29 | 8 | REQ-AUD-i18n-a11y, REQ-AUD-mobile-state | T-01.9-51, T-01.9-52 | StatusMessage-only setters; gates at error; ratchets at 0; leftovers in declared files only | typecheck + lint + unit | plan 29 `<automated>` | ✅ | ✅ green |
| 01.9-31-T1 | 31 | 9 | all | T-01.9-53 | Full local gate, measurements, criterion evidence, ratchet (App row raised) | full suite | plan 31 Task 1 `<automated>` | ✅ | ✅ green |
| 01.9-31-T2 | 31 | 9 | all | T-01.9-55 | Every CI job green on PR #159, native builds included (orchestrator evidence); owner merges | checkpoint + grep | plan 31 Task 2 `<automated>` | n/a | ✅ green |
| 01.9-31-T3 | 31 | 9 | REQ-AUD-mobile-state | T-01.9-54 | Post-deploy bbox probes, then owner device check from main (D-11) | simulated production + owner (iPhone only) | plan 31 Task 3 `<automated>` | n/a | ✅ green |
| 01.9-32-T1..T3 | 32 | 10 | REQ-AUD-hygiene, REQ-AUD-i18n-a11y | T-01.9-56, T-01.9-57 | After 01.8: docs sweep, audit links complete, validation keys checked; closing PR evidence from the orchestrator | grep + unit + checkpoint | plan 32 `<automated>` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `mobile/src/state/render-counts.test.tsx` + `01.9-render-counts-before.json` — five-scenario render-count baseline on the pre-phase code, with fresh row copies from the storage mock (01.9-01, D-02, B6), and the `./App.tsx` coverage row (B5)
- [x] All ten React-spy hook tests on renderHook before the context refactor: four in 01.9-02, six in 01.9-03 (D-01, C-4)
- [x] `mobile/scripts/structure-report.js` + `mobile/src/__checks__/structure.test.ts` — unused styles, 400 lines, literals (object labels included), status id leaks (01.9-04)
- [x] ESLint D-06/D-07 overrides in `mobile/.eslintrc.json` at warn (01.9-04; error in 01.9-29)
- [x] `mobile/src/i18n/**` catalogue skeleton, one section file per owning plan, at 100% coverage until its row exists (01.9-05)
- [x] CI jobs `native-android` / `native-ios` proven on PR #159 before any dependency change (01.9-06, D-10): run 36239341355 (14be3a1)
- [x] `api/test/public-map-bbox.e2e-spec.ts` (01.9-07); `mobile/src/storage/surveys.completion.sqlite.test.ts` and `DraftCard.test.tsx` (01.9-08)
- [x] Coverage rows `./src/state/`, `./src/i18n/` (01.9-09) and `./src/navigation/` (01.9-18) (C-8)
- [x] Autolinking baseline JSON saved before any dependency change (01.9-19)
- New package: `supercluster@9.1.0` (01.9-19; owner-approved 2026-09-26, npm view gate at install)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions | Result |
|----------|-------------|------------|-------------------|--------|
| Native liquid-glass tab bar with 4 tabs in a Release build from main, each tab opens (Explorer shows surveys from the deployed bbox API), search in Mes Relevés; offline cold start keeps surveys (01.5 criterion 7) | REQ-AUD-mobile-state | CI cannot log in or see the native tab bar, and the offline cold start needs a real phone (D-11). This is the only owner step besides merging PRs. It runs after PR #159 is merged and deployed (W4) | 01.9-31 Task 3 French checklist (6 steps) | ✅ approved by the owner on 2026-09-26 ("Je valide l'ensemble des tests.") |

---

## Local gate

Run on 2026-09-26 in the 01.9-31 worktree on the integrated phase (plans 01-30 merged, head `83c538f`), after a clean `npm ci` in the worktree (no symlinks). PostgreSQL 16 (`pg_ctlcluster 16 main`), gate database `ibp_p19_31_test`. Every E2E run had `ACCESS_TOKEN_SECRET` unset, `ATTACHMENTS_UPLOAD_DIR=/tmp/ibp-uploads-p19-31`, and ran under `flock /tmp/ibp-e2e.lock`. Nothing had to be fixed: every command passed on its first run.

| # | Command | Result | Duration |
|---|---------|--------|----------|
| 1 | `npm ci` | 1337 packages added from the lockfile; lockfile unchanged | 32 s |
| 2 | `npm run lint` | exit 0, 0 errors and 0 warnings (mobile + api, D-06/D-07 gates at error) | 21 s |
| 3 | `npm run typecheck` | exit 0 (mobile `tsc --noEmit`, api `tsc -p tsconfig.build.json`) | 15 s |
| 4 | `npm run format:check` | "All matched files use Prettier code style!" | 10 s |
| 5 | `npm --workspace api run test:unit:coverage` (before the ratchet) | 29 suites, 638/638 passed, thresholds met | 31 s |
| 6 | `npm --workspace mobile run test:unit:coverage` (before the ratchet) | 81 suites, 1050/1050 passed, thresholds met | 53 s |
| 7 | `node scripts/coverage-by-directory.js mobile` / `api`, ratchet of both `jest.unit.config.js` | 10 mobile rows and 1 api row raised, none lowered (table below) | — |
| 8 | Rows 5 and 6 again, after the ratchet | api 638/638, mobile 1050/1050, all new thresholds met | 31 s / 53 s |
| 9 | `RENDER_COUNTS_OUT=<phase dir>/01.9-render-counts-after.json npm --workspace mobile run test:unit -- src/state/render-counts.test.tsx` | 5/5 scenarios pass against the pinned `EXPECTED`; JSON written (`gitHead` 83c538f) | ~5 s |
| 10 | `node mobile/scripts/structure-report.js unused-styles --max 0`, `long-files --max 0`, `status-ids --max 0`, `literals src App.tsx --max 0` | `unused-styles: 0`, `long-files: 0`, `status-ids: 0`, `literals: 0`; each exit 0 | < 5 s |
| 11 | `cd mobile && npx eslint "src/**/*.{ts,tsx}" App.tsx --max-warnings 0` | exit 0, no output | ~20 s |
| 12 | `grep -rln "spyOn(React" mobile/src`; `grep -rn "useNavigation() as" mobile/src`; `grep -rn "as any" mobile/src/navigation` | all three empty | — |
| 13 | `npm --workspace mobile run test:unit -- "survey-list-500\|surveys.completion"` | 2 suites, 15/15 passed | ~5 s |
| 14 | `npm --workspace mobile run test:unit -- useSurveyList` | structural-sharing (B6) cases pass | ~3 s |
| 15 | Autolinking capture of 01.9-19 on the final tree (`expo-modules-autolinking resolve -p android\|ios --json`, `react-native-config -p android --json`, `npm ls react react-native expo --all`) | `moduleNames` identical to `01.9-autolinking-before.json` and `-after.json` (29 entries); Expo module versions identical; one copy each of react 19.2.3, react-native 0.86.3, expo 57.0.24 | ~30 s |
| 16 | `cd mobile && EXPO_DOCTOR_SKIP_DEPENDENCY_VERSION_CHECK=1 npx --yes expo-doctor@1.20.4` | "20/20 checks passed. No issues detected!" | 11 s |
| 17 | `npx expo export --platform ios` / `--platform android` (output in the session scratchpad) | iOS: 1490 modules, one Hermes bundle (3.8 MB); Android: 1486 modules, one Hermes bundle (3.8 MB); both exit 0 | 19 s / 20 s |
| 18 | Full E2E, local mode (`OBJECT_STORAGE_MODE=local`) | 25 suites passed; 177 passed, 3 skipped (the `itMinio` cases), 180 total; 45.8 s | 48 s |
| 19 | Full E2E, MinIO mode (container `p19-31-minio`, pinned `pgsty/minio:RELEASE.2026-08-04T00-00-00Z@sha256:b6bfe7239bfc…e602372`, host port 19731, the ci.yml e2e-minio env with `OBJECT_STORAGE_ENDPOINT=http://127.0.0.1:19731`) | 25 suites passed; 179 passed, 1 skipped (the `itLocal` case), 180 total; 33.4 s. Container stopped and removed afterwards | 35 s |
| 20 | Owner-check simulation against the built API (`node api/dist/main.js`, NODE_ENV=test, MinIO mode, port 3100, `ibp_p19_31_test`): `devices`, then stop/start of the same API and `restart`, then `config-refusal`, `deploy-guard` | devices 27/27, restart 4/4, config-refusal 10/10, deploy-guard 16/16 | ~1 min |
| 21 | Same API, `production` phase (read-only, includes the 01.9-07 bbox probes), `SIM_FILES_HEALTH` pointed at the local MinIO | 6/8. Map items 200 (3 items), mainland-France bbox 200 (3 items), `bbox=not-a-bbox` 400 without echo, /health 200, /surveys 401, MinIO 200. The two FAILs are expected on a NODE_ENV=test API and only mean something against production: `/debug/test-token` answers 201 in test mode, and the development CORS policy reflects the origin. Extra probe: a bbox in the Pacific (`150,-40,151,-39`) answers `{"items":[]}` | < 10 s |

Environment notes:
- In this sandbox `localhost:19731` resolves to `::1` first and the Docker port proxy resets that connection, so the MinIO endpoint used `127.0.0.1`. CI keeps `http://localhost:9000`.
- The API process of rows 20-21 was stopped and the MinIO container removed at the end (`docker ps -a` empty).
- The scratchpad E2E env file still exports `SMTP_ENABLED=false`. The key is unknown to the config since 01.9-10 and is ignored, which row 18 confirms again. In MinIO mode it was unset, as in ci.yml.

---

## Measurements

Measured by 01.9-31 Task 1 on head `83c538f` (plans 01-30 integrated). Commands are the numbered rows of "Local gate".

### Render counts (D-02)

| Scenario | Before (01.9-01) | After (01.9-31) |
|----------|------------------|-----------------|
| initialMount | 16 screen renders (2 per mounted screen) + 19 rows = 35 | 11 screen renders + 10 rows = 21 (the FlatList mounts its initial window only) |
| statusUpdate | 8 screens (every mounted screen) + 19 rows = 27 | 1 (Settings, which shows the status) + 0 rows = 1 |
| formKeystroke | 8 screens + 19 rows = 27 | 3 (survey form, factor detail, parcel selection) + 0 rows = 3 |
| formKeystrokeAutosave | 18 screens (all twice) + 38 rows = 56 | 7 screens once + 1 row = 8 |
| oneSurveyRefresh | 9 screens + 19 rows = 28 | 4 (home, list, detail, map) + 1 row = 5 |

Per-screen numbers: `01.9-render-counts-before.json` (`gitHead` 9f2e679, pre-phase) and `01.9-render-counts-after.json` (`gitHead` 83c538f). The harness pins the after numbers in `EXPECTED`, so any regression fails the unit suite. Account and Settings no longer re-render on a keystroke, an autosave or a list refresh; the form screens no longer re-render on a status update or a list refresh; list rows re-render only when their survey object changes.

### Structure and text gates (D-04, D-06, D-07)

| Check | Result |
|-------|--------|
| Unused style keys (committed script, must report zero) | `unused-styles: 0` (baseline 301 in 01.9-04) |
| Files over 400 lines under screens/ and navigation/ | `long-files: 0`; the longest are `src/screens/SurveyListScreen.tsx` (377) and `src/screens/survey-form/useParcelMap.ts` (349) |
| User-facing literals outside src/i18n (object labels included) | `literals: 0` over `src` and `App.tsx` (baseline 635) |
| Status id/raw-error leaks | `status-ids: 0` (baseline 66) |
| ESLint D-06/D-07 gates (errors, zero warnings) | `npx eslint "src/**/*.{ts,tsx}" App.tsx --max-warnings 0` exit 0; `npm run lint` 0 warnings |
| React spies left in mobile tests | none (`grep -rln "spyOn(React" mobile/src` empty) |
| `useNavigation() as` / `as any` in navigation | none (both greps empty) |

### 500-survey list (D-03)

| Check | Result |
|-------|--------|
| listLocalSurveys on 500 rows without JSON.parse | pass: "500 surveys list without a single JSON.parse and without reading payload_json" (`surveys.completion.sqlite.test.ts`, 5/5) |
| FlatList mounts only the initial window; one survey change re-renders one row | pass: `survey-list-500.test.tsx` 10/10 (initial window only, exactly one row on a replaced survey, none on a parent re-render or off-screen selection) |
| Refresh with unchanged rows keeps row identity (structural sharing) | pass: `useSurveyList` B6 cases (same array and objects on equal rows, only the changed object replaced) |

### Hygiene (D-09)

| Check | Result |
|-------|--------|
| Native module names before = after (autolinking) | identical: 29 names (8 community, 21 Expo) on the final tree = before = 01.9-19 after; Expo module versions unchanged |
| Single copy of react / react-native / expo | one `node_modules/{react,react-native,expo}` each, at the root; `npm ls` exit 0 |
| expo-doctor, expo export (iOS, Android) | expo-doctor 20/20; export iOS 1490 modules, Android 1486 modules, exit 0 |
| API image: no mobile deps, boots | 01.9-19 local image smoke (uid 1000, no react/expo/react-native or removed packages, /v1/health 200); "Docker image check" green on CI run 36244081522 (c404428) |

### Coverage ratchet

Thresholds are the floor of the measurement of row 6 (mobile) and row 5 (api); a row is only raised, never lowered. Format: statements / branches / functions / lines.

**Mobile (`mobile/jest.unit.config.js`)**

| Row | Before | Measured | After |
|-----|--------|----------|-------|
| `./App.tsx` | 100 / 100 / 100 / 100 (raised in 01.9-09) | 100 / 100 / 100 / 100 | 100 / 100 / 100 / 100 (equal, already at the maximum) |
| `./src/state/` | 95 / 75 / 87 / 97 | 96 / 75 / 90 / 97 | 96 / 75 / 90 / 97 |
| `./src/navigation/` | 100 / 96 / 100 / 100 | 100 / 98 / 100 / 100 | 100 / 98 / 100 / 100 |
| `./src/i18n/` | 100 / 58 / 100 / 100 | 100 / 73 / 100 / 100 | 100 / 73 / 100 / 100 |
| `./src/api/` | 95 / 97 / 91 / 95 | 95 / 97 / 91 / 95 | unchanged |
| `./src/app/` | 61 / 55 / 45 / 63 | 86 / 73 / 93 / 91 | 86 / 73 / 93 / 91 |
| `./src/components/` | 6 / 0 / 0 / 6 | 22 / 11 / 10 / 22 | 22 / 11 / 10 / 22 |
| `./src/hooks/` | 85 / 69 / 83 / 87 | 90 / 78 / 94 / 90 | 90 / 78 / 94 / 90 |
| `./src/screens/` | 10 / 3 / 7 / 10 | 46 / 33 / 40 / 46 | 46 / 33 / 40 / 46 |
| `./src/storage/` | 92 / 80 / 90 / 94 | 92 / 81 / 91 / 94 | 92 / 81 / 91 / 94 |
| `./src/ui/` | 29 / 28 / 21 / 30 | 44 / 29 / 31 / 46 | 44 / 29 / 31 / 46 |
| `global` | 100 statements / 100 lines | no file left outside a row | unchanged |

Mobile "All files": 76.46 / 59.02 / 75.72 / 77.22 (01.9-19 measured 70.54 / 52.39 / 67.27 / 71.26).

**API (`api/jest.unit.config.js`)**

| Row | Before | Measured | After |
|-----|--------|----------|-------|
| `./src/users/` | 74 / 67 / 51 / 74 | 92 / 91 / 58 / 92 | 92 / 91 / 58 / 92 (EmailService and its untested code left in 01.9-10) |
| `global`, `auth`, `common`, `config`, `database`, `debug`, `reports`, `storage`, `surveys` | as before | equal to the thresholds | unchanged |

---

## Criterion evidence

Filled by 01.9-31 Task 1 (W8). This table maps each criterion wording that cannot be met literally to the evidence used instead.

| Criterion wording | Substitute evidence | Decision | Result |
|-------------------|---------------------|----------|--------|
| 1 — React DevTools profile before/after | Committed render-count harness, before/after JSON | D-02 | Met: a status update goes from 27 renders to 1, a keystroke from 27 to 3, keystroke + autosave from 56 to 8, a one-survey refresh from 28 to 5 (Render counts table); pinned in `EXPECTED` |
| 2 — stays fluid with 500 surveys | 500-survey render test + no-parse storage test + structural sharing | D-03 | Met: 500-survey FlatList mounts only its window and re-renders one row per changed survey; listing 500 rows does no `JSON.parse`; unchanged rows keep identity |
| 2 — the 158 unused style keys are gone | Script reports zero (the count was re-measured at 301) | D-04 | Met: `unused-styles: 0`; no screen or navigation file over 400 lines |
| 5 — the unused tab library is removed | Neither library is unused; both kept by owner decision, with evidence in 01.9-19-SUMMARY | D-08 | Met by substitution: `@react-navigation/bottom-tabs` (Android + JS fallback), `@bottom-tabs/react-navigation` and `react-native-bottom-tabs` (native iOS bar) are all imported; autolinking unchanged (29 modules) |
| 5 — native iOS and Android builds still passing | Native CI jobs on PR #159 + the owner's Release build | D-10, D-11 | Proven after the dependency cleanup: run [36244081522](https://github.com/florianlepont/cortege/actions/runs/36244081522) on `c404428`, every job green including both native builds (baseline before the cleanup: [36239341355](https://github.com/florianlepont/cortege/actions/runs/36239341355) on `14be3a1`). The run on the final head and the owner's Release build are recorded in "CI Evidence" (Task 2) and "Owner device check" (Task 3) |
| 6 — CLAUDE.md and docs match; audit links each finding | 01.9-30 (this phase's facts) + 01.9-32 (after 01.8) | D-12 | Split as decided: this phase's facts are in CLAUDE.md (01.9-30) and the audit Statut rows for 01.2-01.7 are written (01.9-06); the docs sweep, the 01.8/01.9 Statut links and the validation messages wait for 01.8 in 01.9-32 |

---

## CI Evidence

PR: https://github.com/florianlepont/cortege/pull/159 (branch `claude/code-audit-complete-3sn99m`).

Earlier native runs on this PR (supplied by the orchestrator):

| Run | Head | What it proves | Conclusion |
|-----|------|----------------|------------|
| [36239341355](https://github.com/florianlepont/cortege/actions/runs/36239341355) | `14be3a1` | Baseline native builds before any dependency change (01.9-06): Android `BUILD SUCCESSFUL in 18m 33s` (gradle step 18 min 35 s), iOS `** BUILD SUCCEEDED **` (xcodebuild step 9 min 45 s) | success |
| [36244081522](https://github.com/florianlepont/cortege/actions/runs/36244081522) | `c404428` | Native builds after the dependency cleanup (01.9-19); every job green, including "Native build — Android" and "Native build — iOS" | success |

### Final run on the latest head (Task 2)

- Head: `cd48cd40891bce029bde2ba700f57ea9e2156de0`. This is the phase branch with 01.9-31 Task 1, plus `origin/main` (PR #160, phase 1 closed) merged in. That merge conflicted only on ROADMAP.md and STATE.md.
- CI run: [36247034895](https://github.com/florianlepont/cortege/actions/runs/36247034895). Conclusion: success, 14:00:30 → 14:16:23 UTC.
- CodeQL run: [36247034850](https://github.com/florianlepont/cortege/actions/runs/36247034850). Conclusion: success, 14:00:30 → 14:01:58 UTC.

| Job | Conclusion | Start → end (UTC) | Duration |
|-----|------------|-------------------|----------|
| Detect changed paths | success | 14:00:32 → 14:00:41 | 9 s |
| Lint, format, typecheck (incl. actionlint) | success | 14:00:34 → 14:01:23 | 49 s |
| Dependency audit | success | 14:00:32 → 14:00:47 | 15 s |
| Unit tests — API | success | 14:00:43 → 14:01:23 | 40 s |
| Unit tests — Mobile | success | 14:00:43 → 14:01:28 | 45 s |
| E2E tests — API (run 1, run 2 on the same database, reset assertion) | success | 14:00:43 → 14:02:02 | 1 min 19 s |
| E2E tests — API (MinIO mode) | success | 14:00:43 → 14:01:37 | 54 s |
| Mobile build check (expo-doctor + expo export) | success | 14:00:43 → 14:01:31 | 48 s |
| Docker image check (all 5 smoke tests) | success | 14:00:43 → 14:01:51 | 1 min 8 s |
| Native build — Android | success ([job 108418059963](https://github.com/florianlepont/cortege/actions/runs/36247034895/job/108418059963)) | 14:00:44 → 14:16:16; `gradle assembleRelease` 14:01:19 → 14:16:13 | 15 min 32 s (gradle 14 min 54 s) |
| Native build — iOS | success ([job 108418059939](https://github.com/florianlepont/cortege/actions/runs/36247034895/job/108418059939)) | 14:00:47 → 14:14:05; `xcodebuild` Release, simulator, unsigned 14:02:17 → 14:13:56 | 13 min 18 s (xcodebuild 11 min 39 s) |
| CI OK | success | 14:16:18 → 14:16:22 | 4 s |
| CodeQL | success | 14:00:30 → 14:01:58 | 1 min 28 s |
| Build & push Docker image | skipped (PR run; main pushes only) | — | — |

Log lines quoted from the jobs:

- Android: `BUILD SUCCESSFUL in 14m 53s` and `584 actionable tasks: 584 executed`
- iOS: `** BUILD SUCCEEDED **`

**Comparison with the 01.9-06 baseline (run 36239341355, `14be3a1`, before the dependency cleanup):**
- Android: the gradle step took 14 min 54 s, down from 18 min 35 s. Both runs executed all 584 tasks from a cold Gradle cache, so the task count is unchanged.
- iOS: the xcodebuild step took 11 min 39 s, up from 9 min 45 s.
- The phase's changes (dependency cleanup, supercluster, the 4-tab navigation) did not break either native build.

**Merge:** the owner marked PR #159 ready for review at 14:02 UTC and merged it into `main` at **2026-09-26 15:00 UTC** (merge commit `c4fcad2`). The main-push CI run [36250412409](https://github.com/florianlepont/cortege/actions/runs/36250412409) succeeded (15:00:18 → 15:20:21 UTC); it built and pushed the API image.

---

## Post-deploy

| Check | Result |
|-------|--------|
| /v1/health answers 200 after the merge | ✅ 16:02 UTC: `GET https://cortege.algernon.ovh/v1/health` → `{"status":"ok","service":"cortege-api","timestamp":"2026-09-26T16:02:00.576Z"}` |
| Production probe: map items with a bbox (200, items array) | ✅ `/public/map-items?bbox=<mainland France>` → 200 with an items array. The array is empty (0 items), because production has no public survey yet. The 200 still proves the new image is live: the pre-phase image (`main` before the merge) has no `bbox` field in `PublicMapItemsQueryDto` and runs the ValidationPipe with `forbidNonWhitelisted: true`, so it would answer 400 to any `bbox` |
| Production probe: malformed bbox (400) | ✅ `bbox=not-a-bbox` → 400, value not echoed |

Command, run by the orchestrator at 16:02 UTC (read-only): `SIM_BASE=https://cortege.algernon.ovh/v1 node scripts/owner-check-simulation.mjs production`. Result: **8/8 passed**:
- /health 200
- POST /debug/test-token 404
- GET /surveys without a token 401
- /public/map-items 200 with an items array (0 items)
- mainland-France bbox 200 with an items array (0 items)
- `bbox=not-a-bbox` 400 without echo
- a foreign-origin preflight gets no Allow-Origin and no Allow-Credentials
- MinIO health (`cortege-files.algernon.ovh`) 200

Limit: with 0 public surveys in production, the probes check the response shape, not the filtering itself. The E2E suite covers the filtering (`public-map-bbox.e2e-spec.ts`, and local gate rows 18-19). Explorer on the owner's phone (step 4) will show surveys once some are public. Until then, step 4 of the owner check expects Explorer to open, possibly with an empty map.

---

## Owner device check

Réponse du propriétaire, le 2026-09-26 : « Je valide l'ensemble des tests. » Elle couvre les 6 étapes, faites sur une build Release depuis `main` contre la production (T-01.9-54).

| Étape | Résultat |
|-------|----------|
| 1. main à jour, npm install | ✅ approuvé. La branche locale `main` n'avait pas d'upstream : `git branch --set-upstream-to=origin/main main` avant le `git pull`, sans effet sur le code |
| 2. Ligne NATIVE_TABS=false absente de mobile/.env | ✅ approuvé. La ligne `EXPO_PUBLIC_ENABLE_NATIVE_TABS=false` a été supprimée (sed), puis `grep NATIVE_TABS mobile/.env` ne renvoie plus rien |
| 3. Build Release installée sur l'iPhone | ✅ approuvé |
| 4. Barre « verre liquide », 4 onglets, chacun s'ouvre, la carte d'Explorer s'affiche (vide tant qu'aucun relevé n'est public en production) | ✅ approuvé |
| 5. Recherche dans Mes Relevés | ✅ approuvé |
| 6. Mode avion, app fermée puis rouverte : relevés présents | ✅ approuvé (01.5 criterion 7 closed) |

---

## Closing plan (after phase 01.8)

_Filled by 01.9-32 once phase 01.8 is merged. It will hold the docs-sweep fact table, the completed audit Statut table, the validation message keys, and the closing PR's CI run from the orchestrator._
